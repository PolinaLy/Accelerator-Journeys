(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
"use strict";

var _iosVhFix = require("./utils/ios-vh-fix");
var _heroSwiper = require("./modules/hero-swiper");
var _video = require("./modules/video");
var _audioPlayer = require("./modules/audio-player");
var _toursSwiper = require("./modules/tours-swiper");
var _trainingSwiper = require("./modules/training-swiper");
var _reviewsSwiper = require("./modules/reviews-swiper");
var _advantagesSwiper = require("./modules/advantages-swiper");
var _photogallerySwiper = require("./modules/photogallery-swiper");
var _form = require("./modules/form-validate/form");
require("./modules/open-menu");
require("./modules/leaflet");
var _map = require("./modules/map");
var mediaQuery = window.matchMedia('(min-width: 1200px)');

// ---------------------------------

window.addEventListener('DOMContentLoaded', function () {
  // Utils
  // ---------------------------------

  (0, _iosVhFix.iosVhFix)();

  // Modules
  // ---------------------------------

  // все скрипты должны быть в обработчике 'DOMContentLoaded', но не все в 'load'
  // в load следует добавить скрипты, не участвующие в работе первого экрана
  window.addEventListener('load', function () {
    (0, _heroSwiper.initHeroSwiper)();
    (0, _video.initVideo)();
    (0, _audioPlayer.initAudio)();
    (0, _toursSwiper.initToursSwiper)();
    (0, _trainingSwiper.initTrainingSwiper)();
    (0, _reviewsSwiper.initReviewsSwiper)();
    if (mediaQuery.matches) {
      (0, _advantagesSwiper.initAdvantagesSwiper)();
    }
    (0, _photogallerySwiper.initPhotogallerySwiper)();
    var form = new _form.Form();
    window.form = form;
    form.init();
    (0, _map.initMap)();
  });
});

// ---------------------------------

// ❗❗❗ обязательно установите плагины eslint, stylelint, editorconfig в редактор кода.

// привязывайте js не на классы, а на дата атрибуты (data-validate)

// вместо модификаторов .block--active используем утилитарные классы
// .is-active || .is-open || .is-invalid и прочие (обязателен нейминг в два слова)
// .select.select--opened ❌ ---> [data-select].is-open ✅

// выносим все в дата атрибуты
// url до иконок пинов карты, настройки автопрокрутки слайдера, url к json и т.д.

// для адаптивного JS используется matchMedia и addListener
// const breakpoint = window.matchMedia(`(min-width:1024px)`);
// const breakpointChecker = () => {
//   if (breakpoint.matches) {
//   } else {
//   }
// };
// breakpoint.addListener(breakpointChecker);
// breakpointChecker();

// используйте .closest(el)

},{"./modules/advantages-swiper":2,"./modules/audio-player":3,"./modules/form-validate/form":5,"./modules/hero-swiper":11,"./modules/leaflet":12,"./modules/map":13,"./modules/open-menu":14,"./modules/photogallery-swiper":15,"./modules/reviews-swiper":16,"./modules/tours-swiper":17,"./modules/training-swiper":18,"./modules/video":19,"./utils/ios-vh-fix":21}],2:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initAdvantagesSwiper = void 0;
var initAdvantagesSwiper = function initAdvantagesSwiper() {
  new Swiper('.advantages__swiper', {
    navigation: {
      nextEl: '.advantages__swiper .slider-button--next',
      prevEl: '.advantages__swiper .slider-button--prev'
    },
    // отключение скролла
    simulateTouch: false,
    loop: true,
    breakpoints: {
      768: {
        slidesPerView: "auto"
      },
      1200: {
        slidesPerView: 3,
        spaceBetween: 30,
        initialSlide: 1
      }
    }
  });
};
exports.initAdvantagesSwiper = initAdvantagesSwiper;

},{}],3:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initAudio = void 0;
var initAudio = function initAudio() {
  var audio = document.querySelector('iframe');
  var buttonPlay = document.querySelector('.hero__audio-player-button');
  var audioPlayerImage = document.querySelector('.audio-player-image');
  if (audio && buttonPlay) {
    buttonPlay.addEventListener('click', function () {
      if (audio.classList.contains('is-active')) {
        audio.classList.remove('is-active');
      } else {
        audio.classList.remove('visually-hidden');
        audio.classList.add('is-active');
        buttonPlay.classList.add('visually-hidden');
        audioPlayerImage.classList.add('visually-hidden');
      }
    });
  }
};
exports.initAudio = initAudio;

},{}],4:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.callbacks = void 0;
var baseSuccessCallback = function baseSuccessCallback(event) {
  event.preventDefault();
  // В данном колбеке бэкендер, либо разработчик при необходимости будет писать запрос на отправку формы на сервер и обрабатывать возможные ошибки или успешную отправку формы на сервер
};

var baseErrorCallback = function baseErrorCallback(event) {
  event.preventDefault();
  // Данный коллбек используется при необходимости выполнить какое-либо действие помимо показа ошибок при попытке отправить неккорректные данные, он не связан с запросами на сервер
};

var callbacks = {
  base: {
    // Сбросс формы
    reset: true,
    // Таймаут сброса формы
    resetTimeout: 500,
    successCallback: baseSuccessCallback,
    errorCallback: baseErrorCallback
  }
};
exports.callbacks = callbacks;

},{}],5:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Form = void 0;
var _validator = require("./validator");
var _callback = require("./callback");
var _initPhoneInput = require("./init-phone-input");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var Form = /*#__PURE__*/function () {
  function Form() {
    _classCallCheck(this, Form);
    this._validator = new _validator.Validator();
    this._initPhoneInput = _initPhoneInput.initPhoneInput;
    this._callbacks = _callback.callbacks;
  }
  _createClass(Form, [{
    key: "_resetSelect",
    value: function _resetSelect(select) {
      var nativeSelect = select.querySelector('select');
      var activeIndex = nativeSelect.options.selectedIndex;
      var selectedOption = nativeSelect.options[activeIndex];
      var buttonText = select.querySelector('.custom-select__text');
      var selectItems = select.querySelectorAll('.custom-select__item');
      buttonText.textContent = selectedOption.textContent;
      selectItems.forEach(function (item, index) {
        if (index === activeIndex - 1) {
          item.setAttribute('aria-selected', 'true');
          return;
        }
        item.setAttribute('aria-selected', 'false');
      });
      if (!nativeSelect.value) {
        select.classList.remove('not-empty');
        select.classList.remove('is-valid');
      }
    }
  }, {
    key: "_resetSelects",
    value: function _resetSelects(form) {
      var _this = this;
      var selects = form.querySelectorAll('[data-select]');
      selects.forEach(function (select) {
        _this._resetSelect(select);
      });
    }
  }, {
    key: "reset",
    value: function reset(form) {
      var _this2 = this;
      form.reset();
      form.querySelectorAll('.is-invalid').forEach(function (item) {
        return item.classList.remove('is-invalid');
      });
      form.querySelectorAll('.is-valid').forEach(function (item) {
        return item.classList.remove('is-valid');
      });
      form.querySelectorAll('.input-message').forEach(function (item) {
        return item.remove();
      });
      setTimeout(function () {
        _this2._resetSelects(form);
      });
    }
  }, {
    key: "initPhoneInput",
    value: function initPhoneInput(parent) {
      this._initPhoneInput(parent);
    }
  }, {
    key: "validateForm",
    value: function validateForm(form) {
      return this._validator.validateForm(form);
    }
  }, {
    key: "validateFormElement",
    value: function validateFormElement(item) {
      return this._validator.validateFormElement(item);
    }
  }, {
    key: "_onFormSubmit",
    value: function _onFormSubmit(event) {
      var _this3 = this;
      var callback = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
      if (this.validateForm(event.target) && callback) {
        this._callbacks[callback].successCallback(event);
        if (this._callbacks[callback].reset) {
          setTimeout(function () {
            _this3.reset(event.target);
          }, this._callbacks[callback].resetTimeout ? this._callbacks[callback].resetTimeout : 500);
        }
        return;
      }
      if (!this.validateForm(event.target) && callback) {
        this._callbacks[callback].errorCallback(event);
        return;
      }
    }
  }, {
    key: "_onFormInput",
    value: function _onFormInput(item) {
      this.validateFormElement(item);
    }
  }, {
    key: "_initValidate",
    value: function _initValidate(parent) {
      var _this4 = this;
      var form = parent.querySelector('form');
      if (!form) {
        return;
      }
      var phoneParents = form.querySelectorAll('[data-validate-type="phone"]');
      phoneParents.forEach(function (item) {
        return _this4._initPhoneInput(item);
      });
      var callback = parent.dataset.callback;
      form.noValidate = true;
      form.addEventListener('submit', function (event) {
        event.preventDefault();
        _this4._onFormSubmit(event, callback);
      });
      form.addEventListener('input', function (event) {
        _this4._onFormInput(event.target);
      });
      form.addEventListener('reset', function (event) {
        _this4.reset(event.target);
      });
    }
  }, {
    key: "init",
    value: function init() {
      var _this5 = this;
      this._validateParent = document.querySelectorAll('[data-form-validate]');
      if (!this._validateParent.length) {
        return;
      }
      this._validateParent.forEach(function (parent) {
        return _this5._initValidate(parent);
      });
    }
  }]);
  return Form;
}();
exports.Form = Form;

},{"./callback":4,"./init-phone-input":6,"./validator":10}],6:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initPhoneInput = void 0;
var baseCountryCode = '+7';
var baseMatrix = ' (___) ___ __ __';
var phoneLength = baseCountryCode.length + baseMatrix.length;
var onPhoneInputInput = function onPhoneInputInput(e) {
  var matrix = "".concat(baseCountryCode).concat(baseMatrix);
  var def = matrix.replace(/\D/g, '');
  var i = 0;
  var val = e.target.value.replace(/\D/g, '');
  if (def.length >= val.length) {
    val = def;
  }
  e.target.value = matrix.replace(/./g, function (a) {
    if (/[_\d]/.test(a) && i < val.length) {
      return val.charAt(i++);
    } else if (i >= val.length) {
      return '';
    } else {
      return a;
    }
  });
};
var onPhoneInputFocus = function onPhoneInputFocus(_ref) {
  var target = _ref.target;
  if (!target.value) {
    target.value = baseCountryCode;
  }
  target.addEventListener('input', onPhoneInputInput);
  target.addEventListener('blur', onPhoneInputBlur);
  target.addEventListener('keydown', onPhoneInputKeydown);
  target.addEventListener('paste', onPhoneInputPaste);
  target.addEventListener('click', onPhoneInputClick);
};
var onPhoneInputClick = function onPhoneInputClick(e) {
  if (e.target.selectionStart < 4) {
    e.preventDefault();
    e.target.setSelectionRange(3, 3);
  }
};
var onPhoneInputPaste = function onPhoneInputPaste(e) {
  e.target.setSelectionRange(0, 0);
  if (!e.target.selectionStart) {
    setTimeout(function () {
      if (e.target.value.startsWith('+7')) {
        return;
      }
      if (e.target.value.startsWith('+8')) {
        e.target.value = "+7 ".concat(e.target.value.slice(3));
        return;
      }
      e.target.value = '';
    });
  }
};
var onPhoneInputKeydown = function onPhoneInputKeydown(e) {
  if (e.target.selectionStart < 4 && (e.keyCode === 37 || e.keyCode === 13)) {
    e.preventDefault();
    e.target.setSelectionRange(3, 3);
  }
};
var onPhoneInputBlur = function onPhoneInputBlur(_ref2) {
  var target = _ref2.target;
  if (target.value === baseCountryCode) {
    var parent = target.closest('[data-validate-type="phone"]');
    target.value = '';
    if (!parent.hasAttribute('data-required')) {
      parent.classList.remove('is-valid');
      parent.classList.remove('is-invalid');
      var parentMessage = parent.querySelector('.input-message');
      if (parentMessage) {
        parentMessage.remove();
      }
    }
    parent.classList.remove('not-empty');
    target.removeEventListener('input', onPhoneInputInput);
    target.removeEventListener('blur', onPhoneInputBlur);
    target.removeEventListener('keydown', onPhoneInputKeydown);
    target.removeEventListener('paste', onPhoneInputPaste);
    target.removeEventListener('click', onPhoneInputClick);
  }
};
var initPhoneInput = function initPhoneInput(parent) {
  var input = parent.querySelector('input');
  parent.dataset.phoneLength = phoneLength;
  input.addEventListener('focus', onPhoneInputFocus);
};
exports.initPhoneInput = initPhoneInput;

},{}],7:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.matrixReplace = void 0;
var matrixReplace = function matrixReplace(item, matrix, RegEx) {
  if (!matrix) {
    // eslint-disable-next-line no-console
    console.error('При валидации по матрице обязательно указывать формат матрицы: data-matrix=""');
    item.value = '';
    return;
  }
  if (!RegEx) {
    // eslint-disable-next-line no-console
    console.error('При валидации по матрице обязательно указывать формат ограничений: data-matrix-limitations=""');
    item.value = '';
    return;
  }
  var def = matrix.replace(RegEx, '');
  var val = item.value.replace(RegEx, '');
  var i = 0;
  if (def.length >= val.length) {
    val = def;
  }
  item.value = matrix.replace(/./g, function (a) {
    if (/[_\^]/.test(a) && i < val.length) {
      return val.charAt(i++);
    } else if (i >= val.length) {
      return '';
    } else {
      return a;
    }
  });
};
exports.matrixReplace = matrixReplace;

},{}],8:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getMatrixLimitationsRegEx = exports.getMailRegEx = exports.getLimitationsRegEx = void 0;
var setLimitationError = function setLimitationError(limitation) {
  // eslint-disable-next-line no-console
  console.error("\u041F\u0435\u0440\u0435\u0434\u0430\u043D\u043D\u044B\u0439 \u0444\u043E\u0440\u043C\u0430\u0442 \u043E\u0433\u0440\u0430\u043D\u0438\u0447\u0435\u043D\u0438\u044F(data-limitation=\"".concat(limitation, "\") - \u043D\u0435 \u043F\u043E\u0434\u0434\u0435\u0440\u0436\u0438\u0432\u0430\u0435\u0442\u0441\u044F. \u041F\u0440\u043E\u0432\u0435\u0440\u044C\u0442\u0435 \u043A\u043E\u0440\u0440\u0435\u043A\u0442\u043D\u043E\u0441\u0442\u044C \u0432\u0432\u0435\u0434\u0451\u043D\u043D\u044B\u0445 \u0437\u043D\u0430\u0447\u0435\u043D\u0438\u0439."));
};
var getLimitationsRegEx = function getLimitationsRegEx(limitation) {
  switch (limitation) {
    case 'digit':
      return /[^\d]/g;
    case 'name':
      return /[^a-zA-Zа-яёА-ЯЁ\-\s]/g;
    case 'letters':
      return /[^a-zA-Zа-яёА-ЯЁ\s]/g;
    case 'letters-and-digit':
      return /[^a-zA-Zа-яёА-ЯЁ\s\d]/g;
    case 'cyrillic':
      return /[^а-яёА-ЯЁ\s]/g;
    case 'latin':
      return /[^a-zA-Z\s]/g;
    default:
      return setLimitationError(limitation);
  }
};
exports.getLimitationsRegEx = getLimitationsRegEx;
var getMatrixLimitationsRegEx = function getMatrixLimitationsRegEx(matrix) {
  switch (matrix) {
    case 'digit':
      return /[^\d]/g;
    case 'name':
      return /[^\а-яё\А-ЯЁ\a-z\A-Z\-]]/g;
    case 'letters':
      return /[^\а-яё\А-ЯЁ\a-z\A-Z]/g;
    case 'letters-and-digit':
      return /[^\а-яё\А-ЯЁ\a-z\A-Z\d]/g;
    case 'cyrillic':
      return /[^\а-яё\А-ЯЁ]/g;
    case 'latin':
      return /[^\a-z\A-Z]/g;
    default:
      return false;
  }
};
exports.getMatrixLimitationsRegEx = getMatrixLimitationsRegEx;
var getMailRegEx = function getMailRegEx() {
  return /[a-zA-Zа-яёА-ЯЁ0-9]{1}([a-zA-Zа-яёА-ЯЁ0-9\-_\.]{1,})?@[a-zA-Zа-яёА-ЯЁ0-9\-]{1}([a-zA-Zа-яёА-ЯЁ0-9.\-]{1,})?[a-zA-Zа-яёА-ЯЁ0-9\-]{1}\.[a-zA-Zа-яёА-ЯЁ]{2,6}/;
};
exports.getMailRegEx = getMailRegEx;

},{}],9:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Message = void 0;
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var Message = /*#__PURE__*/function () {
  function Message() {
    _classCallCheck(this, Message);
    this._baseErrorText = 'Это поле является обязательным';
  }
  _createClass(Message, [{
    key: "_messageTemplate",
    value: function _messageTemplate(message, state) {
      var cssClass = state === 'valid' ? 'is-valid' : 'is-invalid';
      return "<span class=\"input-message ".concat(cssClass, "\">").concat(message, "</span>");
    }
  }, {
    key: "removeMessage",
    value: function removeMessage(parent) {
      var parentMessage = parent.querySelector('.input-message');
      if (parentMessage) {
        parentMessage.remove();
      }
    }
  }, {
    key: "renderMessage",
    value: function renderMessage(parent, message, state) {
      this.removeMessage(parent);
      parent.insertAdjacentHTML('beforeend', this._messageTemplate(message, state));
    }
  }]);
  return Message;
}();
exports.Message = Message;

},{}],10:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Validator = void 0;
var _regularExpression = require("./regular-expression");
var _matrix = require("./matrix");
var _renderMessage2 = require("./render-message");
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var Validator = /*#__PURE__*/function () {
  function Validator() {
    _classCallCheck(this, Validator);
    this._getLimitationsRegEx = _regularExpression.getLimitationsRegEx;
    this._getMatrixLimitationsRegEx = _regularExpression.getMatrixLimitationsRegEx;
    this._getMailRegEx = _regularExpression.getMailRegEx;
    this._matrixReplace = _matrix.matrixReplace;
    this._message = new _renderMessage2.Message();
  }
  _createClass(Validator, [{
    key: "_renderMessage",
    value: function _renderMessage(trigger, parent, input) {
      if (!parent.hasAttribute('data-required') && !input.value) {
        return;
      }
      if (!trigger) {
        parent.classList.add('is-invalid');
        if (parent.hasAttribute('data-message-base') && !input.value) {
          this._message.renderMessage(parent, parent.dataset.messageBase, 'invalid');
        } else if (parent.hasAttribute('data-message-extra') && input.value) {
          this._message.renderMessage(parent, parent.dataset.messageExtra, 'invalid');
        } else if (!parent.hasAttribute('data-message-extra') && parent.hasAttribute('data-message-base') && input.value) {
          this._message.renderMessage(parent, parent.dataset.messageBase, 'invalid');
        } else {
          this._message.removeMessage(parent);
        }
      } else {
        if (parent.hasAttribute('data-message-success')) {
          this._message.renderMessage(parent, parent.dataset.messageSuccess, 'valid');
        } else {
          this._message.removeMessage(parent);
        }
      }
    }
  }, {
    key: "_setItemValidState",
    value: function _setItemValidState(parent, input) {
      if (!parent.hasAttribute('data-required') && !input.value) {
        return;
      }
      parent.classList.add('is-valid');
      parent.classList.remove('is-invalid');
      input.setAttribute('aria-invalid', 'false');
      this._message.removeMessage(parent);
    }
  }, {
    key: "_setItemInvalidState",
    value: function _setItemInvalidState(parent, input) {
      if (!parent.hasAttribute('data-required') && !input.value) {
        return;
      }
      parent.classList.remove('is-valid');
      input.setAttribute('aria-invalid', 'true');
    }
  }, {
    key: "_simpleLimitation",
    value: function _simpleLimitation(item, limitation) {
      item.value = item.value.replace(this._getLimitationsRegEx(limitation), '');
    }
  }, {
    key: "_matrixLimitation",
    value: function _matrixLimitation(item, matrix, limitation) {
      this._matrixReplace(item, matrix, limitation);
    }
  }, {
    key: "_validateTextInput",
    value: function _validateTextInput(parent, input) {
      var flag = true;
      if (input.value.length >= (+input.getAttribute('minlength') || 1)) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_validateMatrixInput",
    value: function _validateMatrixInput(parent, input) {
      var flag = true;
      if (input.value.length === input.closest('[data-matrix]').dataset.matrix.length) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_validateEmailInput",
    value: function _validateEmailInput(parent, input) {
      var flag = true;
      if (new RegExp(this._getMailRegEx(), '').test(input.value)) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_validatePhoneInput",
    value: function _validatePhoneInput(parent, input) {
      var flag = true;
      if (input.value.length >= +parent.dataset.phoneLength) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_validateCheckbox",
    value: function _validateCheckbox(parent, input) {
      var flag = true;
      if (input.checked) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_findSelectedOption",
    value: function _findSelectedOption(options) {
      var flag = false;
      options.forEach(function (option) {
        if (option.value && option.selected) {
          flag = true;
        }
      });
      return flag;
    }
  }, {
    key: "_validateSelect",
    value: function _validateSelect(parent, input) {
      var options = input.querySelectorAll('option');
      var customSelectText = parent.querySelector('.custom-select__text');
      input.setAttribute('aria-invalid', 'false');
      var flag = true;
      if (this._findSelectedOption(options)) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        parent.classList.remove('not-empty');
        customSelectText.innerHTML = '';
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_returnCheckedElements",
    value: function _returnCheckedElements(inputs) {
      var flag = false;
      inputs.forEach(function (input) {
        if (input.checked) {
          flag = true;
        }
      });
      return flag;
    }
  }, {
    key: "_removeGroupAria",
    value: function _removeGroupAria(inputs) {
      inputs.forEach(function (input) {
        if (!input.checked) {
          input.removeAttribute('aria-required');
          input.removeAttribute('aria-invalid');
        } else {
          input.setAttribute('aria-required', true);
          input.setAttribute('aria-invalid', false);
        }
      });
    }
  }, {
    key: "_setGroupAria",
    value: function _setGroupAria(inputs) {
      inputs.forEach(function (input) {
        input.setAttribute('aria-required', true);
        input.setAttribute('aria-invalid', true);
      });
    }
  }, {
    key: "_validateToggleGroup",
    value: function _validateToggleGroup(parent) {
      var formElements = parent.querySelectorAll('input');
      var flag = true;
      if (this._returnCheckedElements(formElements)) {
        this._removeGroupAria(formElements);
        parent.classList.remove('is-invalid');
        parent.classList.add('is-valid');
        this._message.removeMessage(parent);
      } else {
        this._setGroupAria(formElements);
        parent.classList.remove('is-valid');
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_customExample",
    value: function _customExample(parent, input) {
      var flag = true;
      if (!input.value.length) {
        parent.dataset.messageBase = 'Поле обязательно к заполнению';
        this._setItemInvalidState(parent, input);
        flag = false;
      } else if (input.value.length < input.minLength) {
        parent.dataset.messageBase = "\u041E\u0441\u0442\u0430\u043B\u043E\u0441\u044C \u0432\u0432\u0435\u0441\u0442\u0438 \u0435\u0449\u0451 ".concat(input.minLength - input.value.length, " \u0441\u0438\u043C\u0432\u043E\u043B\u043E\u0432");
        this._setItemInvalidState(parent, input);
        flag = false;
      } else if (input.value.length > input.minLength) {
        parent.dataset.messageBase = "\u0412\u044B \u0432\u0432\u0435\u043B\u0438 ".concat(input.value.length - input.minLength, " \u043B\u0438\u0448\u043D\u0438\u0445 \u0441\u0438\u043C\u0432\u043E\u043B\u043E\u0432");
        this._setItemInvalidState(parent, input);
        flag = false;
      } else {
        parent.dataset.messageSuccess = 'Поле заполнено корректно';
        this._setItemValidState(parent, input);
        flag = true;
      }
      return flag;
    }
  }, {
    key: "_validateFile",
    value: function _validateFile(parent, input) {
      var flag = true;
      var sizeTest = parent.dataset.maxSize && input.files[0] ? input.files[0].size < +parent.dataset.maxSize : true;
      if (input.value && sizeTest) {
        this._setItemValidState(parent, input);
      } else {
        this._setItemInvalidState(parent, input);
        flag = false;
      }
      return flag;
    }
  }, {
    key: "_validateInput",
    value: function _validateInput(type, parent, input) {
      switch (type) {
        case 'text':
          return this._validateTextInput(parent, input);
        case 'matrix':
          return this._validateMatrixInput(parent, input);
        case 'email':
          return this._validateEmailInput(parent, input);
        case 'phone':
          return this._validatePhoneInput(parent, input);
        case 'checkbox':
          return this._validateCheckbox(parent, input);
        case 'select':
          return this._validateSelect(parent, input);
        case 'toggle-group':
          return this._validateToggleGroup(parent, input);
        case 'file':
          return this._validateFile(parent, input);
        case 'custom-example':
          return this._customExample(parent, input);
        default:
          return false;
      }
    }
  }, {
    key: "validateFormElement",
    value: function validateFormElement(formElement) {
      var fullValidate = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      var parent = formElement.closest('[data-validate-type]');
      if (!parent) {
        return;
      }
      if (!parent.hasAttribute('data-required')) {
        var removeElement = parent.querySelector('input') || parent.querySelector('select') || parent.querySelector('textarea');
        if (!removeElement.value) {
          parent.classList.remove('is-valid');
          parent.classList.remove('is-invalid');
        }
      }
      var onInputValidate = parent.hasAttribute('data-on-input-validate');
      if (parent.hasAttribute('data-limitation')) {
        this._simpleLimitation(formElement, parent.dataset.limitation);
      }
      if (parent.dataset.validateType === 'matrix') {
        this._matrixLimitation(formElement, parent.dataset.matrix, this._getMatrixLimitationsRegEx(parent.dataset.matrixLimitation));
      }
      var isValid = this._validateInput(parent.dataset.validateType, parent, formElement);
      if (onInputValidate || fullValidate) {
        this._renderMessage(isValid, parent, formElement);
      }
    }
  }, {
    key: "_fullValidate",
    value: function _fullValidate(items) {
      var _this = this;
      var result = true;
      items.forEach(function (item) {
        var formElement = item.querySelector('input') || item.querySelector('select') || item.querySelector('textarea');
        _this.validateFormElement(formElement, true);
        if (item.classList.contains('is-invalid')) {
          result = false;
        }
      });
      return result;
    }
  }, {
    key: "validateForm",
    value: function validateForm(form) {
      var validateItems = form.querySelectorAll('[data-validate-type]');
      var result = this._fullValidate(validateItems);
      return result;
    }
  }]);
  return Validator;
}();
exports.Validator = Validator;

},{"./matrix":7,"./regular-expression":8,"./render-message":9}],11:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initHeroSwiper = void 0;
var initHeroSwiper = function initHeroSwiper() {
  new Swiper(".hero__swiper", {
    pagination: {
      el: '.hero__swiper-pagination',
      clickable: true
    }
  });
};
exports.initHeroSwiper = initHeroSwiper;

},{}],12:[function(require,module,exports){
(function (global){(function (){
"use strict";

var _excluded = ["statics", "includes"];
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArrayLimit(arr, i) { var _i = null == arr ? null : "undefined" != typeof Symbol && arr[Symbol.iterator] || arr["@@iterator"]; if (null != _i) { var _s, _e, _x, _r, _arr = [], _n = !0, _d = !1; try { if (_x = (_i = _i.call(arr)).next, 0 === i) { if (Object(_i) !== _i) return; _n = !1; } else for (; !(_n = (_s = _x.call(_i)).done) && (_arr.push(_s.value), _arr.length !== i); _n = !0); } catch (err) { _d = !0, _e = err; } finally { try { if (!_n && null != _i.return && (_r = _i.return(), Object(_r) !== _r)) return; } finally { if (_d) throw _e; } } return _arr; } }
function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }
function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); Object.defineProperty(subClass, "prototype", { writable: false }); if (superClass) _setPrototypeOf(subClass, superClass); }
function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }
function _createSuper(Derived) { var hasNativeReflectConstruct = _isNativeReflectConstruct(); return function _createSuperInternal() { var Super = _getPrototypeOf(Derived), result; if (hasNativeReflectConstruct) { var NewTarget = _getPrototypeOf(this).constructor; result = Reflect.construct(Super, arguments, NewTarget); } else { result = Super.apply(this, arguments); } return _possibleConstructorReturn(this, result); }; }
function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } else if (call !== void 0) { throw new TypeError("Derived constructors may only return object or undefined"); } return _assertThisInitialized(self); }
function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }
function _isNativeReflectConstruct() { if (typeof Reflect === "undefined" || !Reflect.construct) return false; if (Reflect.construct.sham) return false; if (typeof Proxy === "function") return true; try { Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); return true; } catch (e) { return false; } }
function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }
function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }
function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }
function _createForOfIteratorHelper(o, allowArrayLike) { var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"]; if (!it) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e37) { throw _e37; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = it.call(o); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e38) { didErr = true; err = _e38; }, f: function f() { try { if (!normalCompletion && it.return != null) it.return(); } finally { if (didErr) throw err; } } }; }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
/* @preserve
 * Leaflet 1.9.2+main.41f1f5b, a JS library for interactive maps. https://leafletjs.com
 * (c) 2010-2023 Volodymyr Agafonkin, (c) 2010-2011 CloudMade
 */
!function (t, e) {
  "object" == (typeof exports === "undefined" ? "undefined" : _typeof(exports)) && "undefined" != typeof module ? e(exports) : "function" == typeof define && define.amd ? define(["exports"], e) : e((t = "undefined" != typeof globalThis ? globalThis : t || self).leaflet = {});
}(void 0, function (t) {
  "use strict";

  var I = "1.9.2+main.41f1f5ba";
  function h(t) {
    var i, o, s;
    for (var _len = arguments.length, e = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      e[_key - 1] = arguments[_key];
    }
    for (i = 0, o = e.length; i < o; i++) for (var _n in s = e[i]) t[_n] = s[_n];
    return t;
  }
  var R = 0;
  function r(t) {
    return "_leaflet_id" in t || (t._leaflet_id = ++R), t._leaflet_id;
  }
  function j(e, i, o) {
    var s, n;
    function a() {
      s = !1, n && (t.apply(o, n), n = !1);
    }
    function t() {
      for (var _len2 = arguments.length, t = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        t[_key2] = arguments[_key2];
      }
      s ? n = t : (e.apply(o, t), setTimeout(a, i), s = !0);
    }
    return t;
  }
  function D(t, e, i) {
    var o = e[1],
      e = e[0],
      s = o - e;
    return t === o && i ? t : ((t - e) % s + s) % s + e;
  }
  function a() {
    return !1;
  }
  function i(t, e) {
    return !1 === e ? t : (e = Math.pow(10, void 0 === e ? 6 : e), Math.round(t * e) / e);
  }
  function l(t) {
    return t.trim().split(/\s+/);
  }
  function n(t, e) {
    Object.hasOwn(t, "options") || (t.options = t.options ? Object.create(t.options) : {});
    for (var _i2 in e) Object.hasOwn(e, _i2) && (t.options[_i2] = e[_i2]);
    return t.options;
  }
  function N(t, e, i) {
    var o = [];
    for (var _s in t) Object.hasOwn(t, _s) && o.push(encodeURIComponent(i ? _s.toUpperCase() : _s) + "=" + encodeURIComponent(t[_s]));
    return (e && e.includes("?") ? "&" : "?") + o.join("&");
  }
  var W = /\{ *([\w_ -]+) *\}/g;
  function F(t, o) {
    return t.replace(W, function (t, e) {
      var i = o[e];
      if (void 0 === i) throw new Error("No value provided for variable " + t);
      return i = "function" == typeof i ? i(o) : i;
    });
  }
  var H = "data:image/gif;base64,R0lGODlhAQABAAD/ACwAAAAAAQABAAACADs=";
  function U(t) {
    return window["webkit" + t] || window["moz" + t] || window["ms" + t];
  }
  var V = 0;
  function G(t) {
    var e = +new Date(),
      i = Math.max(0, 16 - (e - V));
    return V = e + i, window.setTimeout(t, i);
  }
  var q = window.requestAnimationFrame || U("RequestAnimationFrame") || G,
    K = window.cancelAnimationFrame || U("CancelAnimationFrame") || U("CancelRequestAnimationFrame") || function (t) {
      window.clearTimeout(t);
    };
  function x(t, e, i) {
    if (!i || q !== G) return q.call(window, t.bind(e));
    t.call(e);
  }
  function d(t) {
    t && K.call(window, t);
  }
  var $ = {
    __proto__: null,
    cancelAnimFrame: d,
    cancelFn: K,
    emptyImageUrl: H,
    extend: h,
    falseFn: a,
    formatNum: i,
    getParamString: N,
    get lastId() {
      return R;
    },
    requestAnimFrame: x,
    requestFn: q,
    setOptions: n,
    splitWords: l,
    stamp: r,
    template: F,
    throttle: j,
    wrapNum: D
  };
  var Y = /*#__PURE__*/function () {
    function Y() {
      _classCallCheck(this, Y);
      _defineProperty(this, "_initHooksCalled", !1);
      n(this), this.initialize && this.initialize.apply(this, arguments), this.callInitHooks();
    }
    _createClass(Y, [{
      key: "callInitHooks",
      value: function callInitHooks() {
        if (!this._initHooksCalled) {
          var e = [];
          var _t2 = this;
          for (; null !== (_t2 = Object.getPrototypeOf(_t2));) e.push(_t2);
          e.reverse();
          for (var _i3 = 0, _e2 = e; _i3 < _e2.length; _i3++) {
            var _i4$_initHooks;
            var _i4 = _e2[_i3];
            var _iterator = _createForOfIteratorHelper((_i4$_initHooks = _i4._initHooks) !== null && _i4$_initHooks !== void 0 ? _i4$_initHooks : []),
              _step;
            try {
              for (_iterator.s(); !(_step = _iterator.n()).done;) {
                var _o = _step.value;
                _o.call(this);
              }
            } catch (err) {
              _iterator.e(err);
            } finally {
              _iterator.f();
            }
          }
          this._initHooksCalled = !0;
        }
      }
    }], [{
      key: "extend",
      value: function extend(_ref) {
        var t = _ref.statics,
          e = _ref.includes,
          i = _objectWithoutProperties(_ref, _excluded);
        var o = /*#__PURE__*/function (_this) {
            _inherits(o, _this);
            var _super = _createSuper(o);
            function o() {
              _classCallCheck(this, o);
              return _super.apply(this, arguments);
            }
            return _createClass(o);
          }(this),
          s = (Object.setPrototypeOf(o, this), this.prototype),
          n = o.prototype;
        return t && h(o, t), e && h.apply(null, [n].concat(e)), h(n, i), n.options && (n.options = s.options ? Object.create(s.options) : {}, h(n.options, i.options)), n._initHooks = [], o;
      }
    }, {
      key: "include",
      value: function include(t) {
        var e = this.prototype.options;
        return h(this.prototype, t), t.options && (this.prototype.options = e, this.mergeOptions(t.options)), this;
      }
    }, {
      key: "mergeOptions",
      value: function mergeOptions(t) {
        return h(this.prototype.options, t), this;
      }
    }, {
      key: "addInitHook",
      value: function addInitHook(t) {
        for (var _len3 = arguments.length, e = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
          e[_key3 - 1] = arguments[_key3];
        }
        var i = "function" == typeof t ? t : function () {
          this[t].apply(this, e);
        };
        return this.prototype._initHooks = this.prototype._initHooks || [], this.prototype._initHooks.push(i), this;
      }
    }]);
    return Y;
  }();
  var e = {
      on: function on(i, o, s) {
        if ("object" == _typeof(i)) for (var _t3 in i) Object.hasOwn(i, _t3) && this._on(_t3, i[_t3], o);else for (var _t4 = 0, _e3 = (i = l(i)).length; _t4 < _e3; _t4++) this._on(i[_t4], o, s);
        return this;
      },
      off: function off(i, o, s) {
        if (arguments.length) {
          if ("object" == _typeof(i)) for (var _t5 in i) Object.hasOwn(i, _t5) && this._off(_t5, i[_t5], o);else {
            i = l(i);
            var n = 1 === arguments.length;
            for (var _t6 = 0, _e4 = i.length; _t6 < _e4; _t6++) n ? this._off(i[_t6]) : this._off(i[_t6], o, s);
          }
        } else delete this._events;
        return this;
      },
      _on: function _on(t, e, i, o) {
        "function" != typeof e ? console.warn("wrong listener type: " + _typeof(e)) : !1 === this._listens(t, e, i) && (e = {
          fn: e,
          ctx: i = i === this ? void 0 : i
        }, o && (e.once = !0), this._events = this._events || {}, this._events[t] = this._events[t] || [], this._events[t].push(e));
      },
      _off: function _off(t, e, i) {
        var o, s, n;
        if (this._events && (o = this._events[t])) if (1 === arguments.length) {
          if (this._firingCount) for (s = 0, n = o.length; s < n; s++) o[s].fn = a;
          delete this._events[t];
        } else "function" != typeof e ? console.warn("wrong listener type: " + _typeof(e)) : !1 !== (e = this._listens(t, e, i)) && (i = o[e], this._firingCount && (i.fn = a, this._events[t] = o = o.slice()), o.splice(e, 1));
      },
      fire: function fire(i, t, e) {
        if (this.listens(i, e)) {
          var o = h({}, t, {
            type: i,
            target: this,
            sourceTarget: t && t.sourceTarget || this
          });
          if (this._events) {
            var s = this._events[i];
            if (s) {
              this._firingCount = this._firingCount + 1 || 1;
              for (var _t7 = 0, _e5 = s.length; _t7 < _e5; _t7++) {
                var n = s[_t7],
                  a = n.fn;
                n.once && this.off(i, a, n.ctx), a.call(n.ctx || this, o);
              }
              this._firingCount--;
            }
          }
          e && this._propagateEvent(o);
        }
        return this;
      },
      listens: function listens(t, e, i, o) {
        "string" != typeof t && console.warn('"string" type argument expected');
        var s = e;
        "function" != typeof e && (o = !!e, i = s = void 0);
        var n = this._events && this._events[t];
        if (n && n.length && !1 !== this._listens(t, s, i)) return !0;
        if (o) for (var _a in this._eventParents) if (this._eventParents[_a].listens(t, e, i, o)) return !0;
        return !1;
      },
      _listens: function _listens(t, i, o) {
        if (this._events) {
          var s = this._events[t] || [];
          if (!i) return !!s.length;
          o === this && (o = void 0);
          for (var _t8 = 0, _e6 = s.length; _t8 < _e6; _t8++) if (s[_t8].fn === i && s[_t8].ctx === o) return _t8;
        }
        return !1;
      },
      once: function once(i, o, s) {
        if ("object" == _typeof(i)) for (var _t9 in i) Object.hasOwn(i, _t9) && this._on(_t9, i[_t9], o, !0);else for (var _t10 = 0, _e7 = (i = l(i)).length; _t10 < _e7; _t10++) this._on(i[_t10], o, s, !0);
        return this;
      },
      addEventParent: function addEventParent(t) {
        return this._eventParents = this._eventParents || {}, this._eventParents[r(t)] = t, this;
      },
      removeEventParent: function removeEventParent(t) {
        return this._eventParents && delete this._eventParents[r(t)], this;
      },
      _propagateEvent: function _propagateEvent(t) {
        for (var _e8 in this._eventParents) Object.hasOwn(this._eventParents, _e8) && this._eventParents[_e8].fire(t.type, h({
          layer: t.target,
          propagatedFrom: t.target
        }, t), !0);
      }
    },
    e = (e.addEventListener = e.on, e.removeEventListener = e.clearAllEventListeners = e.off, e.addOneTimeEventListener = e.once, e.fireEvent = e.fire, e.hasEventListeners = e.listens, Y.extend(e));
  function c(t, e, i) {
    this.x = i ? Math.round(t) : t, this.y = i ? Math.round(e) : e;
  }
  var X = Math.trunc || function (t) {
    return 0 < t ? Math.floor(t) : Math.ceil(t);
  };
  function g(t, e, i) {
    return t instanceof c ? t : Array.isArray(t) ? new c(t[0], t[1]) : null == t ? t : "object" == _typeof(t) && "x" in t && "y" in t ? new c(t.x, t.y) : new c(t, e, i);
  }
  function m(t, e) {
    if (t) {
      var i = e ? [t, e] : t;
      for (var _t11 = 0, _e9 = i.length; _t11 < _e9; _t11++) this.extend(i[_t11]);
    }
  }
  function _(t, e) {
    return !t || t instanceof m ? t : new m(t, e);
  }
  function u(t, e) {
    if (t) {
      var i = e ? [t, e] : t;
      for (var _t12 = 0, _e10 = i.length; _t12 < _e10; _t12++) this.extend(i[_t12]);
    }
  }
  function f(t, e) {
    return t instanceof u ? t : new u(t, e);
  }
  function p(t, e, i) {
    if (isNaN(t) || isNaN(e)) throw new Error("Invalid LatLng object: (".concat(t, ", ").concat(e, ")"));
    this.lat = +t, this.lng = +e, void 0 !== i && (this.alt = +i);
  }
  function w(t, e, i) {
    return t instanceof p ? t : Array.isArray(t) && "object" != _typeof(t[0]) ? 3 === t.length ? new p(t[0], t[1], t[2]) : 2 === t.length ? new p(t[0], t[1]) : null : null == t ? t : "object" == _typeof(t) && "lat" in t ? new p(t.lat, "lng" in t ? t.lng : t.lon, t.alt) : void 0 === e ? null : new p(t, e, i);
  }
  c.prototype = {
    clone: function clone() {
      return new c(this.x, this.y);
    },
    add: function add(t) {
      return this.clone()._add(g(t));
    },
    _add: function _add(t) {
      return this.x += t.x, this.y += t.y, this;
    },
    subtract: function subtract(t) {
      return this.clone()._subtract(g(t));
    },
    _subtract: function _subtract(t) {
      return this.x -= t.x, this.y -= t.y, this;
    },
    divideBy: function divideBy(t) {
      return this.clone()._divideBy(t);
    },
    _divideBy: function _divideBy(t) {
      return this.x /= t, this.y /= t, this;
    },
    multiplyBy: function multiplyBy(t) {
      return this.clone()._multiplyBy(t);
    },
    _multiplyBy: function _multiplyBy(t) {
      return this.x *= t, this.y *= t, this;
    },
    scaleBy: function scaleBy(t) {
      return new c(this.x * t.x, this.y * t.y);
    },
    unscaleBy: function unscaleBy(t) {
      return new c(this.x / t.x, this.y / t.y);
    },
    round: function round() {
      return this.clone()._round();
    },
    _round: function _round() {
      return this.x = Math.round(this.x), this.y = Math.round(this.y), this;
    },
    floor: function floor() {
      return this.clone()._floor();
    },
    _floor: function _floor() {
      return this.x = Math.floor(this.x), this.y = Math.floor(this.y), this;
    },
    ceil: function ceil() {
      return this.clone()._ceil();
    },
    _ceil: function _ceil() {
      return this.x = Math.ceil(this.x), this.y = Math.ceil(this.y), this;
    },
    trunc: function trunc() {
      return this.clone()._trunc();
    },
    _trunc: function _trunc() {
      return this.x = X(this.x), this.y = X(this.y), this;
    },
    distanceTo: function distanceTo(t) {
      var e = (t = g(t)).x - this.x,
        t = t.y - this.y;
      return Math.sqrt(e * e + t * t);
    },
    equals: function equals(t) {
      return (t = g(t)).x === this.x && t.y === this.y;
    },
    contains: function contains(t) {
      return t = g(t), Math.abs(t.x) <= Math.abs(this.x) && Math.abs(t.y) <= Math.abs(this.y);
    },
    toString: function toString() {
      return "Point(".concat(i(this.x), ", ").concat(i(this.y), ")");
    }
  }, m.prototype = {
    extend: function extend(t) {
      var e, i;
      if (t) {
        if (t instanceof c || "number" == typeof t[0] || "x" in t) e = i = g(t);else if (t = _(t), e = t.min, i = t.max, !e || !i) return this;
        this.min || this.max ? (this.min.x = Math.min(e.x, this.min.x), this.max.x = Math.max(i.x, this.max.x), this.min.y = Math.min(e.y, this.min.y), this.max.y = Math.max(i.y, this.max.y)) : (this.min = e.clone(), this.max = i.clone());
      }
      return this;
    },
    getCenter: function getCenter(t) {
      return g((this.min.x + this.max.x) / 2, (this.min.y + this.max.y) / 2, t);
    },
    getBottomLeft: function getBottomLeft() {
      return g(this.min.x, this.max.y);
    },
    getTopRight: function getTopRight() {
      return g(this.max.x, this.min.y);
    },
    getTopLeft: function getTopLeft() {
      return this.min;
    },
    getBottomRight: function getBottomRight() {
      return this.max;
    },
    getSize: function getSize() {
      return this.max.subtract(this.min);
    },
    contains: function contains(t) {
      var e, i;
      return (t = ("number" == typeof t[0] || t instanceof c ? g : _)(t)) instanceof m ? (e = t.min, i = t.max) : e = i = t, e.x >= this.min.x && i.x <= this.max.x && e.y >= this.min.y && i.y <= this.max.y;
    },
    intersects: function intersects(t) {
      t = _(t);
      var e = this.min,
        i = this.max,
        o = t.min,
        t = t.max,
        s = t.x >= e.x && o.x <= i.x,
        t = t.y >= e.y && o.y <= i.y;
      return s && t;
    },
    overlaps: function overlaps(t) {
      t = _(t);
      var e = this.min,
        i = this.max,
        o = t.min,
        t = t.max,
        s = t.x > e.x && o.x < i.x,
        t = t.y > e.y && o.y < i.y;
      return s && t;
    },
    isValid: function isValid() {
      return !(!this.min || !this.max);
    },
    pad: function pad(t) {
      var e = this.min,
        i = this.max,
        o = Math.abs(e.x - i.x) * t,
        t = Math.abs(e.y - i.y) * t;
      return _(g(e.x - o, e.y - t), g(i.x + o, i.y + t));
    },
    equals: function equals(t) {
      return !!t && (t = _(t), this.min.equals(t.getTopLeft())) && this.max.equals(t.getBottomRight());
    }
  }, u.prototype = {
    extend: function extend(t) {
      var e = this._southWest,
        i = this._northEast;
      var o, s;
      if (t instanceof p) o = t, s = t;else {
        if (!(t instanceof u)) return t ? this.extend(w(t) || f(t)) : this;
        if (o = t._southWest, s = t._northEast, !o || !s) return this;
      }
      return e || i ? (e.lat = Math.min(o.lat, e.lat), e.lng = Math.min(o.lng, e.lng), i.lat = Math.max(s.lat, i.lat), i.lng = Math.max(s.lng, i.lng)) : (this._southWest = new p(o.lat, o.lng), this._northEast = new p(s.lat, s.lng)), this;
    },
    pad: function pad(t) {
      var e = this._southWest,
        i = this._northEast,
        o = Math.abs(e.lat - i.lat) * t,
        t = Math.abs(e.lng - i.lng) * t;
      return new u(new p(e.lat - o, e.lng - t), new p(i.lat + o, i.lng + t));
    },
    getCenter: function getCenter() {
      return new p((this._southWest.lat + this._northEast.lat) / 2, (this._southWest.lng + this._northEast.lng) / 2);
    },
    getSouthWest: function getSouthWest() {
      return this._southWest;
    },
    getNorthEast: function getNorthEast() {
      return this._northEast;
    },
    getNorthWest: function getNorthWest() {
      return new p(this.getNorth(), this.getWest());
    },
    getSouthEast: function getSouthEast() {
      return new p(this.getSouth(), this.getEast());
    },
    getWest: function getWest() {
      return this._southWest.lng;
    },
    getSouth: function getSouth() {
      return this._southWest.lat;
    },
    getEast: function getEast() {
      return this._northEast.lng;
    },
    getNorth: function getNorth() {
      return this._northEast.lat;
    },
    contains: function contains(t) {
      t = ("number" == typeof t[0] || t instanceof p || "lat" in t ? w : f)(t);
      var e = this._southWest,
        i = this._northEast;
      var o, s;
      return t instanceof u ? (o = t.getSouthWest(), s = t.getNorthEast()) : o = s = t, o.lat >= e.lat && s.lat <= i.lat && o.lng >= e.lng && s.lng <= i.lng;
    },
    intersects: function intersects(t) {
      t = f(t);
      var e = this._southWest,
        i = this._northEast,
        o = t.getSouthWest(),
        t = t.getNorthEast(),
        s = t.lat >= e.lat && o.lat <= i.lat,
        t = t.lng >= e.lng && o.lng <= i.lng;
      return s && t;
    },
    overlaps: function overlaps(t) {
      t = f(t);
      var e = this._southWest,
        i = this._northEast,
        o = t.getSouthWest(),
        t = t.getNorthEast(),
        s = t.lat > e.lat && o.lat < i.lat,
        t = t.lng > e.lng && o.lng < i.lng;
      return s && t;
    },
    toBBoxString: function toBBoxString() {
      return [this.getWest(), this.getSouth(), this.getEast(), this.getNorth()].join(",");
    },
    equals: function equals(t, e) {
      return !!t && (t = f(t), this._southWest.equals(t.getSouthWest(), e)) && this._northEast.equals(t.getNorthEast(), e);
    },
    isValid: function isValid() {
      return !(!this._southWest || !this._northEast);
    }
  };
  var o = {
    latLngToPoint: function latLngToPoint(t, e) {
      t = this.projection.project(t), e = this.scale(e);
      return this.transformation._transform(t, e);
    },
    pointToLatLng: function pointToLatLng(t, e) {
      e = this.scale(e), t = this.transformation.untransform(t, e);
      return this.projection.unproject(t);
    },
    project: function project(t) {
      return this.projection.project(t);
    },
    unproject: function unproject(t) {
      return this.projection.unproject(t);
    },
    scale: function scale(t) {
      return 256 * Math.pow(2, t);
    },
    zoom: function zoom(t) {
      return Math.log(t / 256) / Math.LN2;
    },
    getProjectedBounds: function getProjectedBounds(t) {
      var e;
      return this.infinite ? null : (e = this.projection.bounds, t = this.scale(t), new m(this.transformation.transform(e.min, t), this.transformation.transform(e.max, t)));
    },
    infinite: !(p.prototype = {
      equals: function equals(t, e) {
        return !!t && (t = w(t), Math.max(Math.abs(this.lat - t.lat), Math.abs(this.lng - t.lng)) <= (void 0 === e ? 1e-9 : e));
      },
      toString: function toString(t) {
        return "LatLng(".concat(i(this.lat, t), ", ").concat(i(this.lng, t), ")");
      },
      distanceTo: function distanceTo(t) {
        return J.distance(this, w(t));
      },
      wrap: function wrap() {
        return J.wrapLatLng(this);
      },
      toBounds: function toBounds(t) {
        var t = 180 * t / 40075017,
          e = t / Math.cos(Math.PI / 180 * this.lat);
        return f([this.lat - t, this.lng - e], [this.lat + t, this.lng + e]);
      },
      clone: function clone() {
        return new p(this.lat, this.lng, this.alt);
      }
    }),
    wrapLatLng: function wrapLatLng(t) {
      var e = this.wrapLng ? D(t.lng, this.wrapLng, !0) : t.lng;
      return new p(this.wrapLat ? D(t.lat, this.wrapLat, !0) : t.lat, e, t.alt);
    },
    wrapLatLngBounds: function wrapLatLngBounds(t) {
      var e = t.getCenter(),
        i = this.wrapLatLng(e),
        o = e.lat - i.lat,
        e = e.lng - i.lng;
      return 0 == o && 0 == e ? t : (i = t.getSouthWest(), t = t.getNorthEast(), new u(new p(i.lat - o, i.lng - e), new p(t.lat - o, t.lng - e)));
    }
  };
  var J = h({}, o, {
      wrapLng: [-180, 180],
      R: 6371e3,
      distance: function distance(t, e) {
        var i = Math.PI / 180,
          o = t.lat * i,
          s = e.lat * i,
          n = Math.sin((e.lat - t.lat) * i / 2),
          e = Math.sin((e.lng - t.lng) * i / 2),
          t = n * n + Math.cos(o) * Math.cos(s) * e * e,
          i = 2 * Math.atan2(Math.sqrt(t), Math.sqrt(1 - t));
        return this.R * i;
      }
    }),
    Q = 6378137,
    tt = {
      R: Q,
      MAX_LATITUDE: 85.0511287798,
      project: function project(t) {
        var e = Math.PI / 180,
          i = this.MAX_LATITUDE,
          i = Math.max(Math.min(i, t.lat), -i),
          i = Math.sin(i * e);
        return new c(this.R * t.lng * e, this.R * Math.log((1 + i) / (1 - i)) / 2);
      },
      unproject: function unproject(t) {
        var e = 180 / Math.PI;
        return new p((2 * Math.atan(Math.exp(t.y / this.R)) - Math.PI / 2) * e, t.x * e / this.R);
      },
      bounds: new m([-(s = Q * Math.PI), -s], [s, s])
    };
  function et(t, e, i, o) {
    Array.isArray(t) ? (this._a = t[0], this._b = t[1], this._c = t[2], this._d = t[3]) : (this._a = t, this._b = e, this._c = i, this._d = o);
  }
  function it(t, e, i, o) {
    return new et(t, e, i, o);
  }
  et.prototype = {
    transform: function transform(t, e) {
      return this._transform(t.clone(), e);
    },
    _transform: function _transform(t, e) {
      return t.x = (e = e || 1) * (this._a * t.x + this._b), t.y = e * (this._c * t.y + this._d), t;
    },
    untransform: function untransform(t, e) {
      return new c((t.x / (e = e || 1) - this._b) / this._a, (t.y / e - this._d) / this._c);
    }
  };
  var s = h({}, J, {
      code: "EPSG:3857",
      projection: tt,
      transformation: it(s = .5 / (Math.PI * tt.R), .5, -s, .5)
    }),
    ot = h({}, s, {
      code: "EPSG:900913"
    }),
    st = ht("chrome"),
    nt = !st && ht("safari"),
    y = "undefined" != typeof orientation || ht("mobile"),
    at = !!window.PointerEvent,
    rt = "ontouchstart" in window || !!window.TouchEvent;
  function ht(t) {
    return navigator.userAgent.toLowerCase().includes(t);
  }
  var v = {
    chrome: st,
    safari: nt,
    mobile: y,
    pointer: at,
    touch: rt || at,
    touchNative: rt,
    retina: 1 < (window.devicePixelRatio || window.screen.deviceXDPI / window.screen.logicalXDPI),
    mac: navigator.platform.startsWith("Mac"),
    linux: navigator.platform.startsWith("Linux")
  };
  var lt = {
      touchstart: "pointerdown",
      touchmove: "pointermove",
      touchend: "pointerup",
      touchcancel: "pointercancel"
    },
    dt = {
      touchstart: function touchstart(t, e) {
        ft(t, e);
      },
      touchmove: ft,
      touchend: ft,
      touchcancel: ft
    },
    _t = {};
  var ut = !1;
  function pt(t, e, i) {
    return "touchstart" !== e || ut || (document.addEventListener("pointerdown", ct, !0), document.addEventListener("pointermove", mt, !0), document.addEventListener("pointerup", gt, !0), document.addEventListener("pointercancel", gt, !0), ut = !0), dt[e] ? (i = dt[e].bind(this, i), t.addEventListener(lt[e], i, !1), i) : (console.warn("wrong event specified:", e), a);
  }
  function ct(t) {
    _t[t.pointerId] = t;
  }
  function mt(t) {
    _t[t.pointerId] && (_t[t.pointerId] = t);
  }
  function gt(t) {
    delete _t[t.pointerId];
  }
  function ft(t, e) {
    if ("mouse" !== e.pointerType) {
      e.touches = [];
      for (var _i5 in _t) Object.hasOwn(_t, _i5) && e.touches.push(_t[_i5]);
      e.changedTouches = [e], t(e);
    }
  }
  var yt = 200;
  function vt(t, e) {
    t.addEventListener("dblclick", e);
    var i = 0,
      o;
    function s(t) {
      var e;
      1 !== t.detail ? o = t.detail : "mouse" === t.pointerType || t.sourceCapabilities && !t.sourceCapabilities.firesTouchEvents || (e = qt(t)).some(function (t) {
        return t instanceof HTMLLabelElement && t.attributes.for;
      }) && !e.some(function (t) {
        return t instanceof HTMLInputElement || t instanceof HTMLSelectElement;
      }) || ((e = Date.now()) - i <= yt ? 2 === ++o && t.target.dispatchEvent(function (t) {
        var e = {
            bubbles: t.bubbles,
            cancelable: t.cancelable,
            composed: t.composed,
            detail: 2,
            view: t.view,
            screenX: t.screenX,
            screenY: t.screenY,
            clientX: t.clientX,
            clientY: t.clientY,
            ctrlKey: t.ctrlKey,
            shiftKey: t.shiftKey,
            altKey: t.altKey,
            metaKey: t.metaKey,
            button: t.button,
            buttons: t.buttons,
            relatedTarget: t.relatedTarget,
            region: t.region
          },
          i;
        return i = new (t instanceof PointerEvent ? (e = _objectSpread(_objectSpread({}, e), {}, {
          pointerId: t.pointerId,
          width: t.width,
          height: t.height,
          pressure: t.pressure,
          tangentialPressure: t.tangentialPressure,
          tiltX: t.tiltX,
          tiltY: t.tiltY,
          twist: t.twist,
          pointerType: t.pointerType,
          isPrimary: t.isPrimary
        }), PointerEvent) : MouseEvent)("dblclick", e);
      }(t)) : o = 1, i = e);
    }
    return t.addEventListener("click", s), {
      dblclick: e,
      simDblclick: s
    };
  }
  function xt(t) {
    return "string" == typeof t ? document.getElementById(t) : t;
  }
  function b(t, e, i) {
    t = document.createElement(t);
    return t.className = e || "", i && i.appendChild(t), t;
  }
  function wt(t) {
    var e = t.parentNode;
    e && e.lastChild !== t && e.appendChild(t);
  }
  function bt(t) {
    var e = t.parentNode;
    e && e.firstChild !== t && e.insertBefore(t, e.firstChild);
  }
  function Lt(t, e, i) {
    e = e || new c(0, 0);
    t.style.transform = "translate3d(".concat(e.x, "px,").concat(e.y, "px,0)") + (i ? " scale(".concat(i, ")") : "");
  }
  var Pt = new WeakMap();
  function L(t, e) {
    Pt.set(t, e), Lt(t, e);
  }
  function Tt(t) {
    var _Pt$get;
    return (_Pt$get = Pt.get(t)) !== null && _Pt$get !== void 0 ? _Pt$get : new c(0, 0);
  }
  var zt = document.documentElement.style,
    Mt = ["userSelect", "WebkitUserSelect"].find(function (t) {
      return t in zt;
    });
  var Ct;
  function Zt() {
    var t = zt[Mt];
    "none" !== t && (Ct = t, zt[Mt] = "none");
  }
  function Ot() {
    void 0 !== Ct && (zt[Mt] = Ct, Ct = void 0);
  }
  function St() {
    P(window, "dragstart", M);
  }
  function Et() {
    z(window, "dragstart", M);
  }
  var kt, Bt;
  function At(t) {
    for (; -1 === t.tabIndex;) t = t.parentNode;
    t.style && (It(), kt = t, Bt = t.style.outlineStyle, t.style.outlineStyle = "none", P(window, "keydown", It));
  }
  function It() {
    kt && (kt.style.outlineStyle = Bt, kt = void 0, Bt = void 0, z(window, "keydown", It));
  }
  function Rt(t) {
    for (; !((t = t.parentNode).offsetWidth && t.offsetHeight || t === document.body););
    return t;
  }
  function jt(t) {
    var e = t.getBoundingClientRect();
    return {
      x: e.width / t.offsetWidth || 1,
      y: e.height / t.offsetHeight || 1,
      boundingClientRect: e
    };
  }
  st = {
    __proto__: null,
    create: b,
    disableImageDrag: St,
    disableTextSelection: Zt,
    enableImageDrag: Et,
    enableTextSelection: Ot,
    get: xt,
    getPosition: Tt,
    getScale: jt,
    getSizedParentNode: Rt,
    preventOutline: At,
    restoreOutline: It,
    setPosition: L,
    setTransform: Lt,
    toBack: bt,
    toFront: wt
  };
  function P(i, o, s, n) {
    if (o && "object" == _typeof(o)) for (var _i6 = 0, _Object$entries = Object.entries(o); _i6 < _Object$entries.length; _i6++) {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i6], 2),
        t = _Object$entries$_i[0],
        e = _Object$entries$_i[1];
      Wt(i, t, e, s);
    } else for (var _t13 = 0, _e11 = (o = l(o)).length; _t13 < _e11; _t13++) Wt(i, o[_t13], s, n);
    return this;
  }
  var T = "_leaflet_events";
  function z(i, o, s, n) {
    if (1 === arguments.length) Dt(i), delete i[T];else if (o && "object" == _typeof(o)) for (var _i7 = 0, _Object$entries2 = Object.entries(o); _i7 < _Object$entries2.length; _i7++) {
      var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i7], 2),
        t = _Object$entries2$_i[0],
        e = _Object$entries2$_i[1];
      Ft(i, t, e, s);
    } else if (o = l(o), 2 === arguments.length) Dt(i, function (t) {
      return o.includes(t);
    });else for (var _t14 = 0, _e12 = o.length; _t14 < _e12; _t14++) Ft(i, o[_t14], s, n);
    return this;
  }
  function Dt(t, e) {
    for (var _o2 in t[T]) {
      var i;
      Object.hasOwn(t[T], _o2) && (i = _o2.split(/\d/)[0], e && !e(i) || Ft(t, i, null, null, _o2));
    }
  }
  var Nt = {
    mouseenter: "mouseover",
    mouseleave: "mouseout",
    wheel: !("onwheel" in window) && "mousewheel"
  };
  function Wt(e, i, o, s) {
    var n = i + r(o) + (s ? "_" + r(s) : "");
    if (!e[T] || !e[T][n]) {
      var _t15 = function _t15(t) {
        return o.call(s || e, t || window.event);
      };
      var _a2 = _t15;
      !v.touchNative && v.pointer && i.startsWith("touch") ? _t15 = pt(e, i, _t15) : v.touch && "dblclick" === i ? _t15 = vt(e, _t15) : "addEventListener" in e ? "touchstart" === i || "touchmove" === i || "wheel" === i || "mousewheel" === i ? e.addEventListener(Nt[i] || i, _t15, {
        passive: !1
      }) : "mouseenter" === i || "mouseleave" === i ? (_t15 = function _t15(t) {
        t = t || window.event, Xt(e, t) && _a2(t);
      }, e.addEventListener(Nt[i], _t15, !1)) : e.addEventListener(i, _a2, !1) : e.attachEvent("on" + i, _t15), e[T] = e[T] || {}, e[T][n] = _t15;
    }
  }
  function Ft(t, e, i, o, s) {
    s = s || e + r(i) + (o ? "_" + r(o) : "");
    var n,
      a,
      i = t[T] && t[T][s];
    i && (!v.touchNative && v.pointer && e.startsWith("touch") ? (o = t, n = e, a = i, lt[n] ? o.removeEventListener(lt[n], a, !1) : console.warn("wrong event specified:", n)) : v.touch && "dblclick" === e ? (o = i, (a = t).removeEventListener("dblclick", o.dblclick), a.removeEventListener("click", o.simDblclick)) : "removeEventListener" in t ? t.removeEventListener(Nt[e] || e, i, !1) : t.detachEvent("on" + e, i), t[T][s] = null);
  }
  function Ht(t) {
    return t.stopPropagation ? t.stopPropagation() : t.originalEvent ? t.originalEvent._stopped = !0 : t.cancelBubble = !0, this;
  }
  function Ut(t) {
    return Wt(t, "wheel", Ht), this;
  }
  function Vt(t) {
    return P(t, "mousedown touchstart dblclick contextmenu", Ht), t._leaflet_disable_click = !0, this;
  }
  function M(t) {
    return t.preventDefault ? t.preventDefault() : t.returnValue = !1, this;
  }
  function Gt(t) {
    return M(t), Ht(t), this;
  }
  function qt(t) {
    if (t.composedPath) return t.composedPath();
    var e = [];
    var i = t.target;
    for (; i;) e.push(i), i = i.parentNode;
    return e;
  }
  function Kt(t, e) {
    var i, o;
    return e ? (o = (i = jt(e)).boundingClientRect, new c((t.clientX - o.left) / i.x - e.clientLeft, (t.clientY - o.top) / i.y - e.clientTop)) : new c(t.clientX, t.clientY);
  }
  function $t() {
    var t = window.devicePixelRatio;
    return v.linux && v.chrome ? t : v.mac ? 3 * t : 0 < t ? 2 * t : 1;
  }
  function Yt(t) {
    return t.deltaY && 0 === t.deltaMode ? -t.deltaY / $t() : t.deltaY && 1 === t.deltaMode ? 20 * -t.deltaY : t.deltaY && 2 === t.deltaMode ? 60 * -t.deltaY : t.deltaX || t.deltaZ ? 0 : t.wheelDelta ? (t.wheelDeltaY || t.wheelDelta) / 2 : t.detail && Math.abs(t.detail) < 32765 ? 20 * -t.detail : t.detail ? t.detail / -32765 * 60 : 0;
  }
  function Xt(t, e) {
    var i = e.relatedTarget;
    if (!i) return !0;
    try {
      for (; i && i !== t;) i = i.parentNode;
    } catch (t) {
      return !1;
    }
    return i !== t;
  }
  nt = {
    __proto__: null,
    addListener: P,
    disableClickPropagation: Vt,
    disableScrollPropagation: Ut,
    getMousePosition: Kt,
    getPropagationPath: qt,
    getWheelDelta: Yt,
    getWheelPxFactor: $t,
    isExternalTarget: Xt,
    off: z,
    on: P,
    preventDefault: M,
    removeListener: z,
    stop: Gt,
    stopPropagation: Ht
  };
  var Jt = e.extend({
      run: function run(t, e, i, o) {
        this.stop(), this._el = t, this._inProgress = !0, this._duration = i || .25, this._easeOutPower = 1 / Math.max(o || .5, .2), this._startPos = Tt(t), this._offset = e.subtract(this._startPos), this._startTime = +new Date(), this.fire("start"), this._animate();
      },
      stop: function stop() {
        this._inProgress && (this._step(!0), this._complete());
      },
      _animate: function _animate() {
        this._animId = x(this._animate, this), this._step();
      },
      _step: function _step(t) {
        var e = +new Date() - this._startTime,
          i = 1e3 * this._duration;
        e < i ? this._runFrame(this._easeOut(e / i), t) : (this._runFrame(1), this._complete());
      },
      _runFrame: function _runFrame(t, e) {
        t = this._startPos.add(this._offset.multiplyBy(t));
        e && t._round(), L(this._el, t), this.fire("step");
      },
      _complete: function _complete() {
        d(this._animId), this._inProgress = !1, this.fire("end");
      },
      _easeOut: function _easeOut(t) {
        return 1 - Math.pow(1 - t, this._easeOutPower);
      }
    }),
    C = e.extend({
      options: {
        crs: s,
        center: void 0,
        zoom: void 0,
        minZoom: void 0,
        maxZoom: void 0,
        layers: [],
        maxBounds: void 0,
        renderer: void 0,
        zoomAnimation: !0,
        zoomAnimationThreshold: 4,
        fadeAnimation: !0,
        markerZoomAnimation: !0,
        transform3DLimit: 8388608,
        zoomSnap: 1,
        zoomDelta: 1,
        trackResize: !0
      },
      initialize: function initialize(t, e) {
        e = n(this, e), this._handlers = [], this._layers = {}, this._zoomBoundLayers = {}, this._sizeChanged = !0, this._initContainer(t), this._initLayout(), this._initEvents(), e.maxBounds && this.setMaxBounds(e.maxBounds), void 0 !== e.zoom && (this._zoom = this._limitZoom(e.zoom)), e.center && void 0 !== e.zoom && this.setView(w(e.center), e.zoom, {
          reset: !0
        }), this.callInitHooks(), this._zoomAnimated = this.options.zoomAnimation, this._zoomAnimated && (this._createAnimProxy(), P(this._proxy, "transitionend", this._catchTransitionEnd, this)), this._addLayers(this.options.layers);
      },
      setView: function setView(t, e, i) {
        if ((e = void 0 === e ? this._zoom : this._limitZoom(e), t = this._limitCenter(w(t), e, this.options.maxBounds), i = i || {}, this._stop(), this._loaded && !i.reset && !0 !== i) && (void 0 !== i.animate && (i.zoom = h({
          animate: i.animate
        }, i.zoom), i.pan = h({
          animate: i.animate,
          duration: i.duration
        }, i.pan)), this._zoom !== e ? this._tryAnimatedZoom && this._tryAnimatedZoom(t, e, i.zoom) : this._tryAnimatedPan(t, i.pan))) return clearTimeout(this._sizeTimer), this;
        return this._resetView(t, e, i.pan && i.pan.noMoveStart), this;
      },
      setZoom: function setZoom(t, e) {
        return this._loaded ? this.setView(this.getCenter(), t, {
          zoom: e
        }) : (this._zoom = t, this);
      },
      zoomIn: function zoomIn(t, e) {
        return t = t || this.options.zoomDelta, this.setZoom(this._zoom + t, e);
      },
      zoomOut: function zoomOut(t, e) {
        return t = t || this.options.zoomDelta, this.setZoom(this._zoom - t, e);
      },
      setZoomAround: function setZoomAround(t, e, i) {
        var o = this.getZoomScale(e),
          s = this.getSize().divideBy(2),
          t = (t instanceof c ? t : this.latLngToContainerPoint(t)).subtract(s).multiplyBy(1 - 1 / o),
          o = this.containerPointToLatLng(s.add(t));
        return this.setView(o, e, {
          zoom: i
        });
      },
      _getBoundsCenterZoom: function _getBoundsCenterZoom(t, e) {
        e = e || {}, t = t.getBounds ? t.getBounds() : f(t);
        var i = g(e.paddingTopLeft || e.padding || [0, 0]),
          o = g(e.paddingBottomRight || e.padding || [0, 0]),
          s = this.getBoundsZoom(t, !1, i.add(o));
        return (s = "number" == typeof e.maxZoom ? Math.min(e.maxZoom, s) : s) === 1 / 0 ? {
          center: t.getCenter(),
          zoom: s
        } : (e = o.subtract(i).divideBy(2), o = this.project(t.getSouthWest(), s), i = this.project(t.getNorthEast(), s), {
          center: this.unproject(o.add(i).divideBy(2).add(e), s),
          zoom: s
        });
      },
      fitBounds: function fitBounds(t, e) {
        if ((t = f(t)).isValid()) return t = this._getBoundsCenterZoom(t, e), this.setView(t.center, t.zoom, e);
        throw new Error("Bounds are not valid.");
      },
      fitWorld: function fitWorld(t) {
        return this.fitBounds([[-90, -180], [90, 180]], t);
      },
      panTo: function panTo(t, e) {
        return this.setView(t, this._zoom, {
          pan: e
        });
      },
      panBy: function panBy(t, e) {
        var i;
        return e = e || {}, (t = g(t).round()).x || t.y ? (!0 === e.animate || this.getSize().contains(t) ? (this._panAnim || (this._panAnim = new Jt(), this._panAnim.on({
          step: this._onPanTransitionStep,
          end: this._onPanTransitionEnd
        }, this)), e.noMoveStart || this.fire("movestart"), !1 !== e.animate ? (this._mapPane.classList.add("leaflet-pan-anim"), i = this._getMapPanePos().subtract(t).round(), this._panAnim.run(this._mapPane, i, e.duration || .25, e.easeLinearity)) : (this._rawPanBy(t), this.fire("move").fire("moveend"))) : this._resetView(this.unproject(this.project(this.getCenter()).add(t)), this.getZoom()), this) : this.fire("moveend");
      },
      flyTo: function flyTo(o, s, t) {
        if (!1 === (t = t || {}).animate) return this.setView(o, s, t);
        this._stop();
        var n = this.project(this.getCenter()),
          a = this.project(o),
          e = this.getSize(),
          r = this._zoom,
          h = (o = w(o), s = void 0 === s ? r : this._limitZoom(s), Math.max(e.x, e.y)),
          i = h * this.getZoomScale(r, s),
          l = a.distanceTo(n) || 1,
          d = 1.42,
          _ = d * d;
        function u(t) {
          var e = t ? -1 : 1,
            t = t ? i : h,
            e = (i * i - h * h + e * _ * _ * l * l) / (2 * t * _ * l),
            t = Math.sqrt(e * e + 1) - e;
          return t < 1e-9 ? -18 : Math.log(t);
        }
        function p(t) {
          return (Math.exp(t) - Math.exp(-t)) / 2;
        }
        function c(t) {
          return (Math.exp(t) + Math.exp(-t)) / 2;
        }
        var m = u(0);
        function g(t) {
          return h * (c(m) * (p(t = m + d * t) / c(t)) - p(m)) / _;
        }
        var f = Date.now(),
          y = (u(1) - m) / d,
          v = t.duration ? 1e3 * t.duration : 1e3 * y * .8;
        return this._moveStart(!0, t.noMoveStart), function t() {
          var e = (Date.now() - f) / v,
            i = (1 - Math.pow(1 - e, 1.5)) * y;
          e <= 1 ? (this._flyToFrame = x(t, this), this._move(this.unproject(n.add(a.subtract(n).multiplyBy(g(i) / l)), r), this.getScaleZoom(h / (e = i, h * (c(m) / c(m + d * e))), r), {
            flyTo: !0
          })) : this._move(o, s)._moveEnd(!0);
        }.call(this), this;
      },
      flyToBounds: function flyToBounds(t, e) {
        t = this._getBoundsCenterZoom(t, e);
        return this.flyTo(t.center, t.zoom, e);
      },
      setMaxBounds: function setMaxBounds(t) {
        return t = f(t), this.listens("moveend", this._panInsideMaxBounds) && this.off("moveend", this._panInsideMaxBounds), t.isValid() ? (this.options.maxBounds = t, this._loaded && this._panInsideMaxBounds(), this.on("moveend", this._panInsideMaxBounds)) : (this.options.maxBounds = null, this);
      },
      setMinZoom: function setMinZoom(t) {
        var e = this.options.minZoom;
        return this.options.minZoom = t, this._loaded && e !== t && (this.fire("zoomlevelschange"), this.getZoom() < this.options.minZoom) ? this.setZoom(t) : this;
      },
      setMaxZoom: function setMaxZoom(t) {
        var e = this.options.maxZoom;
        return this.options.maxZoom = t, this._loaded && e !== t && (this.fire("zoomlevelschange"), this.getZoom() > this.options.maxZoom) ? this.setZoom(t) : this;
      },
      panInsideBounds: function panInsideBounds(t, e) {
        this._enforcingBounds = !0;
        var i = this.getCenter(),
          t = this._limitCenter(i, this._zoom, f(t));
        return i.equals(t) || this.panTo(t, e), this._enforcingBounds = !1, this;
      },
      panInside: function panInside(t, e) {
        var i = g((e = e || {}).paddingTopLeft || e.padding || [0, 0]),
          o = g(e.paddingBottomRight || e.padding || [0, 0]),
          s = this.project(this.getCenter()),
          t = this.project(t),
          n = this.getPixelBounds(),
          i = _([n.min.add(i), n.max.subtract(o)]),
          n = i.getSize();
        return i.contains(t) || (this._enforcingBounds = !0, o = t.subtract(i.getCenter()), i = i.extend(t).getSize().subtract(n), s.x += o.x < 0 ? -i.x : i.x, s.y += o.y < 0 ? -i.y : i.y, this.panTo(this.unproject(s), e), this._enforcingBounds = !1), this;
      },
      invalidateSize: function invalidateSize(t) {
        if (!this._loaded) return this;
        t = h({
          animate: !1,
          pan: !0
        }, !0 === t ? {
          animate: !0
        } : t);
        var e = this.getSize(),
          i = (this._sizeChanged = !0, this._lastCenter = null, this.getSize()),
          o = e.divideBy(2).round(),
          s = i.divideBy(2).round(),
          o = o.subtract(s);
        return o.x || o.y ? (t.animate && t.pan ? this.panBy(o) : (t.pan && this._rawPanBy(o), this.fire("move"), t.debounceMoveend ? (clearTimeout(this._sizeTimer), this._sizeTimer = setTimeout(this.fire.bind(this, "moveend"), 200)) : this.fire("moveend")), this.fire("resize", {
          oldSize: e,
          newSize: i
        })) : this;
      },
      stop: function stop() {
        return this.setZoom(this._limitZoom(this._zoom)), this.options.zoomSnap || this.fire("viewreset"), this._stop();
      },
      locate: function locate(t) {
        var e, i;
        return t = this._locateOptions = h({
          timeout: 1e4,
          watch: !1
        }, t), "geolocation" in navigator ? (e = this._handleGeolocationResponse.bind(this), i = this._handleGeolocationError.bind(this), t.watch ? this._locationWatchId = navigator.geolocation.watchPosition(e, i, t) : navigator.geolocation.getCurrentPosition(e, i, t)) : this._handleGeolocationError({
          code: 0,
          message: "Geolocation not supported."
        }), this;
      },
      stopLocate: function stopLocate() {
        return navigator.geolocation && navigator.geolocation.clearWatch && navigator.geolocation.clearWatch(this._locationWatchId), this._locateOptions && (this._locateOptions.setView = !1), this;
      },
      _handleGeolocationError: function _handleGeolocationError(t) {
        var e;
        this._container._leaflet_id && (e = t.code, t = t.message || (1 === e ? "permission denied" : 2 === e ? "position unavailable" : "timeout"), this._locateOptions.setView && !this._loaded && this.fitWorld(), this.fire("locationerror", {
          code: e,
          message: "Geolocation error: ".concat(t, ".")
        }));
      },
      _handleGeolocationResponse: function _handleGeolocationResponse(t) {
        if (this._container._leaflet_id) {
          var e,
            i = new p(t.coords.latitude, t.coords.longitude),
            o = i.toBounds(2 * t.coords.accuracy),
            s = this._locateOptions,
            n = (s.setView && (e = this.getBoundsZoom(o), this.setView(i, s.maxZoom ? Math.min(e, s.maxZoom) : e)), {
              latlng: i,
              bounds: o,
              timestamp: t.timestamp
            });
          for (var _a3 in t.coords) "number" == typeof t.coords[_a3] && (n[_a3] = t.coords[_a3]);
          this.fire("locationfound", n);
        }
      },
      addHandler: function addHandler(t, e) {
        return e && (e = this[t] = new e(this), this._handlers.push(e), this.options[t]) && e.enable(), this;
      },
      remove: function remove() {
        if (this._initEvents(!0), this.options.maxBounds && this.off("moveend", this._panInsideMaxBounds), this._containerId !== this._container._leaflet_id) throw new Error("Map container is being reused by another instance");
        try {
          delete this._container._leaflet_id, delete this._containerId;
        } catch (t) {
          this._container._leaflet_id = void 0, this._containerId = void 0;
        }
        void 0 !== this._locationWatchId && this.stopLocate(), this._stop(), this._mapPane.remove(), this._clearControlPos && this._clearControlPos(), this._resizeRequest && (d(this._resizeRequest), this._resizeRequest = null), this._clearHandlers(), this._loaded && this.fire("unload");
        var t;
        for (t in this._layers) Object.hasOwn(this._layers, t) && this._layers[t].remove();
        for (t in this._panes) Object.hasOwn(this._panes, t) && this._panes[t].remove();
        return this._layers = [], this._panes = {}, delete this._mapPane, delete this._renderer, this;
      },
      createPane: function createPane(t, e) {
        e = b("div", "leaflet-pane" + (t ? " leaflet-".concat(t.replace("Pane", ""), "-pane") : ""), e || this._mapPane);
        return t && (this._panes[t] = e), e;
      },
      getCenter: function getCenter() {
        return this._checkIfLoaded(), this._lastCenter && !this._moved() ? this._lastCenter.clone() : this.layerPointToLatLng(this._getCenterLayerPoint());
      },
      getZoom: function getZoom() {
        return this._zoom;
      },
      getBounds: function getBounds() {
        var t = this.getPixelBounds();
        return new u(this.unproject(t.getBottomLeft()), this.unproject(t.getTopRight()));
      },
      getMinZoom: function getMinZoom() {
        return void 0 === this.options.minZoom ? this._layersMinZoom || 0 : this.options.minZoom;
      },
      getMaxZoom: function getMaxZoom() {
        return void 0 === this.options.maxZoom ? void 0 === this._layersMaxZoom ? 1 / 0 : this._layersMaxZoom : this.options.maxZoom;
      },
      getBoundsZoom: function getBoundsZoom(t, e, i) {
        t = f(t), i = g(i || [0, 0]);
        var o = this.getZoom() || 0;
        var s = this.getMinZoom(),
          n = this.getMaxZoom(),
          a = t.getNorthWest(),
          t = t.getSouthEast(),
          i = this.getSize().subtract(i),
          t = _(this.project(t, o), this.project(a, o)).getSize(),
          a = this.options.zoomSnap,
          r = i.x / t.x,
          i = i.y / t.y,
          t = e ? Math.max(r, i) : Math.min(r, i);
        return o = this.getScaleZoom(t, o), a && (o = Math.round(o / (a / 100)) * (a / 100), o = e ? Math.ceil(o / a) * a : Math.floor(o / a) * a), Math.max(s, Math.min(n, o));
      },
      getSize: function getSize() {
        return this._size && !this._sizeChanged || (this._size = new c(this._container.clientWidth || 0, this._container.clientHeight || 0), this._sizeChanged = !1), this._size.clone();
      },
      getPixelBounds: function getPixelBounds(t, e) {
        t = this._getTopLeftPoint(t, e);
        return new m(t, t.add(this.getSize()));
      },
      getPixelOrigin: function getPixelOrigin() {
        return this._checkIfLoaded(), this._pixelOrigin;
      },
      getPixelWorldBounds: function getPixelWorldBounds(t) {
        return this.options.crs.getProjectedBounds(void 0 === t ? this.getZoom() : t);
      },
      getPane: function getPane(t) {
        return "string" == typeof t ? this._panes[t] : t;
      },
      getPanes: function getPanes() {
        return this._panes;
      },
      getContainer: function getContainer() {
        return this._container;
      },
      getZoomScale: function getZoomScale(t, e) {
        var i = this.options.crs;
        return e = void 0 === e ? this._zoom : e, i.scale(t) / i.scale(e);
      },
      getScaleZoom: function getScaleZoom(t, e) {
        var i = this.options.crs,
          t = (e = void 0 === e ? this._zoom : e, i.zoom(t * i.scale(e)));
        return isNaN(t) ? 1 / 0 : t;
      },
      project: function project(t, e) {
        return e = void 0 === e ? this._zoom : e, this.options.crs.latLngToPoint(w(t), e);
      },
      unproject: function unproject(t, e) {
        return e = void 0 === e ? this._zoom : e, this.options.crs.pointToLatLng(g(t), e);
      },
      layerPointToLatLng: function layerPointToLatLng(t) {
        t = g(t).add(this.getPixelOrigin());
        return this.unproject(t);
      },
      latLngToLayerPoint: function latLngToLayerPoint(t) {
        return this.project(w(t))._round()._subtract(this.getPixelOrigin());
      },
      wrapLatLng: function wrapLatLng(t) {
        return this.options.crs.wrapLatLng(w(t));
      },
      wrapLatLngBounds: function wrapLatLngBounds(t) {
        return this.options.crs.wrapLatLngBounds(f(t));
      },
      distance: function distance(t, e) {
        return this.options.crs.distance(w(t), w(e));
      },
      containerPointToLayerPoint: function containerPointToLayerPoint(t) {
        return g(t).subtract(this._getMapPanePos());
      },
      layerPointToContainerPoint: function layerPointToContainerPoint(t) {
        return g(t).add(this._getMapPanePos());
      },
      containerPointToLatLng: function containerPointToLatLng(t) {
        t = this.containerPointToLayerPoint(g(t));
        return this.layerPointToLatLng(t);
      },
      latLngToContainerPoint: function latLngToContainerPoint(t) {
        return this.layerPointToContainerPoint(this.latLngToLayerPoint(w(t)));
      },
      mouseEventToContainerPoint: function mouseEventToContainerPoint(t) {
        return Kt(t, this._container);
      },
      mouseEventToLayerPoint: function mouseEventToLayerPoint(t) {
        return this.containerPointToLayerPoint(this.mouseEventToContainerPoint(t));
      },
      mouseEventToLatLng: function mouseEventToLatLng(t) {
        return this.layerPointToLatLng(this.mouseEventToLayerPoint(t));
      },
      _initContainer: function _initContainer(t) {
        t = this._container = xt(t);
        if (!t) throw new Error("Map container not found.");
        if (t._leaflet_id) throw new Error("Map container is already initialized.");
        P(t, "scroll", this._onScroll, this), this._containerId = r(t);
      },
      _initLayout: function _initLayout() {
        var _t$classList;
        var t = this._container,
          e = (this._fadeAnimated = this.options.fadeAnimation, ["leaflet-container"]),
          e = (v.touch && e.push("leaflet-touch"), v.retina && e.push("leaflet-retina"), v.safari && e.push("leaflet-safari"), this._fadeAnimated && e.push("leaflet-fade-anim"), (_t$classList = t.classList).add.apply(_t$classList, _toConsumableArray(e)), getComputedStyle(t))["position"];
        "absolute" !== e && "relative" !== e && "fixed" !== e && "sticky" !== e && (t.style.position = "relative"), this._initPanes(), this._initControlPos && this._initControlPos();
      },
      _initPanes: function _initPanes() {
        var t = this._panes = {};
        this._paneRenderers = {}, this._mapPane = this.createPane("mapPane", this._container), L(this._mapPane, new c(0, 0)), this.createPane("tilePane"), this.createPane("overlayPane"), this.createPane("shadowPane"), this.createPane("markerPane"), this.createPane("tooltipPane"), this.createPane("popupPane"), this.options.markerZoomAnimation || (t.markerPane.classList.add("leaflet-zoom-hide"), t.shadowPane.classList.add("leaflet-zoom-hide"));
      },
      _resetView: function _resetView(t, e, i) {
        L(this._mapPane, new c(0, 0));
        var o = !this._loaded,
          s = (this._loaded = !0, e = this._limitZoom(e), this.fire("viewprereset"), this._zoom !== e);
        this._moveStart(s, i)._move(t, e)._moveEnd(s), this.fire("viewreset"), o && this.fire("load");
      },
      _moveStart: function _moveStart(t, e) {
        return t && this.fire("zoomstart"), e || this.fire("movestart"), this;
      },
      _move: function _move(t, e, i, o) {
        void 0 === e && (e = this._zoom);
        var s = this._zoom !== e;
        return this._zoom = e, this._lastCenter = t, this._pixelOrigin = this._getNewPixelOrigin(t), o ? i && i.pinch && this.fire("zoom", i) : ((s || i && i.pinch) && this.fire("zoom", i), this.fire("move", i)), this;
      },
      _moveEnd: function _moveEnd(t) {
        return t && this.fire("zoomend"), this.fire("moveend");
      },
      _stop: function _stop() {
        return d(this._flyToFrame), this._panAnim && this._panAnim.stop(), this;
      },
      _rawPanBy: function _rawPanBy(t) {
        L(this._mapPane, this._getMapPanePos().subtract(t));
      },
      _getZoomSpan: function _getZoomSpan() {
        return this.getMaxZoom() - this.getMinZoom();
      },
      _panInsideMaxBounds: function _panInsideMaxBounds() {
        this._enforcingBounds || this.panInsideBounds(this.options.maxBounds);
      },
      _checkIfLoaded: function _checkIfLoaded() {
        if (!this._loaded) throw new Error("Set map center and zoom first.");
      },
      _initEvents: function _initEvents(t) {
        this._targets = {}, (t ? z : P)((this._targets[r(this._container)] = this)._container, "click dblclick mousedown mouseup mouseover mouseout mousemove contextmenu keypress keydown keyup", this._handleDOMEvent, this), this.options.trackResize && (t ? this._resizeObserver.disconnect() : (this._resizeObserver || (this._resizeObserver = new ResizeObserver(this._onResize.bind(this))), this._resizeObserver.observe(this._container))), this.options.transform3DLimit && (t ? this.off : this.on).call(this, "moveend", this._onMoveEnd);
      },
      _onResize: function _onResize() {
        d(this._resizeRequest), this._resizeRequest = x(function () {
          this.invalidateSize({
            debounceMoveend: !0
          });
        }, this);
      },
      _onScroll: function _onScroll() {
        this._container.scrollTop = 0, this._container.scrollLeft = 0;
      },
      _onMoveEnd: function _onMoveEnd() {
        var t = this._getMapPanePos();
        Math.max(Math.abs(t.x), Math.abs(t.y)) >= this.options.transform3DLimit && this._resetView(this.getCenter(), this.getZoom());
      },
      _findEventTargets: function _findEventTargets(t, e) {
        var i = [],
          o,
          s = t.target || t.srcElement,
          n = !1;
        for (var a = "mouseout" === e || "mouseover" === e; s;) {
          if ((o = this._targets[r(s)]) && ("click" === e || "preclick" === e) && this._draggableMoved(o)) {
            n = !0;
            break;
          }
          if (o && o.listens(e, !0)) {
            if (a && !Xt(s, t)) break;
            if (i.push(o), a) break;
          }
          if (s === this._container) break;
          s = s.parentNode;
        }
        return i = i.length || n || a || !this.listens(e, !0) ? i : [this];
      },
      _isClickDisabled: function _isClickDisabled(t) {
        for (; t && t !== this._container;) {
          if (t._leaflet_disable_click || !t.parentNode) return !0;
          t = t.parentNode;
        }
      },
      _handleDOMEvent: function _handleDOMEvent(t) {
        var e,
          i = t.target || t.srcElement;
        !this._loaded || i._leaflet_disable_events || "click" === t.type && this._isClickDisabled(i) || ("mousedown" === (e = t.type) && At(i), this._fireDOMEvent(t, e));
      },
      _mouseEvents: ["click", "dblclick", "mouseover", "mouseout", "contextmenu"],
      _fireDOMEvent: function _fireDOMEvent(t, e, i) {
        "click" === t.type && ((a = h({}, t)).type = "preclick", this._fireDOMEvent(a, a.type, i));
        var o = this._findEventTargets(t, e);
        if (i) {
          var s = [];
          for (var _t16 = 0; _t16 < i.length; _t16++) i[_t16].listens(e, !0) && s.push(i[_t16]);
          o = s.concat(o);
        }
        if (o.length) {
          "contextmenu" === e && M(t);
          var n,
            a = o[0],
            r = {
              originalEvent: t
            };
          "keypress" !== t.type && "keydown" !== t.type && "keyup" !== t.type && (n = a.getLatLng && (!a._radius || a._radius <= 10), r.containerPoint = n ? this.latLngToContainerPoint(a.getLatLng()) : this.mouseEventToContainerPoint(t), r.layerPoint = this.containerPointToLayerPoint(r.containerPoint), r.latlng = n ? a.getLatLng() : this.layerPointToLatLng(r.layerPoint));
          for (var _t17 = 0; _t17 < o.length; _t17++) if (o[_t17].fire(e, r, !0), r.originalEvent._stopped || !1 === o[_t17].options.bubblingMouseEvents && this._mouseEvents.includes(e)) return;
        }
      },
      _draggableMoved: function _draggableMoved(t) {
        return (t = t.dragging && t.dragging.enabled() ? t : this).dragging && t.dragging.moved() || this.boxZoom && this.boxZoom.moved();
      },
      _clearHandlers: function _clearHandlers() {
        for (var _t18 = 0, _e13 = this._handlers.length; _t18 < _e13; _t18++) this._handlers[_t18].disable();
      },
      whenReady: function whenReady(t, e) {
        return this._loaded ? t.call(e || this, {
          target: this
        }) : this.on("load", t, e), this;
      },
      _getMapPanePos: function _getMapPanePos() {
        return Tt(this._mapPane) || new c(0, 0);
      },
      _moved: function _moved() {
        var t = this._getMapPanePos();
        return t && !t.equals([0, 0]);
      },
      _getTopLeftPoint: function _getTopLeftPoint(t, e) {
        return (t && void 0 !== e ? this._getNewPixelOrigin(t, e) : this.getPixelOrigin()).subtract(this._getMapPanePos());
      },
      _getNewPixelOrigin: function _getNewPixelOrigin(t, e) {
        var i = this.getSize()._divideBy(2);
        return this.project(t, e)._subtract(i)._add(this._getMapPanePos())._round();
      },
      _latLngToNewLayerPoint: function _latLngToNewLayerPoint(t, e, i) {
        i = this._getNewPixelOrigin(i, e);
        return this.project(t, e)._subtract(i);
      },
      _latLngBoundsToNewLayerBounds: function _latLngBoundsToNewLayerBounds(t, e, i) {
        i = this._getNewPixelOrigin(i, e);
        return _([this.project(t.getSouthWest(), e)._subtract(i), this.project(t.getNorthWest(), e)._subtract(i), this.project(t.getSouthEast(), e)._subtract(i), this.project(t.getNorthEast(), e)._subtract(i)]);
      },
      _getCenterLayerPoint: function _getCenterLayerPoint() {
        return this.containerPointToLayerPoint(this.getSize()._divideBy(2));
      },
      _getCenterOffset: function _getCenterOffset(t) {
        return this.latLngToLayerPoint(t).subtract(this._getCenterLayerPoint());
      },
      _limitCenter: function _limitCenter(t, e, i) {
        var o, s;
        return !i || (o = this.project(t, e), s = this.getSize().divideBy(2), s = new m(o.subtract(s), o.add(s)), s = this._getBoundsOffset(s, i, e), Math.abs(s.x) <= 1 && Math.abs(s.y) <= 1) ? t : this.unproject(o.add(s), e);
      },
      _limitOffset: function _limitOffset(t, e) {
        var i;
        return e ? (i = new m((i = this.getPixelBounds()).min.add(t), i.max.add(t)), t.add(this._getBoundsOffset(i, e))) : t;
      },
      _getBoundsOffset: function _getBoundsOffset(t, e, i) {
        e = _(this.project(e.getNorthEast(), i), this.project(e.getSouthWest(), i)), i = e.min.subtract(t.min), e = e.max.subtract(t.max);
        return new c(this._rebound(i.x, -e.x), this._rebound(i.y, -e.y));
      },
      _rebound: function _rebound(t, e) {
        return 0 < t + e ? Math.round(t - e) / 2 : Math.max(0, Math.ceil(t)) - Math.max(0, Math.floor(e));
      },
      _limitZoom: function _limitZoom(t) {
        var e = this.getMinZoom(),
          i = this.getMaxZoom(),
          o = this.options.zoomSnap;
        return o && (t = Math.round(t / o) * o), Math.max(e, Math.min(i, t));
      },
      _onPanTransitionStep: function _onPanTransitionStep() {
        this.fire("move");
      },
      _onPanTransitionEnd: function _onPanTransitionEnd() {
        this._mapPane.classList.remove("leaflet-pan-anim"), this.fire("moveend");
      },
      _tryAnimatedPan: function _tryAnimatedPan(t, e) {
        t = this._getCenterOffset(t)._trunc();
        return !(!0 !== (e && e.animate) && !this.getSize().contains(t) || (this.panBy(t, e), 0));
      },
      _createAnimProxy: function _createAnimProxy() {
        var t = this._proxy = b("div", "leaflet-proxy leaflet-zoom-animated");
        this._panes.mapPane.appendChild(t), this.on("zoomanim", function (t) {
          var e = this._proxy.style.transform;
          Lt(this._proxy, this.project(t.center, t.zoom), this.getZoomScale(t.zoom, 1)), e === this._proxy.style.transform && this._animatingZoom && this._onZoomTransitionEnd();
        }, this), this.on("load moveend", this._animMoveEnd, this), this._on("unload", this._destroyAnimProxy, this);
      },
      _destroyAnimProxy: function _destroyAnimProxy() {
        this._proxy.remove(), this.off("load moveend", this._animMoveEnd, this), delete this._proxy;
      },
      _animMoveEnd: function _animMoveEnd() {
        var t = this.getCenter(),
          e = this.getZoom();
        Lt(this._proxy, this.project(t, e), this.getZoomScale(e, 1));
      },
      _catchTransitionEnd: function _catchTransitionEnd(t) {
        this._animatingZoom && t.propertyName.includes("transform") && this._onZoomTransitionEnd();
      },
      _nothingToAnimate: function _nothingToAnimate() {
        return !this._container.getElementsByClassName("leaflet-zoom-animated").length;
      },
      _tryAnimatedZoom: function _tryAnimatedZoom(t, e, i) {
        if (!this._animatingZoom) {
          if (i = i || {}, !this._zoomAnimated || !1 === i.animate || this._nothingToAnimate() || Math.abs(e - this._zoom) > this.options.zoomAnimationThreshold) return !1;
          var o = this.getZoomScale(e),
            o = this._getCenterOffset(t)._divideBy(1 - 1 / o);
          if (!0 !== i.animate && !this.getSize().contains(o)) return !1;
          x(function () {
            var _i$noMoveStart;
            this._moveStart(!0, (_i$noMoveStart = i.noMoveStart) !== null && _i$noMoveStart !== void 0 ? _i$noMoveStart : !1)._animateZoom(t, e, !0);
          }, this);
        }
        return !0;
      },
      _animateZoom: function _animateZoom(t, e, i, o) {
        this._mapPane && (i && (this._animatingZoom = !0, this._animateToCenter = t, this._animateToZoom = e, this._mapPane.classList.add("leaflet-zoom-anim")), this.fire("zoomanim", {
          center: t,
          zoom: e,
          noUpdate: o
        }), this._tempFireZoomEvent || (this._tempFireZoomEvent = this._zoom !== this._animateToZoom), this._move(this._animateToCenter, this._animateToZoom, void 0, !0), setTimeout(this._onZoomTransitionEnd.bind(this), 250));
      },
      _onZoomTransitionEnd: function _onZoomTransitionEnd() {
        this._animatingZoom && (this._mapPane && this._mapPane.classList.remove("leaflet-zoom-anim"), this._animatingZoom = !1, this._move(this._animateToCenter, this._animateToZoom, void 0, !0), this._tempFireZoomEvent && this.fire("zoom"), delete this._tempFireZoomEvent, this.fire("move"), this._moveEnd(!0));
      }
    });
  function Qt(t, e) {
    return new C(t, e);
  }
  var Z = Y.extend({
    options: {
      position: "topright"
    },
    initialize: function initialize(t) {
      n(this, t);
    },
    getPosition: function getPosition() {
      return this.options.position;
    },
    setPosition: function setPosition(t) {
      var e = this._map;
      return e && e.removeControl(this), this.options.position = t, e && e.addControl(this), this;
    },
    getContainer: function getContainer() {
      return this._container;
    },
    addTo: function addTo(t) {
      this.remove(), this._map = t;
      var e = this._container = this.onAdd(t),
        i = this.getPosition(),
        t = t._controlCorners[i];
      return e.classList.add("leaflet-control"), i.includes("bottom") ? t.insertBefore(e, t.firstChild) : t.appendChild(e), this._map.on("unload", this.remove, this), this;
    },
    remove: function remove() {
      return this._map && (this._container.remove(), this.onRemove && this.onRemove(this._map), this._map.off("unload", this.remove, this), this._map = null), this;
    },
    _refocusOnMap: function _refocusOnMap(t) {
      this._map && t && 0 < t.screenX && 0 < t.screenY && this._map.getContainer().focus();
    }
  });
  function te(t) {
    return new Z(t);
  }
  C.include({
    addControl: function addControl(t) {
      return t.addTo(this), this;
    },
    removeControl: function removeControl(t) {
      return t.remove(), this;
    },
    _initControlPos: function _initControlPos() {
      var o = this._controlCorners = {},
        s = "leaflet-",
        n = this._controlContainer = b("div", s + "control-container", this._container);
      function t(t, e) {
        var i = s + t + " " + s + e;
        o[t + e] = b("div", i, n);
      }
      t("top", "left"), t("top", "right"), t("bottom", "left"), t("bottom", "right");
    },
    _clearControlPos: function _clearControlPos() {
      for (var _t19 in this._controlCorners) Object.hasOwn(this._controlCorners, _t19) && this._controlCorners[_t19].remove();
      this._controlContainer.remove(), delete this._controlCorners, delete this._controlContainer;
    }
  });
  var ee = Z.extend({
    options: {
      collapsed: !0,
      position: "topright",
      autoZIndex: !0,
      hideSingleBase: !1,
      sortLayers: !1,
      sortFunction: function sortFunction(t, e, i, o) {
        return i < o ? -1 : o < i ? 1 : 0;
      }
    },
    initialize: function initialize(t, e, i) {
      n(this, i), this._layerControlInputs = [], this._layers = [], this._lastZIndex = 0, this._handlingClick = !1, this._preventClick = !1;
      for (var _o3 in t) Object.hasOwn(t, _o3) && this._addLayer(t[_o3], _o3);
      for (var _s2 in e) Object.hasOwn(e, _s2) && this._addLayer(e[_s2], _s2, !0);
    },
    onAdd: function onAdd(t) {
      this._initLayout(), this._update(), (this._map = t).on("zoomend", this._checkDisabledLayers, this);
      for (var _t20 = 0; _t20 < this._layers.length; _t20++) this._layers[_t20].layer.on("add remove", this._onLayerChange, this);
      return this.options.collapsed || t.on("resize", this._expandIfNotCollapsed, this), this._container;
    },
    addTo: function addTo(t) {
      return Z.prototype.addTo.call(this, t), this._expandIfNotCollapsed();
    },
    onRemove: function onRemove() {
      this._map.off("zoomend", this._checkDisabledLayers, this);
      for (var _t21 = 0; _t21 < this._layers.length; _t21++) this._layers[_t21].layer.off("add remove", this._onLayerChange, this);
      this._map.off("resize", this._expandIfNotCollapsed, this);
    },
    addBaseLayer: function addBaseLayer(t, e) {
      return this._addLayer(t, e), this._map ? this._update() : this;
    },
    addOverlay: function addOverlay(t, e) {
      return this._addLayer(t, e, !0), this._map ? this._update() : this;
    },
    removeLayer: function removeLayer(t) {
      t.off("add remove", this._onLayerChange, this);
      t = this._getLayer(r(t));
      return t && this._layers.splice(this._layers.indexOf(t), 1), this._map ? this._update() : this;
    },
    expand: function expand() {
      this._container.classList.add("leaflet-control-layers-expanded"), this._section.style.height = null;
      var t = this._map.getSize().y - (this._container.offsetTop + 50);
      return t < this._section.clientHeight ? (this._section.classList.add("leaflet-control-layers-scrollbar"), this._section.style.height = t + "px") : this._section.classList.remove("leaflet-control-layers-scrollbar"), this._checkDisabledLayers(), this;
    },
    collapse: function collapse(t) {
      return t && "pointerleave" === t.type && "touch" === t.pointerType || this._container.classList.remove("leaflet-control-layers-expanded"), this;
    },
    _initLayout: function _initLayout() {
      var t = "leaflet-control-layers",
        e = this._container = b("div", t),
        i = this.options.collapsed,
        o = (e.setAttribute("aria-haspopup", !0), Vt(e), Ut(e), this._section = b("fieldset", t + "-list")),
        s = (i && (this._map.on("click", this.collapse, this), P(e, {
          pointerenter: this._expandSafely,
          pointerleave: this.collapse
        }, this)), this._layersLink = b("a", t + "-toggle", e));
      s.href = "#", s.title = "Layers", s.setAttribute("role", "button"), P(s, {
        keydown: function keydown(t) {
          "Enter" === t.code && this._expandSafely();
        },
        click: function click(t) {
          M(t), this._expandSafely();
        }
      }, this), i || this.expand(), this._baseLayersList = b("div", t + "-base", o), this._separator = b("div", t + "-separator", o), this._overlaysList = b("div", t + "-overlays", o), e.appendChild(o);
    },
    _getLayer: function _getLayer(e) {
      for (var _t22 = 0; _t22 < this._layers.length; _t22++) if (this._layers[_t22] && r(this._layers[_t22].layer) === e) return this._layers[_t22];
    },
    _addLayer: function _addLayer(t, e, i) {
      var _this2 = this;
      this._map && t.on("add remove", this._onLayerChange, this), this._layers.push({
        layer: t,
        name: e,
        overlay: i
      }), this.options.sortLayers && this._layers.sort(function (t, e) {
        return _this2.options.sortFunction(t.layer, e.layer, t.name, e.name);
      }), this.options.autoZIndex && t.setZIndex && (this._lastZIndex++, t.setZIndex(this._lastZIndex)), this._expandIfNotCollapsed();
    },
    _update: function _update() {
      if (this._container) {
        this._baseLayersList.replaceChildren(), this._overlaysList.replaceChildren(), this._layerControlInputs = [];
        var _t23,
          _e14,
          _i8,
          _o4,
          _s3 = 0;
        for (_i8 = 0; _i8 < this._layers.length; _i8++) _o4 = this._layers[_i8], this._addItem(_o4), _e14 = _e14 || _o4.overlay, _t23 = _t23 || !_o4.overlay, _s3 += _o4.overlay ? 0 : 1;
        this.options.hideSingleBase && (_t23 = _t23 && 1 < _s3, this._baseLayersList.style.display = _t23 ? "" : "none"), this._separator.style.display = _e14 && _t23 ? "" : "none";
      }
      return this;
    },
    _onLayerChange: function _onLayerChange(t) {
      this._handlingClick || this._update();
      var e = this._getLayer(r(t.target)),
        t = e.overlay ? "add" === t.type ? "overlayadd" : "overlayremove" : "add" === t.type ? "baselayerchange" : null;
      t && this._map.fire(t, e);
    },
    _createRadioElement: function _createRadioElement(t, e) {
      t = "<input type=\"radio\" class=\"leaflet-control-layers-selector\" name=\"".concat(t, "\"").concat(e ? ' checked="checked"' : "", "/>"), e = document.createElement("div");
      return e.innerHTML = t, e.firstChild;
    },
    _addItem: function _addItem(t) {
      var e = document.createElement("label"),
        i = this._map.hasLayer(t.layer);
      var o;
      t.overlay ? ((o = document.createElement("input")).type = "checkbox", o.className = "leaflet-control-layers-selector", o.defaultChecked = i) : o = this._createRadioElement("leaflet-base-layers_" + r(this), i), this._layerControlInputs.push(o), o.layerId = r(t.layer), P(o, "click", this._onInputClick, this);
      var i = document.createElement("span"),
        s = (i.innerHTML = " " + t.name, document.createElement("span"));
      return e.appendChild(s), s.appendChild(o), s.appendChild(i), (t.overlay ? this._overlaysList : this._baseLayersList).appendChild(e), this._checkDisabledLayers(), e;
    },
    _onInputClick: function _onInputClick() {
      if (!this._preventClick) {
        var e,
          i,
          o = this._layerControlInputs,
          s = [],
          n = [];
        this._handlingClick = !0;
        for (var _t24 = o.length - 1; 0 <= _t24; _t24--) e = o[_t24], i = this._getLayer(e.layerId).layer, e.checked ? s.push(i) : e.checked || n.push(i);
        for (var _t25 = 0; _t25 < n.length; _t25++) this._map.hasLayer(n[_t25]) && this._map.removeLayer(n[_t25]);
        for (var _t26 = 0; _t26 < s.length; _t26++) this._map.hasLayer(s[_t26]) || this._map.addLayer(s[_t26]);
        this._handlingClick = !1, this._refocusOnMap();
      }
    },
    _checkDisabledLayers: function _checkDisabledLayers() {
      var e,
        i,
        o = this._layerControlInputs,
        s = this._map.getZoom();
      for (var _t27 = o.length - 1; 0 <= _t27; _t27--) e = o[_t27], i = this._getLayer(e.layerId).layer, e.disabled = void 0 !== i.options.minZoom && s < i.options.minZoom || void 0 !== i.options.maxZoom && s > i.options.maxZoom;
    },
    _expandIfNotCollapsed: function _expandIfNotCollapsed() {
      return this._map && !this.options.collapsed && this.expand(), this;
    },
    _expandSafely: function _expandSafely() {
      var _this3 = this;
      var t = this._section;
      this._preventClick = !0, P(t, "click", M), this.expand(), setTimeout(function () {
        z(t, "click", M), _this3._preventClick = !1;
      });
    }
  });
  var ie = Z.extend({
    options: {
      position: "topleft",
      zoomInText: '<span aria-hidden="true">+</span>',
      zoomInTitle: "Zoom in",
      zoomOutText: '<span aria-hidden="true">&#x2212;</span>',
      zoomOutTitle: "Zoom out"
    },
    onAdd: function onAdd(t) {
      var e = "leaflet-control-zoom",
        i = b("div", e + " leaflet-bar"),
        o = this.options;
      return this._zoomInButton = this._createButton(o.zoomInText, o.zoomInTitle, e + "-in", i, this._zoomIn), this._zoomOutButton = this._createButton(o.zoomOutText, o.zoomOutTitle, e + "-out", i, this._zoomOut), this._updateDisabled(), t.on("zoomend zoomlevelschange", this._updateDisabled, this), i;
    },
    onRemove: function onRemove(t) {
      t.off("zoomend zoomlevelschange", this._updateDisabled, this);
    },
    disable: function disable() {
      return this._disabled = !0, this._updateDisabled(), this;
    },
    enable: function enable() {
      return this._disabled = !1, this._updateDisabled(), this;
    },
    _zoomIn: function _zoomIn(t) {
      !this._disabled && this._map._zoom < this._map.getMaxZoom() && this._map.zoomIn(this._map.options.zoomDelta * (t.shiftKey ? 3 : 1));
    },
    _zoomOut: function _zoomOut(t) {
      !this._disabled && this._map._zoom > this._map.getMinZoom() && this._map.zoomOut(this._map.options.zoomDelta * (t.shiftKey ? 3 : 1));
    },
    _createButton: function _createButton(t, e, i, o, s) {
      i = b("a", i, o);
      return i.innerHTML = t, i.href = "#", i.title = e, i.setAttribute("role", "button"), i.setAttribute("aria-label", e), Vt(i), P(i, "click", Gt), P(i, "click", s, this), P(i, "click", this._refocusOnMap, this), i;
    },
    _updateDisabled: function _updateDisabled() {
      var t = this._map,
        e = "leaflet-disabled";
      this._zoomInButton.classList.remove(e), this._zoomOutButton.classList.remove(e), this._zoomInButton.setAttribute("aria-disabled", "false"), this._zoomOutButton.setAttribute("aria-disabled", "false"), !this._disabled && t._zoom !== t.getMinZoom() || (this._zoomOutButton.classList.add(e), this._zoomOutButton.setAttribute("aria-disabled", "true")), !this._disabled && t._zoom !== t.getMaxZoom() || (this._zoomInButton.classList.add(e), this._zoomInButton.setAttribute("aria-disabled", "true"));
    }
  });
  C.mergeOptions({
    zoomControl: !0
  }), C.addInitHook(function () {
    this.options.zoomControl && (this.zoomControl = new ie(), this.addControl(this.zoomControl));
  });
  var oe = Z.extend({
    options: {
      position: "bottomleft",
      maxWidth: 100,
      metric: !0,
      imperial: !0
    },
    onAdd: function onAdd(t) {
      var e = "leaflet-control-scale",
        i = b("div", e),
        o = this.options;
      return this._addScales(o, e + "-line", i), t.on(o.updateWhenIdle ? "moveend" : "move", this._update, this), t.whenReady(this._update, this), i;
    },
    onRemove: function onRemove(t) {
      t.off(this.options.updateWhenIdle ? "moveend" : "move", this._update, this);
    },
    _addScales: function _addScales(t, e, i) {
      t.metric && (this._mScale = b("div", e, i)), t.imperial && (this._iScale = b("div", e, i));
    },
    _update: function _update() {
      var t = this._map,
        e = t.getSize().y / 2,
        t = t.distance(t.containerPointToLatLng([0, e]), t.containerPointToLatLng([this.options.maxWidth, e]));
      this._updateScales(t);
    },
    _updateScales: function _updateScales(t) {
      this.options.metric && t && this._updateMetric(t), this.options.imperial && t && this._updateImperial(t);
    },
    _updateMetric: function _updateMetric(t) {
      var e = this._getRoundNum(t);
      this._updateScale(this._mScale, e < 1e3 ? e + " m" : e / 1e3 + " km", e / t);
    },
    _updateImperial: function _updateImperial(t) {
      var e,
        i,
        t = 3.2808399 * t;
      5280 < t ? (i = this._getRoundNum(e = t / 5280), this._updateScale(this._iScale, i + " mi", i / e)) : (i = this._getRoundNum(t), this._updateScale(this._iScale, i + " ft", i / t));
    },
    _updateScale: function _updateScale(t, e, i) {
      t.style.width = Math.round(this.options.maxWidth * i) + "px", t.innerHTML = e;
    },
    _getRoundNum: function _getRoundNum(t) {
      var e = Math.pow(10, ("" + Math.floor(t)).length - 1),
        t = t / e;
      return e * (10 <= t ? 10 : 5 <= t ? 5 : 3 <= t ? 3 : 2 <= t ? 2 : 1);
    }
  });
  var se = Z.extend({
    options: {
      position: "bottomright",
      prefix: '<a href="https://leafletjs.com" title="A JavaScript library for interactive maps"><svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="12" height="8" viewBox="0 0 12 8" class="leaflet-attribution-flag"><path fill="#4C7BE1" d="M0 0h12v4H0z"/><path fill="#FFD500" d="M0 4h12v3H0z"/><path fill="#E0BC00" d="M0 7h12v1H0z"/></svg> Leaflet</a>'
    },
    initialize: function initialize(t) {
      n(this, t), this._attributions = {};
    },
    onAdd: function onAdd(t) {
      (t.attributionControl = this)._container = b("div", "leaflet-control-attribution"), Vt(this._container);
      for (var _e15 in t._layers) t._layers[_e15].getAttribution && this.addAttribution(t._layers[_e15].getAttribution());
      return this._update(), t.on("layeradd", this._addAttribution, this), this._container;
    },
    onRemove: function onRemove(t) {
      t.off("layeradd", this._addAttribution, this);
    },
    _addAttribution: function _addAttribution(t) {
      t.layer.getAttribution && (this.addAttribution(t.layer.getAttribution()), t.layer.once("remove", function () {
        this.removeAttribution(t.layer.getAttribution());
      }, this));
    },
    setPrefix: function setPrefix(t) {
      return this.options.prefix = t, this._update(), this;
    },
    addAttribution: function addAttribution(t) {
      return t && (this._attributions[t] || (this._attributions[t] = 0), this._attributions[t]++, this._update()), this;
    },
    removeAttribution: function removeAttribution(t) {
      return t && this._attributions[t] && (this._attributions[t]--, this._update()), this;
    },
    _update: function _update() {
      if (this._map) {
        var t = [];
        for (var _i9 in this._attributions) this._attributions[_i9] && t.push(_i9);
        var e = [];
        this.options.prefix && e.push(this.options.prefix), t.length && e.push(t.join(", ")), this._container.innerHTML = e.join(' <span aria-hidden="true">|</span> ');
      }
    }
  });
  C.mergeOptions({
    attributionControl: !0
  }), C.addInitHook(function () {
    this.options.attributionControl && new se().addTo(this);
  });
  Z.Layers = ee, Z.Zoom = ie, Z.Scale = oe, Z.Attribution = se, te.layers = function (t, e, i) {
    return new ee(t, e, i);
  }, te.zoom = function (t) {
    return new ie(t);
  }, te.scale = function (t) {
    return new oe(t);
  }, te.attribution = function (t) {
    return new se(t);
  };
  y = Y.extend({
    initialize: function initialize(t) {
      this._map = t;
    },
    enable: function enable() {
      return this._enabled || (this._enabled = !0, this.addHooks()), this;
    },
    disable: function disable() {
      return this._enabled && (this._enabled = !1, this.removeHooks()), this;
    },
    enabled: function enabled() {
      return !!this._enabled;
    }
  });
  y.addTo = function (t, e) {
    return t.addHandler(e, this), this;
  };
  var ne = v.touch ? "touchstart mousedown" : "mousedown",
    ae = e.extend({
      options: {
        clickTolerance: 3
      },
      initialize: function initialize(t, e, i, o) {
        n(this, o), this._element = t, this._dragStartTarget = e || t, this._preventOutline = i;
      },
      enable: function enable() {
        this._enabled || (P(this._dragStartTarget, ne, this._onDown, this), this._enabled = !0);
      },
      disable: function disable() {
        this._enabled && (ae._dragging === this && this.finishDrag(!0), z(this._dragStartTarget, ne, this._onDown, this), this._enabled = !1, this._moved = !1);
      },
      _onDown: function _onDown(t) {
        var e, i;
        this._enabled && (this._moved = !1, this._element.classList.contains("leaflet-zoom-anim") || (t.touches && 1 !== t.touches.length ? ae._dragging === this && this.finishDrag() : ae._dragging || t.shiftKey || 0 !== t.button && !t.touches || ((ae._dragging = this)._preventOutline && At(this._element), St(), Zt(), this._moving) || (this.fire("down"), i = t.touches ? t.touches[0] : t, e = Rt(this._element), this._startPoint = new c(i.clientX, i.clientY), this._startPos = Tt(this._element), this._parentScale = jt(e), i = "mousedown" === t.type, P(document, i ? "mousemove" : "touchmove", this._onMove, this), P(document, i ? "mouseup" : "touchend touchcancel", this._onUp, this))));
      },
      _onMove: function _onMove(t) {
        var e;
        this._enabled && (t.touches && 1 < t.touches.length ? this._moved = !0 : !(e = new c((e = t.touches && 1 === t.touches.length ? t.touches[0] : t).clientX, e.clientY)._subtract(this._startPoint)).x && !e.y || Math.abs(e.x) + Math.abs(e.y) < this.options.clickTolerance || (e.x /= this._parentScale.x, e.y /= this._parentScale.y, M(t), this._moved || (this.fire("dragstart"), this._moved = !0, document.body.classList.add("leaflet-dragging"), this._lastTarget = t.target || t.srcElement, window.SVGElementInstance && this._lastTarget instanceof window.SVGElementInstance && (this._lastTarget = this._lastTarget.correspondingUseElement), this._lastTarget.classList.add("leaflet-drag-target")), this._newPos = this._startPos.add(e), this._moving = !0, this._lastEvent = t, this._updatePosition()));
      },
      _updatePosition: function _updatePosition() {
        var t = {
          originalEvent: this._lastEvent
        };
        this.fire("predrag", t), L(this._element, this._newPos), this.fire("drag", t);
      },
      _onUp: function _onUp() {
        this._enabled && this.finishDrag();
      },
      finishDrag: function finishDrag(t) {
        document.body.classList.remove("leaflet-dragging"), this._lastTarget && (this._lastTarget.classList.remove("leaflet-drag-target"), this._lastTarget = null), z(document, "mousemove touchmove", this._onMove, this), z(document, "mouseup touchend touchcancel", this._onUp, this), Et(), Ot();
        var e = this._moved && this._moving;
        this._moving = !1, ae._dragging = !1, e && this.fire("dragend", {
          noInertia: t,
          distance: this._newPos.distanceTo(this._startPos)
        });
      }
    });
  function re(t, e, i) {
    var o, s, n, a, r, h, l, d, _;
    var u = [1, 4, 2, 8];
    for (s = 0, l = t.length; s < l; s++) t[s]._code = me(t[s], e);
    for (a = 0; a < 4; a++) {
      for (d = u[a], o = [], s = 0, l = t.length, n = l - 1; s < l; n = s++) r = t[s], h = t[n], r._code & d ? h._code & d || ((_ = ce(h, r, d, e, i))._code = me(_, e), o.push(_)) : (h._code & d && ((_ = ce(h, r, d, e, i))._code = me(_, e), o.push(_)), o.push(r));
      t = o;
    }
    return t;
  }
  function he(t, e) {
    var i, o, s, n, a, r, h, l, d;
    if (!t || 0 === t.length) throw new Error("latlngs not passed");
    O(t) || (console.warn("latlngs are not flat! Only the first ring will be used"), t = t[0]);
    var _ = w([0, 0]);
    var u = f(t),
      p = (u.getNorthWest().distanceTo(u.getSouthWest()) * u.getNorthEast().distanceTo(u.getNorthWest()) < 1700 && (_ = le(t)), t.length),
      c = [];
    for (i = 0; i < p; i++) {
      var m = w(t[i]);
      c.push(e.project(w([m.lat - _.lat, m.lng - _.lng])));
    }
    for (r = h = l = 0, i = 0, o = p - 1; i < p; o = i++) s = c[i], n = c[o], a = s.y * n.x - n.y * s.x, h += (s.x + n.x) * a, l += (s.y + n.y) * a, r += 3 * a;
    d = 0 === r ? c[0] : [h / r, l / r];
    u = e.unproject(g(d));
    return w([u.lat + _.lat, u.lng + _.lng]);
  }
  function le(e) {
    var i = 0,
      o = 0,
      s = 0;
    for (var _t28 = 0; _t28 < e.length; _t28++) {
      var n = w(e[_t28]);
      i += n.lat, o += n.lng, s++;
    }
    return w([i / s, o / s]);
  }
  at = {
    __proto__: null,
    centroid: le,
    clipPolygon: re,
    polygonCenter: he
  };
  function de(e, i) {
    if (i && e.length) {
      i = i * i;
      {
        var o = e = function (e, i) {
            var o = [e[0]];
            var s = 0;
            for (var _t29 = 1; _t29 < e.length; _t29++) (function (t, e) {
              var i = e.x - t.x,
                e = e.y - t.y;
              return i * i + e * e;
            })(e[_t29], e[s]) > i && (o.push(e[_t29]), s = _t29);
            s < e.length - 1 && o.push(e[e.length - 1]);
            return o;
          }(e, i),
          s = o.length,
          n = new ("undefined" != typeof Uint8Array ? Uint8Array : Array)(s);
        n[0] = n[s - 1] = 1, function t(e, i, o, s, n) {
          var a = 0,
            r,
            h,
            l;
          for (h = s + 1; h <= n - 1; h++) (l = ge(e[h], e[s], e[n], !0)) > a && (r = h, a = l);
          a > o && (i[r] = 1, t(e, i, o, s, r), t(e, i, o, r, n));
        }(o, n, i, 0, s - 1);
        var _t30;
        var a = [];
        for (_t30 = 0; _t30 < s; _t30++) n[_t30] && a.push(o[_t30]);
        return a;
      }
    }
    return e.slice();
  }
  function _e(t, e, i) {
    return Math.sqrt(ge(t, e, i, !0));
  }
  var ue;
  function pe(t, e, i, o, s) {
    var n = o ? ue : me(t, i),
      a = me(e, i),
      r,
      h,
      l;
    for (ue = a;;) {
      if (!(n | a)) return [t, e];
      if (n & a) return !1;
      l = me(h = ce(t, e, r = n || a, i, s), i), r === n ? (t = h, n = l) : (e = h, a = l);
    }
  }
  function ce(t, e, i, o, s) {
    var n = e.x - t.x,
      e = e.y - t.y,
      a = o.min,
      o = o.max;
    var r, h;
    return 8 & i ? (r = t.x + n * (o.y - t.y) / e, h = o.y) : 4 & i ? (r = t.x + n * (a.y - t.y) / e, h = a.y) : 2 & i ? (r = o.x, h = t.y + e * (o.x - t.x) / n) : 1 & i && (r = a.x, h = t.y + e * (a.x - t.x) / n), new c(r, h, s);
  }
  function me(t, e) {
    var i = 0;
    return t.x < e.min.x ? i |= 1 : t.x > e.max.x && (i |= 2), t.y < e.min.y ? i |= 4 : t.y > e.max.y && (i |= 8), i;
  }
  function ge(t, e, i, o) {
    var s = e.x,
      n = e.y,
      a = i.x - s,
      r = i.y - n,
      h;
    e = a * a + r * r;
    return 0 < e && (1 < (h = ((t.x - s) * a + (t.y - n) * r) / e) ? (s = i.x, n = i.y) : 0 < h && (s += a * h, n += r * h)), a = t.x - s, r = t.y - n, o ? a * a + r * r : new c(s, n);
  }
  function O(t) {
    return !Array.isArray(t[0]) || "object" != _typeof(t[0][0]) && void 0 !== t[0][0];
  }
  function fe(t, e) {
    var i, o, s, n, a, r, h, l;
    if (!t || 0 === t.length) throw new Error("latlngs not passed");
    O(t) || (console.warn("latlngs are not flat! Only the first ring will be used"), t = t[0]);
    var d = w([0, 0]);
    var _ = f(t),
      u = (_.getNorthWest().distanceTo(_.getSouthWest()) * _.getNorthEast().distanceTo(_.getNorthWest()) < 1700 && (d = le(t)), t.length),
      p = [];
    for (i = 0; i < u; i++) {
      var c = w(t[i]);
      p.push(e.project(w([c.lat - d.lat, c.lng - d.lng])));
    }
    for (i = 0, o = 0; i < u - 1; i++) o += p[i].distanceTo(p[i + 1]) / 2;
    if (0 === o) l = p[0];else for (i = 0, n = 0; i < u - 1; i++) if (a = p[i], r = p[i + 1], s = a.distanceTo(r), (n += s) > o) {
      h = (n - o) / s, l = [r.x - h * (r.x - a.x), r.y - h * (r.y - a.y)];
      break;
    }
    _ = e.unproject(g(l));
    return w([_.lat + d.lat, _.lng + d.lng]);
  }
  var rt = {
      __proto__: null,
      _getBitCode: me,
      _getEdgeIntersection: ce,
      _sqClosestPointOnSegment: ge,
      clipSegment: pe,
      closestPointOnSegment: function closestPointOnSegment(t, e, i) {
        return ge(t, e, i);
      },
      isFlat: O,
      pointToSegmentDistance: _e,
      polylineCenter: fe,
      simplify: de
    },
    ye = {
      project: function project(t) {
        return new c(t.lng, t.lat);
      },
      unproject: function unproject(t) {
        return new p(t.y, t.x);
      },
      bounds: new m([-180, -90], [180, 90])
    };
  var ve = {
    R: 6378137,
    R_MINOR: 6356752.314245179,
    bounds: new m([-20037508.34279, -15496570.73972], [20037508.34279, 18764656.23138]),
    project: function project(t) {
      var e = Math.PI / 180,
        i = this.R,
        o = this.R_MINOR / i,
        o = Math.sqrt(1 - o * o),
        s = t.lat * e,
        n = o * Math.sin(s),
        n = Math.tan(Math.PI / 4 - s / 2) / Math.pow((1 - n) / (1 + n), o / 2),
        s = -i * Math.log(Math.max(n, 1e-10));
      return new c(t.lng * e * i, s);
    },
    unproject: function unproject(t) {
      var e = 180 / Math.PI,
        i = this.R,
        o = this.R_MINOR / i,
        s = Math.sqrt(1 - o * o),
        n = Math.exp(-t.y / i);
      var a = Math.PI / 2 - 2 * Math.atan(n);
      for (var _t31 = 0, _e16 = .1, _i10; _t31 < 15 && 1e-7 < Math.abs(_e16); _t31++) _i10 = s * Math.sin(a), _i10 = Math.pow((1 - _i10) / (1 + _i10), s / 2), _e16 = Math.PI / 2 - 2 * Math.atan(n * _i10) - a, a += _e16;
      return new p(a * e, t.x * e / i);
    }
  };
  var xe = {
      __proto__: null,
      LonLat: ye,
      Mercator: ve,
      SphericalMercator: tt
    },
    S = h({}, J, {
      code: "EPSG:3395",
      projection: ve,
      transformation: it(S = .5 / (Math.PI * ve.R), .5, -S, .5)
    });
  var we = h({}, J, {
    code: "EPSG:4326",
    projection: ye,
    transformation: it(1 / 180, 1, -1 / 180, .5)
  });
  ye = h({}, o, {
    projection: ye,
    transformation: it(1, 0, -1, 0),
    scale: function scale(t) {
      return Math.pow(2, t);
    },
    zoom: function zoom(t) {
      return Math.log(t) / Math.LN2;
    },
    distance: function distance(t, e) {
      var i = e.lng - t.lng,
        e = e.lat - t.lat;
      return Math.sqrt(i * i + e * e);
    },
    infinite: !0
  }), o.Earth = J, o.EPSG3395 = S, o.EPSG3857 = s, o.EPSG900913 = ot, o.EPSG4326 = we, o.Simple = ye, S = e.extend({
    options: {
      pane: "overlayPane",
      attribution: null,
      bubblingMouseEvents: !0
    },
    addTo: function addTo(t) {
      return t.addLayer(this), this;
    },
    remove: function remove() {
      return this.removeFrom(this._map || this._mapToAdd);
    },
    removeFrom: function removeFrom(t) {
      return t && t.removeLayer(this), this;
    },
    getPane: function getPane(t) {
      return this._map.getPane(t ? this.options[t] || t : this.options.pane);
    },
    addInteractiveTarget: function addInteractiveTarget(t) {
      return this._map._targets[r(t)] = this;
    },
    removeInteractiveTarget: function removeInteractiveTarget(t) {
      return delete this._map._targets[r(t)], this;
    },
    getAttribution: function getAttribution() {
      return this.options.attribution;
    },
    _layerAdd: function _layerAdd(t) {
      var e = t.target;
      if (e.hasLayer(this)) {
        if (this._map = e, this._zoomAnimated = e._zoomAnimated, this.getEvents) {
          var _i11 = this.getEvents();
          e.on(_i11, this), this.once("remove", function () {
            e.off(_i11, this);
          }, this);
        }
        this.onAdd(e), this.fire("add"), e.fire("layeradd", {
          layer: this
        });
      }
    }
  });
  C.include({
    addLayer: function addLayer(t) {
      var e;
      if (t._layerAdd) return e = r(t), this._layers[e] || ((this._layers[e] = t)._mapToAdd = this, t.beforeAdd && t.beforeAdd(this), this.whenReady(t._layerAdd, t)), this;
      throw new Error("The provided object is not a Layer.");
    },
    removeLayer: function removeLayer(t) {
      var e = r(t);
      return this._layers[e] && (this._loaded && t.onRemove(this), delete this._layers[e], this._loaded && (this.fire("layerremove", {
        layer: t
      }), t.fire("remove")), t._map = t._mapToAdd = null), this;
    },
    hasLayer: function hasLayer(t) {
      return r(t) in this._layers;
    },
    eachLayer: function eachLayer(t, e) {
      for (var _i12 in this._layers) Object.hasOwn(this._layers, _i12) && t.call(e, this._layers[_i12]);
      return this;
    },
    _addLayers: function _addLayers(i) {
      for (var _t32 = 0, _e17 = (i = i ? Array.isArray(i) ? i : [i] : []).length; _t32 < _e17; _t32++) this.addLayer(i[_t32]);
    },
    _addZoomLimit: function _addZoomLimit(t) {
      isNaN(t.options.maxZoom) && isNaN(t.options.minZoom) || (this._zoomBoundLayers[r(t)] = t, this._updateZoomLevels());
    },
    _removeZoomLimit: function _removeZoomLimit(t) {
      t = r(t);
      this._zoomBoundLayers[t] && (delete this._zoomBoundLayers[t], this._updateZoomLevels());
    },
    _updateZoomLevels: function _updateZoomLevels() {
      var t = 1 / 0,
        e = -1 / 0;
      var i,
        o = this._getZoomSpan();
      for (var _s4 in this._zoomBoundLayers) Object.hasOwn(this._zoomBoundLayers, _s4) && (i = this._zoomBoundLayers[_s4].options, t = void 0 === i.minZoom ? t : Math.min(t, i.minZoom), e = void 0 === i.maxZoom ? e : Math.max(e, i.maxZoom));
      this._layersMaxZoom = e === -1 / 0 ? void 0 : e, this._layersMinZoom = t === 1 / 0 ? void 0 : t, o !== this._getZoomSpan() && this.fire("zoomlevelschange"), void 0 === this.options.maxZoom && this._layersMaxZoom && this.getZoom() > this._layersMaxZoom && this.setZoom(this._layersMaxZoom), void 0 === this.options.minZoom && this._layersMinZoom && this.getZoom() < this._layersMinZoom && this.setZoom(this._layersMinZoom);
    }
  });
  var be = S.extend({
    initialize: function initialize(t, e) {
      n(this, e), this._layers = {};
      var i, o;
      if (t) for (i = 0, o = t.length; i < o; i++) this.addLayer(t[i]);
    },
    addLayer: function addLayer(t) {
      var e = this.getLayerId(t);
      return this._layers[e] = t, this._map && this._map.addLayer(t), this;
    },
    removeLayer: function removeLayer(t) {
      t = t in this._layers ? t : this.getLayerId(t);
      return this._map && this._layers[t] && this._map.removeLayer(this._layers[t]), delete this._layers[t], this;
    },
    hasLayer: function hasLayer(t) {
      return ("number" == typeof t ? t : this.getLayerId(t)) in this._layers;
    },
    clearLayers: function clearLayers() {
      return this.eachLayer(this.removeLayer, this);
    },
    invoke: function invoke(t) {
      var i, o;
      for (var _len4 = arguments.length, e = new Array(_len4 > 1 ? _len4 - 1 : 0), _key4 = 1; _key4 < _len4; _key4++) {
        e[_key4 - 1] = arguments[_key4];
      }
      for (i in this._layers) Object.hasOwn(this._layers, i) && (o = this._layers[i])[t] && o[t].apply(o, e);
      return this;
    },
    onAdd: function onAdd(t) {
      this.eachLayer(t.addLayer, t);
    },
    onRemove: function onRemove(t) {
      this.eachLayer(t.removeLayer, t);
    },
    eachLayer: function eachLayer(t, e) {
      for (var _i13 in this._layers) Object.hasOwn(this._layers, _i13) && t.call(e, this._layers[_i13]);
      return this;
    },
    getLayer: function getLayer(t) {
      return this._layers[t];
    },
    getLayers: function getLayers() {
      var t = [];
      return this.eachLayer(t.push, t), t;
    },
    setZIndex: function setZIndex(t) {
      return this.invoke("setZIndex", t);
    },
    getLayerId: function getLayerId(t) {
      return r(t);
    }
  });
  function Le(t, e) {
    return new be(t, e);
  }
  var E = be.extend({
    addLayer: function addLayer(t) {
      return this.hasLayer(t) ? this : (t.addEventParent(this), be.prototype.addLayer.call(this, t), this.fire("layeradd", {
        layer: t
      }));
    },
    removeLayer: function removeLayer(t) {
      return this.hasLayer(t) ? ((t = t in this._layers ? this._layers[t] : t).removeEventParent(this), be.prototype.removeLayer.call(this, t), this.fire("layerremove", {
        layer: t
      })) : this;
    },
    setStyle: function setStyle(t) {
      return this.invoke("setStyle", t);
    },
    bringToFront: function bringToFront() {
      return this.invoke("bringToFront");
    },
    bringToBack: function bringToBack() {
      return this.invoke("bringToBack");
    },
    getBounds: function getBounds() {
      var t,
        e = new u();
      for (var _i14 in this._layers) Object.hasOwn(this._layers, _i14) && (t = this._layers[_i14], e.extend(t.getBounds ? t.getBounds() : t.getLatLng()));
      return e;
    }
  });
  function Pe(t, e) {
    return new E(t, e);
  }
  var Te = Y.extend({
    options: {
      popupAnchor: [0, 0],
      tooltipAnchor: [0, 0],
      crossOrigin: !1
    },
    initialize: function initialize(t) {
      n(this, t);
    },
    createIcon: function createIcon(t) {
      return this._createIcon("icon", t);
    },
    createShadow: function createShadow(t) {
      return this._createIcon("shadow", t);
    },
    _createIcon: function _createIcon(t, e) {
      var i = this._getIconUrl(t);
      if (i) return i = this._createImg(i, e && "IMG" === e.tagName ? e : null), this._setIconStyles(i, t), !this.options.crossOrigin && "" !== this.options.crossOrigin || (i.crossOrigin = !0 === this.options.crossOrigin ? "" : this.options.crossOrigin), i;
      if ("icon" === t) throw new Error("iconUrl not set in Icon options (see the docs).");
      return null;
    },
    _setIconStyles: function _setIconStyles(t, e) {
      var i = this.options;
      var o = i[e + "Size"];
      var s = g(o = "number" == typeof o ? [o, o] : o),
        n = g("shadow" === e && i.shadowAnchor || i.iconAnchor || s && s.divideBy(2, !0));
      t.className = "leaflet-marker-".concat(e, " ") + (i.className || ""), n && (t.style.marginLeft = -n.x + "px", t.style.marginTop = -n.y + "px"), s && (t.style.width = s.x + "px", t.style.height = s.y + "px");
    },
    _createImg: function _createImg(t, e) {
      return (e = e || document.createElement("img")).src = t, e;
    },
    _getIconUrl: function _getIconUrl(t) {
      return v.retina && this.options[t + "RetinaUrl"] || this.options[t + "Url"];
    }
  });
  function ze(t) {
    return new Te(t);
  }
  var Me = Te.extend({
      options: {
        iconUrl: "marker-icon.png",
        iconRetinaUrl: "marker-icon-2x.png",
        shadowUrl: "marker-shadow.png",
        iconSize: [25, 41],
        iconAnchor: [12, 41],
        popupAnchor: [1, -34],
        tooltipAnchor: [16, -28],
        shadowSize: [41, 41]
      },
      _getIconUrl: function _getIconUrl(t) {
        return "string" != typeof Me.imagePath && (Me.imagePath = this._detectIconPath()), (this.options.imagePath || Me.imagePath) + Te.prototype._getIconUrl.call(this, t);
      },
      _stripUrl: function _stripUrl(t) {
        function e(t, e, i) {
          return (e = e.exec(t)) && e[i];
        }
        return (t = e(t, /^url\((['"])?(.+)\1\)$/, 2)) && e(t, /^(.*)marker-icon\.png$/, 1);
      },
      _detectIconPath: function _detectIconPath() {
        var t = b("div", "leaflet-default-icon-path", document.body),
          e = this._stripUrl(getComputedStyle(t).backgroundImage);
        return document.body.removeChild(t), e || ((t = document.querySelector('link[href$="leaflet.css"]')) ? t.href.substring(0, t.href.length - "leaflet.css".length - 1) : "");
      }
    }),
    Ce = y.extend({
      initialize: function initialize(t) {
        this._marker = t;
      },
      addHooks: function addHooks() {
        var t = this._marker._icon;
        this._draggable || (this._draggable = new ae(t, t, !0)), this._draggable.on({
          dragstart: this._onDragStart,
          predrag: this._onPreDrag,
          drag: this._onDrag,
          dragend: this._onDragEnd
        }, this).enable(), t.classList.add("leaflet-marker-draggable");
      },
      removeHooks: function removeHooks() {
        this._draggable.off({
          dragstart: this._onDragStart,
          predrag: this._onPreDrag,
          drag: this._onDrag,
          dragend: this._onDragEnd
        }, this).disable(), this._marker._icon && this._marker._icon.classList.remove("leaflet-marker-draggable");
      },
      moved: function moved() {
        return this._draggable && this._draggable._moved;
      },
      _adjustPan: function _adjustPan(t) {
        var e = this._marker,
          i = e._map,
          o = this._marker.options.autoPanSpeed,
          s = this._marker.options.autoPanPadding,
          n = Tt(e._icon),
          a = i.getPixelBounds(),
          r = i.getPixelOrigin(),
          r = _(a.min._subtract(r).add(s), a.max._subtract(r).subtract(s));
        r.contains(n) || (s = g((Math.max(r.max.x, n.x) - r.max.x) / (a.max.x - r.max.x) - (Math.min(r.min.x, n.x) - r.min.x) / (a.min.x - r.min.x), (Math.max(r.max.y, n.y) - r.max.y) / (a.max.y - r.max.y) - (Math.min(r.min.y, n.y) - r.min.y) / (a.min.y - r.min.y)).multiplyBy(o), i.panBy(s, {
          animate: !1
        }), this._draggable._newPos._add(s), this._draggable._startPos._add(s), L(e._icon, this._draggable._newPos), this._onDrag(t), this._panRequest = x(this._adjustPan.bind(this, t)));
      },
      _onDragStart: function _onDragStart() {
        this._oldLatLng = this._marker.getLatLng(), this._marker.closePopup && this._marker.closePopup(), this._marker.fire("movestart").fire("dragstart");
      },
      _onPreDrag: function _onPreDrag(t) {
        this._marker.options.autoPan && (d(this._panRequest), this._panRequest = x(this._adjustPan.bind(this, t)));
      },
      _onDrag: function _onDrag(t) {
        var e = this._marker,
          i = e._shadow,
          o = Tt(e._icon),
          s = e._map.layerPointToLatLng(o);
        i && L(i, o), e._latlng = s, t.latlng = s, t.oldLatLng = this._oldLatLng, e.fire("move", t).fire("drag", t);
      },
      _onDragEnd: function _onDragEnd(t) {
        d(this._panRequest), delete this._oldLatLng, this._marker.fire("moveend").fire("dragend", t);
      }
    }),
    Ze = S.extend({
      options: {
        icon: new Me(),
        interactive: !0,
        keyboard: !0,
        title: "",
        alt: "Marker",
        zIndexOffset: 0,
        opacity: 1,
        riseOnHover: !1,
        riseOffset: 250,
        pane: "markerPane",
        shadowPane: "shadowPane",
        bubblingMouseEvents: !1,
        autoPanOnFocus: !0,
        draggable: !1,
        autoPan: !1,
        autoPanPadding: [50, 50],
        autoPanSpeed: 10
      },
      initialize: function initialize(t, e) {
        n(this, e), this._latlng = w(t);
      },
      onAdd: function onAdd(t) {
        this._zoomAnimated = this._zoomAnimated && t.options.markerZoomAnimation, this._zoomAnimated && t.on("zoomanim", this._animateZoom, this), this._initIcon(), this.update();
      },
      onRemove: function onRemove(t) {
        this.dragging && this.dragging.enabled() && (this.options.draggable = !0, this.dragging.removeHooks()), delete this.dragging, this._zoomAnimated && t.off("zoomanim", this._animateZoom, this), this._removeIcon(), this._removeShadow();
      },
      getEvents: function getEvents() {
        return {
          zoom: this.update,
          viewreset: this.update
        };
      },
      getLatLng: function getLatLng() {
        return this._latlng;
      },
      setLatLng: function setLatLng(t) {
        var e = this._latlng;
        return this._latlng = w(t), this.update(), this.fire("move", {
          oldLatLng: e,
          latlng: this._latlng
        });
      },
      setZIndexOffset: function setZIndexOffset(t) {
        return this.options.zIndexOffset = t, this.update();
      },
      getIcon: function getIcon() {
        return this.options.icon;
      },
      setIcon: function setIcon(t) {
        return this.options.icon = t, this._map && (this._initIcon(), this.update()), this._popup && this.bindPopup(this._popup, this._popup.options), this;
      },
      getElement: function getElement() {
        return this._icon;
      },
      update: function update() {
        var t;
        return this._icon && this._map && (t = this._map.latLngToLayerPoint(this._latlng).round(), this._setPos(t)), this;
      },
      _initIcon: function _initIcon() {
        var t = this.options,
          e = "leaflet-zoom-" + (this._zoomAnimated ? "animated" : "hide"),
          i = t.icon.createIcon(this._icon);
        var o = !1;
        i !== this._icon && (this._icon && this._removeIcon(), o = !0, t.title && (i.title = t.title), "IMG" === i.tagName) && (i.alt = t.alt || ""), i.classList.add(e), t.keyboard && (i.tabIndex = "0", i.setAttribute("role", "button")), this._icon = i, t.riseOnHover && this.on({
          mouseover: this._bringToFront,
          mouseout: this._resetZIndex
        }), this.options.autoPanOnFocus && P(i, "focus", this._panOnFocus, this);
        i = t.icon.createShadow(this._shadow);
        var s = !1;
        i !== this._shadow && (this._removeShadow(), s = !0), i && (i.classList.add(e), i.alt = ""), this._shadow = i, t.opacity < 1 && this._updateOpacity(), o && this.getPane().appendChild(this._icon), this._initInteraction(), i && s && this.getPane(t.shadowPane).appendChild(this._shadow);
      },
      _removeIcon: function _removeIcon() {
        this.options.riseOnHover && this.off({
          mouseover: this._bringToFront,
          mouseout: this._resetZIndex
        }), this.options.autoPanOnFocus && z(this._icon, "focus", this._panOnFocus, this), this._icon.remove(), this.removeInteractiveTarget(this._icon), this._icon = null;
      },
      _removeShadow: function _removeShadow() {
        this._shadow && this._shadow.remove(), this._shadow = null;
      },
      _setPos: function _setPos(t) {
        this._icon && L(this._icon, t), this._shadow && L(this._shadow, t), this._zIndex = t.y + this.options.zIndexOffset, this._resetZIndex();
      },
      _updateZIndex: function _updateZIndex(t) {
        this._icon && (this._icon.style.zIndex = this._zIndex + t);
      },
      _animateZoom: function _animateZoom(t) {
        t = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center).round();
        this._setPos(t);
      },
      _initInteraction: function _initInteraction() {
        if (this.options.interactive && (this._icon.classList.add("leaflet-interactive"), this.addInteractiveTarget(this._icon), Ce)) {
          var _t33 = this.options.draggable;
          this.dragging && (_t33 = this.dragging.enabled(), this.dragging.disable()), this.dragging = new Ce(this), _t33 && this.dragging.enable();
        }
      },
      setOpacity: function setOpacity(t) {
        return this.options.opacity = t, this._map && this._updateOpacity(), this;
      },
      _updateOpacity: function _updateOpacity() {
        var t = this.options.opacity;
        this._icon && (this._icon.style.opacity = t), this._shadow && (this._shadow.style.opacity = t);
      },
      _bringToFront: function _bringToFront() {
        this._updateZIndex(this.options.riseOffset);
      },
      _resetZIndex: function _resetZIndex() {
        this._updateZIndex(0);
      },
      _panOnFocus: function _panOnFocus() {
        var t,
          e,
          i = this._map;
        i && (t = (e = this.options.icon.options).iconSize ? g(e.iconSize) : g(0, 0), e = e.iconAnchor ? g(e.iconAnchor) : g(0, 0), i.panInside(this._latlng, {
          paddingTopLeft: e,
          paddingBottomRight: t.subtract(e)
        }));
      },
      _getPopupAnchor: function _getPopupAnchor() {
        return this.options.icon.options.popupAnchor;
      },
      _getTooltipAnchor: function _getTooltipAnchor() {
        return this.options.icon.options.tooltipAnchor;
      }
    });
  function Oe(t, e) {
    return new Ze(t, e);
  }
  var Se = S.extend({
      options: {
        stroke: !0,
        color: "#3388ff",
        weight: 3,
        opacity: 1,
        lineCap: "round",
        lineJoin: "round",
        dashArray: null,
        dashOffset: null,
        fill: !1,
        fillColor: null,
        fillOpacity: .2,
        fillRule: "evenodd",
        interactive: !0,
        bubblingMouseEvents: !0
      },
      beforeAdd: function beforeAdd(t) {
        this._renderer = t.getRenderer(this);
      },
      onAdd: function onAdd() {
        this._renderer._initPath(this), this._reset(), this._renderer._addPath(this);
      },
      onRemove: function onRemove() {
        this._renderer._removePath(this);
      },
      redraw: function redraw() {
        return this._map && this._renderer._updatePath(this), this;
      },
      setStyle: function setStyle(t) {
        return n(this, t), this._renderer && (this._renderer._updateStyle(this), this.options.stroke) && t && Object.hasOwn(t, "weight") && this._updateBounds(), this;
      },
      bringToFront: function bringToFront() {
        return this._renderer && this._renderer._bringToFront(this), this;
      },
      bringToBack: function bringToBack() {
        return this._renderer && this._renderer._bringToBack(this), this;
      },
      getElement: function getElement() {
        return this._path;
      },
      _reset: function _reset() {
        this._project(), this._update();
      },
      _clickTolerance: function _clickTolerance() {
        return (this.options.stroke ? this.options.weight / 2 : 0) + (this._renderer.options.tolerance || 0);
      }
    }),
    Ee = Se.extend({
      options: {
        fill: !0,
        radius: 10
      },
      initialize: function initialize(t, e) {
        n(this, e), this._latlng = w(t), this._radius = this.options.radius;
      },
      setLatLng: function setLatLng(t) {
        var e = this._latlng;
        return this._latlng = w(t), this.redraw(), this.fire("move", {
          oldLatLng: e,
          latlng: this._latlng
        });
      },
      getLatLng: function getLatLng() {
        return this._latlng;
      },
      setRadius: function setRadius(t) {
        return this.options.radius = this._radius = t, this.redraw();
      },
      getRadius: function getRadius() {
        return this._radius;
      },
      setStyle: function setStyle(t) {
        var e = t && t.radius || this._radius;
        return Se.prototype.setStyle.call(this, t), this.setRadius(e), this;
      },
      _project: function _project() {
        this._point = this._map.latLngToLayerPoint(this._latlng), this._updateBounds();
      },
      _updateBounds: function _updateBounds() {
        var t = this._radius,
          e = this._radiusY || t,
          i = this._clickTolerance(),
          t = [t + i, e + i];
        this._pxBounds = new m(this._point.subtract(t), this._point.add(t));
      },
      _update: function _update() {
        this._map && this._updatePath();
      },
      _updatePath: function _updatePath() {
        this._renderer._updateCircle(this);
      },
      _empty: function _empty() {
        return this._radius && !this._renderer._bounds.intersects(this._pxBounds);
      },
      _containsPoint: function _containsPoint(t) {
        return t.distanceTo(this._point) <= this._radius + this._clickTolerance();
      }
    });
  function ke(t, e) {
    return new Ee(t, e);
  }
  var Be = Ee.extend({
    initialize: function initialize(t, e, i) {
      if (n(this, e = "number" == typeof e ? h({}, i, {
        radius: e
      }) : e), this._latlng = w(t), isNaN(this.options.radius)) throw new Error("Circle radius cannot be NaN");
      this._mRadius = this.options.radius;
    },
    setRadius: function setRadius(t) {
      return this._mRadius = t, this.redraw();
    },
    getRadius: function getRadius() {
      return this._mRadius;
    },
    getBounds: function getBounds() {
      var t = [this._radius, this._radiusY || this._radius];
      return new u(this._map.layerPointToLatLng(this._point.subtract(t)), this._map.layerPointToLatLng(this._point.add(t)));
    },
    setStyle: Se.prototype.setStyle,
    _project: function _project() {
      var e = this._latlng.lng,
        i = this._latlng.lat,
        o = this._map,
        t = o.options.crs;
      if (t.distance === J.distance) {
        var s = Math.PI / 180,
          n = this._mRadius / J.R / s,
          a = o.project([i + n, e]),
          r = o.project([i - n, e]),
          r = a.add(r).divideBy(2),
          h = o.unproject(r).lat;
        var _t34 = Math.acos((Math.cos(n * s) - Math.sin(i * s) * Math.sin(h * s)) / (Math.cos(i * s) * Math.cos(h * s))) / s;
        !isNaN(_t34) && 0 !== _t34 || (_t34 = n / Math.cos(Math.PI / 180 * i)), this._point = r.subtract(o.getPixelOrigin()), this._radius = isNaN(_t34) ? 0 : r.x - o.project([h, e - _t34]).x, this._radiusY = r.y - a.y;
      } else {
        s = t.unproject(t.project(this._latlng).subtract([this._mRadius, 0]));
        this._point = o.latLngToLayerPoint(this._latlng), this._radius = this._point.x - o.latLngToLayerPoint(s).x;
      }
      this._updateBounds();
    }
  });
  function Ae(t, e, i) {
    return new Be(t, e, i);
  }
  var Ie = Se.extend({
    options: {
      smoothFactor: 1,
      noClip: !1
    },
    initialize: function initialize(t, e) {
      n(this, e), this._setLatLngs(t);
    },
    getLatLngs: function getLatLngs() {
      return this._latlngs;
    },
    setLatLngs: function setLatLngs(t) {
      return this._setLatLngs(t), this.redraw();
    },
    isEmpty: function isEmpty() {
      return !this._latlngs.length;
    },
    closestLayerPoint: function closestLayerPoint(i) {
      var o = 1 / 0,
        s = null,
        n,
        a;
      var r = ge;
      for (var _t35 = 0, _e18 = this._parts.length; _t35 < _e18; _t35++) {
        var h = this._parts[_t35];
        for (var _t36 = 1, _e19 = h.length; _t36 < _e19; _t36++) {
          var l = r(i, n = h[_t36 - 1], a = h[_t36], !0);
          l < o && (o = l, s = r(i, n, a));
        }
      }
      return s && (s.distance = Math.sqrt(o)), s;
    },
    getCenter: function getCenter() {
      if (this._map) return fe(this._defaultShape(), this._map.options.crs);
      throw new Error("Must add layer to map before using getCenter()");
    },
    getBounds: function getBounds() {
      return this._bounds;
    },
    addLatLng: function addLatLng(t, e) {
      return e = e || this._defaultShape(), t = w(t), e.push(t), this._bounds.extend(t), this.redraw();
    },
    _setLatLngs: function _setLatLngs(t) {
      this._bounds = new u(), this._latlngs = this._convertLatLngs(t);
    },
    _defaultShape: function _defaultShape() {
      return O(this._latlngs) ? this._latlngs : this._latlngs[0];
    },
    _convertLatLngs: function _convertLatLngs(i) {
      var o = [],
        s = O(i);
      for (var _t37 = 0, _e20 = i.length; _t37 < _e20; _t37++) s ? (o[_t37] = w(i[_t37]), this._bounds.extend(o[_t37])) : o[_t37] = this._convertLatLngs(i[_t37]);
      return o;
    },
    _project: function _project() {
      var t = new m();
      this._rings = [], this._projectLatlngs(this._latlngs, this._rings, t), this._bounds.isValid() && t.isValid() && (this._rawPxBounds = t, this._updateBounds());
    },
    _updateBounds: function _updateBounds() {
      var t = this._clickTolerance(),
        t = new c(t, t);
      this._rawPxBounds && (this._pxBounds = new m([this._rawPxBounds.min.subtract(t), this._rawPxBounds.max.add(t)]));
    },
    _projectLatlngs: function _projectLatlngs(t, e, i) {
      var o = t[0] instanceof p,
        s = t.length;
      var n, a;
      if (o) {
        for (a = [], n = 0; n < s; n++) a[n] = this._map.latLngToLayerPoint(t[n]), i.extend(a[n]);
        e.push(a);
      } else for (n = 0; n < s; n++) this._projectLatlngs(t[n], e, i);
    },
    _clipPoints: function _clipPoints() {
      var r = this._renderer._bounds;
      if (this._parts = [], this._pxBounds && this._pxBounds.intersects(r)) if (this.options.noClip) this._parts = this._rings;else {
        var h = this._parts;
        var _t38, _e21, _i15, _o5, _s5, _n2, _a4;
        for (_t38 = 0, _i15 = 0, _o5 = this._rings.length; _t38 < _o5; _t38++) for (_a4 = this._rings[_t38], _e21 = 0, _s5 = _a4.length; _e21 < _s5 - 1; _e21++) (_n2 = pe(_a4[_e21], _a4[_e21 + 1], r, _e21, !0)) && (h[_i15] = h[_i15] || [], h[_i15].push(_n2[0]), _n2[1] === _a4[_e21 + 1] && _e21 !== _s5 - 2 || (h[_i15].push(_n2[1]), _i15++));
      }
    },
    _simplifyPoints: function _simplifyPoints() {
      var i = this._parts,
        o = this.options.smoothFactor;
      for (var _t39 = 0, _e22 = i.length; _t39 < _e22; _t39++) i[_t39] = de(i[_t39], o);
    },
    _update: function _update() {
      this._map && (this._clipPoints(), this._simplifyPoints(), this._updatePath());
    },
    _updatePath: function _updatePath() {
      this._renderer._updatePoly(this);
    },
    _containsPoint: function _containsPoint(t, e) {
      var i, o, s, n, a, r;
      var h = this._clickTolerance();
      if (this._pxBounds && this._pxBounds.contains(t)) for (i = 0, n = this._parts.length; i < n; i++) for (r = this._parts[i], o = 0, a = r.length, s = a - 1; o < a; s = o++) if ((e || 0 !== o) && _e(t, r[s], r[o]) <= h) return !0;
      return !1;
    }
  });
  function Re(t, e) {
    return new Ie(t, e);
  }
  var je = Ie.extend({
    options: {
      fill: !0
    },
    isEmpty: function isEmpty() {
      return !this._latlngs.length || !this._latlngs[0].length;
    },
    getCenter: function getCenter() {
      if (this._map) return he(this._defaultShape(), this._map.options.crs);
      throw new Error("Must add layer to map before using getCenter()");
    },
    _convertLatLngs: function _convertLatLngs(t) {
      var t = Ie.prototype._convertLatLngs.call(this, t),
        e = t.length;
      return 2 <= e && t[0] instanceof p && t[0].equals(t[e - 1]) && t.pop(), t;
    },
    _setLatLngs: function _setLatLngs(t) {
      Ie.prototype._setLatLngs.call(this, t), O(this._latlngs) && (this._latlngs = [this._latlngs]);
    },
    _defaultShape: function _defaultShape() {
      return (O(this._latlngs[0]) ? this._latlngs : this._latlngs[0])[0];
    },
    _clipPoints: function _clipPoints() {
      var o = this._renderer._bounds;
      var t = this.options.weight,
        t = new c(t, t);
      if (o = new m(o.min.subtract(t), o.max.add(t)), this._parts = [], this._pxBounds && this._pxBounds.intersects(o)) if (this.options.noClip) this._parts = this._rings;else for (var _t40 = 0, _e23 = this._rings.length, _i16; _t40 < _e23; _t40++) (_i16 = re(this._rings[_t40], o, !0)).length && this._parts.push(_i16);
    },
    _updatePath: function _updatePath() {
      this._renderer._updatePoly(this, !0);
    },
    _containsPoint: function _containsPoint(t) {
      var e = !1,
        i,
        o,
        s,
        n,
        a,
        r,
        h,
        l;
      if (!this._pxBounds || !this._pxBounds.contains(t)) return !1;
      for (n = 0, h = this._parts.length; n < h; n++) for (i = this._parts[n], a = 0, l = i.length, r = l - 1; a < l; r = a++) o = i[a], s = i[r], o.y > t.y != s.y > t.y && t.x < (s.x - o.x) * (t.y - o.y) / (s.y - o.y) + o.x && (e = !e);
      return e || Ie.prototype._containsPoint.call(this, t, !0);
    }
  });
  function De(t, e) {
    return new je(t, e);
  }
  var k = E.extend({
    initialize: function initialize(t, e) {
      n(this, e), this._layers = {}, t && this.addData(t);
    },
    addData: function addData(t) {
      var e = Array.isArray(t) ? t : t.features;
      var i, o, s;
      if (e) {
        for (i = 0, o = e.length; i < o; i++) ((s = e[i]).geometries || s.geometry || s.features || s.coordinates) && this.addData(s);
        return this;
      }
      var n,
        a = this.options;
      return (!a.filter || a.filter(t)) && (n = Ne(t, a)) ? (n.feature = qe(t), n.defaultOptions = n.options, this.resetStyle(n), a.onEachFeature && a.onEachFeature(t, n), this.addLayer(n)) : this;
    },
    resetStyle: function resetStyle(t) {
      return void 0 === t ? this.eachLayer(this.resetStyle, this) : (t.options = h({}, t.defaultOptions), this._setLayerStyle(t, this.options.style), this);
    },
    setStyle: function setStyle(e) {
      return this.eachLayer(function (t) {
        this._setLayerStyle(t, e);
      }, this);
    },
    _setLayerStyle: function _setLayerStyle(t, e) {
      t.setStyle && ("function" == typeof e && (e = e(t.feature)), t.setStyle(e));
    }
  });
  function Ne(t, e) {
    var i = "Feature" === t.type ? t.geometry : t,
      o = i ? i.coordinates : null,
      s = [],
      n = e && e.pointToLayer,
      a = e && e.coordsToLatLng || Fe;
    var r, h, l, d;
    if (!o && !i) return null;
    switch (i.type) {
      case "Point":
        return We(n, t, r = a(o), e);
      case "MultiPoint":
        for (l = 0, d = o.length; l < d; l++) r = a(o[l]), s.push(We(n, t, r, e));
        return new E(s);
      case "LineString":
      case "MultiLineString":
        return h = He(o, "LineString" === i.type ? 0 : 1, a), new Ie(h, e);
      case "Polygon":
      case "MultiPolygon":
        return h = He(o, "Polygon" === i.type ? 1 : 2, a), new je(h, e);
      case "GeometryCollection":
        for (l = 0, d = i.geometries.length; l < d; l++) {
          var _ = Ne({
            geometry: i.geometries[l],
            type: "Feature",
            properties: t.properties
          }, e);
          _ && s.push(_);
        }
        return new E(s);
      case "FeatureCollection":
        for (l = 0, d = i.features.length; l < d; l++) {
          var u = Ne(i.features[l], e);
          u && s.push(u);
        }
        return new E(s);
      default:
        throw new Error("Invalid GeoJSON object.");
    }
  }
  function We(t, e, i, o) {
    return t ? t(e, i) : new Ze(i, o && o.markersInheritOptions && o);
  }
  function Fe(t) {
    return new p(t[1], t[0], t[2]);
  }
  function He(o, s, n) {
    var a = [];
    for (var _t41 = 0, _e24 = o.length, _i17; _t41 < _e24; _t41++) _i17 = s ? He(o[_t41], s - 1, n) : (n || Fe)(o[_t41]), a.push(_i17);
    return a;
  }
  function Ue(t, e) {
    return void 0 !== (t = w(t)).alt ? [i(t.lng, e), i(t.lat, e), i(t.alt, e)] : [i(t.lng, e), i(t.lat, e)];
  }
  function Ve(i, o, s, n) {
    var a = [];
    for (var _t42 = 0, _e25 = i.length; _t42 < _e25; _t42++) a.push(o ? Ve(i[_t42], O(i[_t42]) ? 0 : o - 1, s, n) : Ue(i[_t42], n));
    return !o && s && 0 < a.length && a.push(a[0].slice()), a;
  }
  function Ge(t, e) {
    return t.feature ? h({}, t.feature, {
      geometry: e
    }) : qe(e);
  }
  function qe(t) {
    return "Feature" === t.type || "FeatureCollection" === t.type ? t : {
      type: "Feature",
      properties: {},
      geometry: t
    };
  }
  s = {
    toGeoJSON: function toGeoJSON(t) {
      return Ge(this, {
        type: "Point",
        coordinates: Ue(this.getLatLng(), t)
      });
    }
  };
  function Ke(t, e) {
    return new k(t, e);
  }
  Ze.include(s), Be.include(s), Ee.include(s), Ie.include({
    toGeoJSON: function toGeoJSON(t) {
      var e = !O(this._latlngs);
      return Ge(this, {
        type: "".concat(e ? "Multi" : "", "LineString"),
        coordinates: Ve(this._latlngs, e ? 1 : 0, !1, t)
      });
    }
  }), je.include({
    toGeoJSON: function toGeoJSON(t) {
      var e = !O(this._latlngs),
        i = e && !O(this._latlngs[0]);
      var o = Ve(this._latlngs, i ? 2 : e ? 1 : 0, !0, t);
      return Ge(this, {
        type: "".concat(i ? "Multi" : "", "Polygon"),
        coordinates: o = e ? o : [o]
      });
    }
  }), be.include({
    toMultiPoint: function toMultiPoint(e) {
      var i = [];
      return this.eachLayer(function (t) {
        i.push(t.toGeoJSON(e).geometry.coordinates);
      }), Ge(this, {
        type: "MultiPoint",
        coordinates: i
      });
    },
    toGeoJSON: function toGeoJSON(e) {
      var t = this.feature && this.feature.geometry && this.feature.geometry.type;
      if ("MultiPoint" === t) return this.toMultiPoint(e);
      var i = "GeometryCollection" === t,
        o = [];
      return this.eachLayer(function (t) {
        t.toGeoJSON && (t = t.toGeoJSON(e), i ? o.push(t.geometry) : "FeatureCollection" === (t = qe(t)).type ? o.push.apply(o, t.features) : o.push(t));
      }), i ? Ge(this, {
        geometries: o,
        type: "GeometryCollection"
      }) : {
        type: "FeatureCollection",
        features: o
      };
    }
  });
  ot = Ke;
  var $e = S.extend({
      options: {
        padding: .1,
        continuous: !1
      },
      initialize: function initialize(t) {
        n(this, t);
      },
      onAdd: function onAdd() {
        this._container || (this._initContainer(), this._container.classList.add("leaflet-zoom-animated")), this.getPane().appendChild(this._container), this._resizeContainer(), this._onMoveEnd();
      },
      onRemove: function onRemove() {
        this._destroyContainer();
      },
      getEvents: function getEvents() {
        var t = {
          viewreset: this._reset,
          zoom: this._onZoom,
          moveend: this._onMoveEnd,
          zoomend: this._onZoomEnd,
          resize: this._resizeContainer
        };
        return this._zoomAnimated && (t.zoomanim = this._onAnimZoom), this.options.continuous && (t.move = this._onMoveEnd), t;
      },
      _onAnimZoom: function _onAnimZoom(t) {
        this._updateTransform(t.center, t.zoom);
      },
      _onZoom: function _onZoom() {
        this._updateTransform(this._map.getCenter(), this._map.getZoom());
      },
      _updateTransform: function _updateTransform(t, e) {
        var i = this._map.getZoomScale(e, this._zoom),
          o = this._map.getSize().multiplyBy(.5 + this.options.padding),
          s = this._map.project(this._center, e),
          o = o.multiplyBy(-i).add(s).subtract(this._map._getNewPixelOrigin(t, e));
        Lt(this._container, o, i);
      },
      _onMoveEnd: function _onMoveEnd(t) {
        var e = this.options.padding,
          i = this._map.getSize(),
          o = this._map.containerPointToLayerPoint(i.multiplyBy(-e)).round();
        this._bounds = new m(o, o.add(i.multiplyBy(1 + 2 * e)).round()), this._center = this._map.getCenter(), this._zoom = this._map.getZoom(), this._updateTransform(this._center, this._zoom), this._onSettled(t);
      },
      _reset: function _reset() {
        this._onSettled(), this._updateTransform(this._center, this._zoom), this._onViewReset();
      },
      _initContainer: function _initContainer() {
        this._container = b("div");
      },
      _destroyContainer: function _destroyContainer() {
        z(this._container), this._container.remove(), delete this._container;
      },
      _resizeContainer: function _resizeContainer() {
        var t = this.options.padding,
          t = this._map.getSize().multiplyBy(1 + 2 * t).round();
        return this._container.style.width = t.x + "px", this._container.style.height = t.y + "px", t;
      },
      _onZoomEnd: a,
      _onViewReset: a,
      _onSettled: a
    }),
    Ye = S.extend({
      options: {
        opacity: 1,
        alt: "",
        interactive: !1,
        crossOrigin: !1,
        errorOverlayUrl: "",
        zIndex: 1,
        className: "",
        decoding: "auto"
      },
      initialize: function initialize(t, e, i) {
        this._url = t, this._bounds = f(e), n(this, i);
      },
      onAdd: function onAdd() {
        this._image || (this._initImage(), this.options.opacity < 1 && this._updateOpacity()), this.options.interactive && (this._image.classList.add("leaflet-interactive"), this.addInteractiveTarget(this._image)), this.getPane().appendChild(this._image), this._reset();
      },
      onRemove: function onRemove() {
        this._image.remove(), this.options.interactive && this.removeInteractiveTarget(this._image);
      },
      setOpacity: function setOpacity(t) {
        return this.options.opacity = t, this._image && this._updateOpacity(), this;
      },
      setStyle: function setStyle(t) {
        return t.opacity && this.setOpacity(t.opacity), this;
      },
      bringToFront: function bringToFront() {
        return this._map && wt(this._image), this;
      },
      bringToBack: function bringToBack() {
        return this._map && bt(this._image), this;
      },
      setUrl: function setUrl(t) {
        return this._url = t, this._image && (this._image.src = t), this;
      },
      setBounds: function setBounds(t) {
        return this._bounds = f(t), this._map && this._reset(), this;
      },
      getEvents: function getEvents() {
        var t = {
          zoom: this._reset,
          viewreset: this._reset
        };
        return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
      },
      setZIndex: function setZIndex(t) {
        return this.options.zIndex = t, this._updateZIndex(), this;
      },
      getBounds: function getBounds() {
        return this._bounds;
      },
      getElement: function getElement() {
        return this._image;
      },
      _initImage: function _initImage() {
        var _e$classList;
        var t = "IMG" === this._url.tagName,
          e = this._image = t ? this._url : b("img");
        e.classList.add("leaflet-image-layer"), this._zoomAnimated && e.classList.add("leaflet-zoom-animated"), this.options.className && (_e$classList = e.classList).add.apply(_e$classList, _toConsumableArray(l(this.options.className))), e.onselectstart = a, e.onmousemove = a, e.onload = this.fire.bind(this, "load"), e.onerror = this._overlayOnError.bind(this), !this.options.crossOrigin && "" !== this.options.crossOrigin || (e.crossOrigin = !0 === this.options.crossOrigin ? "" : this.options.crossOrigin), e.decoding = this.options.decoding, this.options.zIndex && this._updateZIndex(), t ? this._url = e.src : (e.src = this._url, e.alt = this.options.alt);
      },
      _animateZoom: function _animateZoom(t) {
        var e = this._map.getZoomScale(t.zoom),
          t = this._map._latLngBoundsToNewLayerBounds(this._bounds, t.zoom, t.center).min;
        Lt(this._image, t, e);
      },
      _reset: function _reset() {
        var t = this._image,
          e = new m(this._map.latLngToLayerPoint(this._bounds.getNorthWest()), this._map.latLngToLayerPoint(this._bounds.getSouthEast())),
          i = e.getSize();
        L(t, e.min), t.style.width = i.x + "px", t.style.height = i.y + "px";
      },
      _updateOpacity: function _updateOpacity() {
        this._image.style.opacity = this.options.opacity;
      },
      _updateZIndex: function _updateZIndex() {
        this._image && void 0 !== this.options.zIndex && null !== this.options.zIndex && (this._image.style.zIndex = this.options.zIndex);
      },
      _overlayOnError: function _overlayOnError() {
        this.fire("error");
        var t = this.options.errorOverlayUrl;
        t && this._url !== t && (this._url = t, this._image.src = t);
      },
      getCenter: function getCenter() {
        return this._bounds.getCenter();
      }
    });
  function Xe(t, e, i) {
    return new Ye(t, e, i);
  }
  var Je = Ye.extend({
    options: {
      autoplay: !0,
      loop: !0,
      keepAspectRatio: !0,
      muted: !1,
      playsInline: !0
    },
    _initImage: function _initImage() {
      var _e$classList2;
      var t = "VIDEO" === this._url.tagName,
        e = this._image = t ? this._url : b("video");
      if (e.classList.add("leaflet-image-layer"), this._zoomAnimated && e.classList.add("leaflet-zoom-animated"), this.options.className && (_e$classList2 = e.classList).add.apply(_e$classList2, _toConsumableArray(l(this.options.className))), e.onselectstart = a, e.onmousemove = a, e.onloadeddata = this.fire.bind(this, "load"), t) {
        var i = e.getElementsByTagName("source"),
          o = [];
        for (var _t43 = 0; _t43 < i.length; _t43++) o.push(i[_t43].src);
        this._url = 0 < i.length ? o : [e.src];
      } else {
        Array.isArray(this._url) || (this._url = [this._url]), !this.options.keepAspectRatio && Object.hasOwn(e.style, "objectFit") && (e.style.objectFit = "fill"), e.autoplay = !!this.options.autoplay, e.loop = !!this.options.loop, e.muted = !!this.options.muted, e.playsInline = !!this.options.playsInline;
        for (var _t44 = 0; _t44 < this._url.length; _t44++) {
          var s = b("source");
          s.src = this._url[_t44], e.appendChild(s);
        }
      }
    }
  });
  function Qe(t, e, i) {
    return new Je(t, e, i);
  }
  var ti = Ye.extend({
    _initImage: function _initImage() {
      var _t$classList2;
      var t = this._image = this._url;
      t.classList.add("leaflet-image-layer"), this._zoomAnimated && t.classList.add("leaflet-zoom-animated"), this.options.className && (_t$classList2 = t.classList).add.apply(_t$classList2, _toConsumableArray(l(this.options.className))), t.onselectstart = a, t.onmousemove = a;
    }
  });
  function ei(t, e, i) {
    return new ti(t, e, i);
  }
  var B = S.extend({
      options: {
        interactive: !1,
        offset: [0, 0],
        className: "",
        pane: void 0,
        content: ""
      },
      initialize: function initialize(t, e) {
        t && (t instanceof p || Array.isArray(t)) ? (this._latlng = w(t), n(this, e)) : (n(this, t), this._source = e), this.options.content && (this._content = this.options.content);
      },
      openOn: function openOn(t) {
        return (t = arguments.length ? t : this._source._map).hasLayer(this) || t.addLayer(this), this;
      },
      close: function close() {
        return this._map && this._map.removeLayer(this), this;
      },
      toggle: function toggle(t) {
        return this._map ? this.close() : (arguments.length ? this._source = t : t = this._source, this._prepareOpen(), this.openOn(t._map)), this;
      },
      onAdd: function onAdd(t) {
        this._zoomAnimated = t._zoomAnimated, this._container || this._initLayout(), t._fadeAnimated && (this._container.style.opacity = 0), clearTimeout(this._removeTimeout), this.getPane().appendChild(this._container), this.update(), t._fadeAnimated && (this._container.style.opacity = 1), this.bringToFront(), this.options.interactive && (this._container.classList.add("leaflet-interactive"), this.addInteractiveTarget(this._container));
      },
      onRemove: function onRemove(t) {
        var _this4 = this;
        t._fadeAnimated ? (this._container.style.opacity = 0, this._removeTimeout = setTimeout(function () {
          return _this4._container.remove();
        }, 200)) : this._container.remove(), this.options.interactive && (this._container.classList.remove("leaflet-interactive"), this.removeInteractiveTarget(this._container));
      },
      getLatLng: function getLatLng() {
        return this._latlng;
      },
      setLatLng: function setLatLng(t) {
        return this._latlng = w(t), this._map && (this._updatePosition(), this._adjustPan()), this;
      },
      getContent: function getContent() {
        return this._content;
      },
      setContent: function setContent(t) {
        return this._content = t, this.update(), this;
      },
      getElement: function getElement() {
        return this._container;
      },
      update: function update() {
        this._map && (this._container.style.visibility = "hidden", this._updateContent(), this._updateLayout(), this._updatePosition(), this._container.style.visibility = "", this._adjustPan());
      },
      getEvents: function getEvents() {
        var t = {
          zoom: this._updatePosition,
          viewreset: this._updatePosition
        };
        return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
      },
      isOpen: function isOpen() {
        return !!this._map && this._map.hasLayer(this);
      },
      bringToFront: function bringToFront() {
        return this._map && wt(this._container), this;
      },
      bringToBack: function bringToBack() {
        return this._map && bt(this._container), this;
      },
      _prepareOpen: function _prepareOpen(t) {
        var e = this._source;
        if (!e._map) return !1;
        if (e instanceof E) {
          e = null;
          var i = this._source._layers;
          for (var _o6 in i) if (i[_o6]._map) {
            e = i[_o6];
            break;
          }
          if (!e) return !1;
          this._source = e;
        }
        if (!t) if (e.getCenter) t = e.getCenter();else if (e.getLatLng) t = e.getLatLng();else {
          if (!e.getBounds) throw new Error("Unable to get source layer LatLng.");
          t = e.getBounds().getCenter();
        }
        return this.setLatLng(t), this._map && this.update(), !0;
      },
      _updateContent: function _updateContent() {
        if (this._content) {
          var t = this._contentNode,
            e = "function" == typeof this._content ? this._content(this._source || this) : this._content;
          if ("string" == typeof e) t.innerHTML = e;else {
            for (; t.hasChildNodes();) t.removeChild(t.firstChild);
            t.appendChild(e);
          }
          this.fire("contentupdate");
        }
      },
      _updatePosition: function _updatePosition() {
        if (this._map) {
          var e = this._map.latLngToLayerPoint(this._latlng),
            i = this._getAnchor();
          var _t45 = g(this.options.offset);
          this._zoomAnimated ? L(this._container, e.add(i)) : _t45 = _t45.add(e).add(i);
          e = this._containerBottom = -_t45.y, i = this._containerLeft = -Math.round(this._containerWidth / 2) + _t45.x;
          this._container.style.bottom = e + "px", this._container.style.left = i + "px";
        }
      },
      _getAnchor: function _getAnchor() {
        return [0, 0];
      }
    }),
    ii = (C.include({
      _initOverlay: function _initOverlay(t, e, i, o) {
        var s = e;
        return s instanceof t || (s = new t(o).setContent(e)), i && s.setLatLng(i), s;
      }
    }), S.include({
      _initOverlay: function _initOverlay(t, e, i, o) {
        var s = i;
        return s instanceof t ? (n(s, o), s._source = this) : (s = e && !o ? e : new t(o, this)).setContent(i), s;
      }
    }), B.extend({
      options: {
        pane: "popupPane",
        offset: [0, 7],
        maxWidth: 300,
        minWidth: 50,
        maxHeight: null,
        autoPan: !0,
        autoPanPaddingTopLeft: null,
        autoPanPaddingBottomRight: null,
        autoPanPadding: [5, 5],
        keepInView: !1,
        closeButton: !0,
        closeButtonLabel: "Close popup",
        autoClose: !0,
        closeOnEscapeKey: !0,
        className: ""
      },
      openOn: function openOn(t) {
        return !(t = arguments.length ? t : this._source._map).hasLayer(this) && t._popup && t._popup.options.autoClose && t.removeLayer(t._popup), t._popup = this, B.prototype.openOn.call(this, t);
      },
      onAdd: function onAdd(t) {
        B.prototype.onAdd.call(this, t), t.fire("popupopen", {
          popup: this
        }), this._source && (this._source.fire("popupopen", {
          popup: this
        }, !0), this._source instanceof Se || this._source.on("preclick", Ht));
      },
      onRemove: function onRemove(t) {
        B.prototype.onRemove.call(this, t), t.fire("popupclose", {
          popup: this
        }), this._source && (this._source.fire("popupclose", {
          popup: this
        }, !0), this._source instanceof Se || this._source.off("preclick", Ht));
      },
      getEvents: function getEvents() {
        var t = B.prototype.getEvents.call(this);
        return (void 0 !== this.options.closeOnClick ? this.options.closeOnClick : this._map.options.closePopupOnClick) && (t.preclick = this.close), this.options.keepInView && (t.moveend = this._adjustPan), t;
      },
      _initLayout: function _initLayout() {
        var t = "leaflet-popup",
          e = this._container = b("div", t + " ".concat(this.options.className || "", " leaflet-zoom-animated")),
          i = this._wrapper = b("div", t + "-content-wrapper", e);
        this._contentNode = b("div", t + "-content", i), Vt(e), Ut(this._contentNode), P(e, "contextmenu", Ht), this._tipContainer = b("div", t + "-tip-container", e), this._tip = b("div", t + "-tip", this._tipContainer), this.options.closeButton && ((i = this._closeButton = b("a", t + "-close-button", e)).setAttribute("role", "button"), i.setAttribute("aria-label", this.options.closeButtonLabel), i.href = "#close", i.innerHTML = '<span aria-hidden="true">&#215;</span>', P(i, "click", function (t) {
          M(t), this.close();
        }, this));
      },
      _updateLayout: function _updateLayout() {
        var t = this._contentNode,
          e = t.style,
          i = (e.width = "", e.whiteSpace = "nowrap", t.offsetWidth),
          i = Math.min(i, this.options.maxWidth),
          i = (i = Math.max(i, this.options.minWidth), e.width = i + 1 + "px", e.whiteSpace = "", e.height = "", t.offsetHeight),
          o = this.options.maxHeight,
          s = "leaflet-popup-scrolled";
        o && o < i ? (e.height = o + "px", t.classList.add(s)) : t.classList.remove(s), this._containerWidth = this._container.offsetWidth;
      },
      _animateZoom: function _animateZoom(t) {
        var t = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center),
          e = this._getAnchor();
        L(this._container, t.add(e));
      },
      _adjustPan: function _adjustPan() {
        if (this.options.autoPan) if (this._map._panAnim && this._map._panAnim.stop(), this._autopanning) this._autopanning = !1;else {
          var i = this._map,
            o = parseInt(getComputedStyle(this._container).marginBottom, 10) || 0,
            o = this._container.offsetHeight + o,
            s = this._containerWidth,
            n = new c(this._containerLeft, -o - this._containerBottom),
            n = (n._add(Tt(this._container)), i.layerPointToContainerPoint(n)),
            a = g(this.options.autoPanPadding),
            r = g(this.options.autoPanPaddingTopLeft || a),
            a = g(this.options.autoPanPaddingBottomRight || a),
            h = i.getSize();
          var _t46 = 0,
            _e26 = 0;
          n.x + s + a.x > h.x && (_t46 = n.x + s - h.x + a.x), n.x - _t46 - r.x < 0 && (_t46 = n.x - r.x), n.y + o + a.y > h.y && (_e26 = n.y + o - h.y + a.y), n.y - _e26 - r.y < 0 && (_e26 = n.y - r.y), (_t46 || _e26) && (this.options.keepInView && (this._autopanning = !0), i.fire("autopanstart").panBy([_t46, _e26]));
        }
      },
      _getAnchor: function _getAnchor() {
        return g(this._source && this._source._getPopupAnchor ? this._source._getPopupAnchor() : [0, 0]);
      }
    }));
  function oi(t, e) {
    return new ii(t, e);
  }
  C.mergeOptions({
    closePopupOnClick: !0
  }), C.include({
    openPopup: function openPopup(t, e, i) {
      return this._initOverlay(ii, t, e, i).openOn(this), this;
    },
    closePopup: function closePopup(t) {
      return (t = arguments.length ? t : this._popup) && t.close(), this;
    }
  }), S.include({
    bindPopup: function bindPopup(t, e) {
      return this._popup = this._initOverlay(ii, this._popup, t, e), this._popupHandlersAdded || (this.on({
        click: this._openPopup,
        keypress: this._onKeyPress,
        remove: this.closePopup,
        move: this._movePopup
      }), this._popupHandlersAdded = !0), this;
    },
    unbindPopup: function unbindPopup() {
      return this._popup && (this.off({
        click: this._openPopup,
        keypress: this._onKeyPress,
        remove: this.closePopup,
        move: this._movePopup
      }), this._popupHandlersAdded = !1, this._popup = null), this;
    },
    openPopup: function openPopup(t) {
      return this._popup && (this instanceof E || (this._popup._source = this), this._popup._prepareOpen(t || this._latlng)) && this._popup.openOn(this._map), this;
    },
    closePopup: function closePopup() {
      return this._popup && this._popup.close(), this;
    },
    togglePopup: function togglePopup() {
      return this._popup && this._popup.toggle(this), this;
    },
    isPopupOpen: function isPopupOpen() {
      return !!this._popup && this._popup.isOpen();
    },
    setPopupContent: function setPopupContent(t) {
      return this._popup && this._popup.setContent(t), this;
    },
    getPopup: function getPopup() {
      return this._popup;
    },
    _openPopup: function _openPopup(t) {
      var e;
      this._popup && this._map && (Gt(t), e = t.layer || t.target, this._popup._source !== e || e instanceof Se ? (this._popup._source = e, this.openPopup(t.latlng)) : this._map.hasLayer(this._popup) ? this.closePopup() : this.openPopup(t.latlng));
    },
    _movePopup: function _movePopup(t) {
      this._popup.setLatLng(t.latlng);
    },
    _onKeyPress: function _onKeyPress(t) {
      "Enter" === t.originalEvent.code && this._openPopup(t);
    }
  });
  var si = B.extend({
    options: {
      pane: "tooltipPane",
      offset: [0, 0],
      direction: "auto",
      permanent: !1,
      sticky: !1,
      opacity: .9
    },
    onAdd: function onAdd(t) {
      B.prototype.onAdd.call(this, t), this.setOpacity(this.options.opacity), t.fire("tooltipopen", {
        tooltip: this
      }), this._source && (this.addEventParent(this._source), this._source.fire("tooltipopen", {
        tooltip: this
      }, !0));
    },
    onRemove: function onRemove(t) {
      B.prototype.onRemove.call(this, t), t.fire("tooltipclose", {
        tooltip: this
      }), this._source && (this.removeEventParent(this._source), this._source.fire("tooltipclose", {
        tooltip: this
      }, !0));
    },
    getEvents: function getEvents() {
      var t = B.prototype.getEvents.call(this);
      return this.options.permanent || (t.preclick = this.close), t;
    },
    _initLayout: function _initLayout() {
      var t = "leaflet-tooltip ".concat(this.options.className || "", " leaflet-zoom-") + (this._zoomAnimated ? "animated" : "hide");
      this._contentNode = this._container = b("div", t), this._container.setAttribute("role", "tooltip"), this._container.setAttribute("id", "leaflet-tooltip-" + r(this));
    },
    _updateLayout: function _updateLayout() {},
    _adjustPan: function _adjustPan() {},
    _setPosition: function _setPosition(t) {
      var e,
        i,
        o = this.options.direction;
      var s = this._map,
        n = this._container,
        a = s.latLngToContainerPoint(s.getCenter()),
        s = s.layerPointToContainerPoint(t),
        r = n.offsetWidth,
        h = n.offsetHeight,
        l = g(this.options.offset),
        d = this._getAnchor();
      i = "top" === o ? (e = r / 2, h) : "bottom" === o ? (e = r / 2, 0) : (e = "center" === o ? r / 2 : "right" === o ? 0 : "left" === o ? r : s.x < a.x ? (o = "right", 0) : (o = "left", r + 2 * (l.x + d.x)), h / 2), t = t.subtract(g(e, i, !0)).add(l).add(d), n.classList.remove("leaflet-tooltip-right", "leaflet-tooltip-left", "leaflet-tooltip-top", "leaflet-tooltip-bottom"), n.classList.add("leaflet-tooltip-" + o), L(n, t);
    },
    _updatePosition: function _updatePosition() {
      var t = this._map.latLngToLayerPoint(this._latlng);
      this._setPosition(t);
    },
    setOpacity: function setOpacity(t) {
      this.options.opacity = t, this._container && (this._container.style.opacity = t);
    },
    _animateZoom: function _animateZoom(t) {
      t = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center);
      this._setPosition(t);
    },
    _getAnchor: function _getAnchor() {
      return g(this._source && this._source._getTooltipAnchor && !this.options.sticky ? this._source._getTooltipAnchor() : [0, 0]);
    }
  });
  function ni(t, e) {
    return new si(t, e);
  }
  C.include({
    openTooltip: function openTooltip(t, e, i) {
      return this._initOverlay(si, t, e, i).openOn(this), this;
    },
    closeTooltip: function closeTooltip(t) {
      return t.close(), this;
    }
  }), S.include({
    bindTooltip: function bindTooltip(t, e) {
      return this._tooltip && this.isTooltipOpen() && this.unbindTooltip(), this._tooltip = this._initOverlay(si, this._tooltip, t, e), this._initTooltipInteractions(), this._tooltip.options.permanent && this._map && this._map.hasLayer(this) && this.openTooltip(), this;
    },
    unbindTooltip: function unbindTooltip() {
      return this._tooltip && (this._initTooltipInteractions(!0), this.closeTooltip(), this._tooltip = null), this;
    },
    _initTooltipInteractions: function _initTooltipInteractions(t) {
      var e, i;
      !t && this._tooltipHandlersAdded || (e = t ? "off" : "on", i = {
        remove: this.closeTooltip,
        move: this._moveTooltip
      }, this._tooltip.options.permanent ? i.add = this._openTooltip : (i.mouseover = this._openTooltip, i.mouseout = this.closeTooltip, i.click = this._openTooltip, this._map ? this._addFocusListeners() : i.add = this._addFocusListeners), this._tooltip.options.sticky && (i.mousemove = this._moveTooltip), this[e](i), this._tooltipHandlersAdded = !t);
    },
    openTooltip: function openTooltip(t) {
      return this._tooltip && (this instanceof E || (this._tooltip._source = this), this._tooltip._prepareOpen(t)) && (this._tooltip.openOn(this._map), this.getElement ? this._setAriaDescribedByOnLayer(this) : this.eachLayer && this.eachLayer(this._setAriaDescribedByOnLayer, this)), this;
    },
    closeTooltip: function closeTooltip() {
      if (this._tooltip) return this._tooltip.close();
    },
    toggleTooltip: function toggleTooltip() {
      return this._tooltip && this._tooltip.toggle(this), this;
    },
    isTooltipOpen: function isTooltipOpen() {
      return this._tooltip.isOpen();
    },
    setTooltipContent: function setTooltipContent(t) {
      return this._tooltip && this._tooltip.setContent(t), this;
    },
    getTooltip: function getTooltip() {
      return this._tooltip;
    },
    _addFocusListeners: function _addFocusListeners() {
      this.getElement ? this._addFocusListenersOnLayer(this) : this.eachLayer && this.eachLayer(this._addFocusListenersOnLayer, this);
    },
    _addFocusListenersOnLayer: function _addFocusListenersOnLayer(t) {
      var e = "function" == typeof t.getElement && t.getElement();
      e && (P(e, "focus", function () {
        this._tooltip._source = t, this.openTooltip();
      }, this), P(e, "blur", this.closeTooltip, this));
    },
    _setAriaDescribedByOnLayer: function _setAriaDescribedByOnLayer(t) {
      t = "function" == typeof t.getElement && t.getElement();
      t && t.setAttribute("aria-describedby", this._tooltip._container.id);
    },
    _openTooltip: function _openTooltip(t) {
      var _this5 = this;
      this._tooltip && this._map && (this._map.dragging && this._map.dragging.moving() && !this._openOnceFlag ? (this._openOnceFlag = !0, this._map.once("moveend", function () {
        _this5._openOnceFlag = !1, _this5._openTooltip(t);
      })) : (this._tooltip._source = t.layer || t.target, this.openTooltip(this._tooltip.options.sticky ? t.latlng : void 0)));
    },
    _moveTooltip: function _moveTooltip(t) {
      var e = t.latlng,
        i,
        o;
      this._tooltip.options.sticky && t.originalEvent && (i = this._map.mouseEventToContainerPoint(t.originalEvent), o = this._map.containerPointToLayerPoint(i), e = this._map.layerPointToLatLng(o)), this._tooltip.setLatLng(e);
    }
  });
  var ai = Te.extend({
    options: {
      iconSize: [12, 12],
      html: !1,
      bgPos: null,
      className: "leaflet-div-icon"
    },
    createIcon: function createIcon(t) {
      var t = t && "DIV" === t.tagName ? t : document.createElement("div"),
        e = this.options;
      return e.html instanceof Element ? (t.replaceChildren(), t.appendChild(e.html)) : t.innerHTML = !1 !== e.html ? e.html : "", e.bgPos && (e = g(e.bgPos), t.style.backgroundPosition = "".concat(-e.x, "px ").concat(-e.y, "px")), this._setIconStyles(t, "icon"), t;
    },
    createShadow: function createShadow() {
      return null;
    }
  });
  function ri(t) {
    return new ai(t);
  }
  Te.Default = Me;
  var hi = S.extend({
    options: {
      tileSize: 256,
      opacity: 1,
      updateWhenIdle: v.mobile,
      updateWhenZooming: !0,
      updateInterval: 200,
      zIndex: 1,
      bounds: null,
      minZoom: 0,
      maxZoom: void 0,
      maxNativeZoom: void 0,
      minNativeZoom: void 0,
      noWrap: !1,
      pane: "tilePane",
      className: "",
      keepBuffer: 2
    },
    initialize: function initialize(t) {
      n(this, t);
    },
    onAdd: function onAdd() {
      this._initContainer(), this._levels = {}, this._tiles = {}, this._resetView();
    },
    beforeAdd: function beforeAdd(t) {
      t._addZoomLimit(this);
    },
    onRemove: function onRemove(t) {
      this._removeAllTiles(), this._container.remove(), t._removeZoomLimit(this), this._container = null, this._tileZoom = void 0;
    },
    bringToFront: function bringToFront() {
      return this._map && (wt(this._container), this._setAutoZIndex(Math.max)), this;
    },
    bringToBack: function bringToBack() {
      return this._map && (bt(this._container), this._setAutoZIndex(Math.min)), this;
    },
    getContainer: function getContainer() {
      return this._container;
    },
    setOpacity: function setOpacity(t) {
      return this.options.opacity = t, this._updateOpacity(), this;
    },
    setZIndex: function setZIndex(t) {
      return this.options.zIndex = t, this._updateZIndex(), this;
    },
    isLoading: function isLoading() {
      return this._loading;
    },
    redraw: function redraw() {
      var t;
      return this._map && (this._removeAllTiles(), (t = this._clampZoom(this._map.getZoom())) !== this._tileZoom && (this._tileZoom = t, this._updateLevels()), this._update()), this;
    },
    getEvents: function getEvents() {
      var t = {
        viewprereset: this._invalidateAll,
        viewreset: this._resetView,
        zoom: this._resetView,
        moveend: this._onMoveEnd
      };
      return this.options.updateWhenIdle || (this._onMove || (this._onMove = j(this._onMoveEnd, this.options.updateInterval, this)), t.move = this._onMove), this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
    },
    createTile: function createTile() {
      return document.createElement("div");
    },
    getTileSize: function getTileSize() {
      var t = this.options.tileSize;
      return t instanceof c ? t : new c(t, t);
    },
    _updateZIndex: function _updateZIndex() {
      this._container && void 0 !== this.options.zIndex && null !== this.options.zIndex && (this._container.style.zIndex = this.options.zIndex);
    },
    _setAutoZIndex: function _setAutoZIndex(o) {
      var s = this.getPane().children;
      var n = -o(-1 / 0, 1 / 0);
      for (var _t47 = 0, _e27 = s.length, _i18; _t47 < _e27; _t47++) _i18 = s[_t47].style.zIndex, s[_t47] !== this._container && _i18 && (n = o(n, +_i18));
      isFinite(n) && (this.options.zIndex = n + o(-1, 1), this._updateZIndex());
    },
    _updateOpacity: function _updateOpacity() {
      if (this._map) {
        this._container.style.opacity = this.options.opacity;
        var i,
          o,
          s = +new Date();
        var _t48 = !1,
          _e28 = !1;
        for (var _n3 in this._tiles) Object.hasOwn(this._tiles, _n3) && (i = this._tiles[_n3]).current && i.loaded && (o = Math.min(1, (s - i.loaded) / 200), (i.el.style.opacity = o) < 1 ? _t48 = !0 : (i.active ? _e28 = !0 : this._onOpaqueTile(i), i.active = !0));
        _e28 && !this._noPrune && this._pruneTiles(), _t48 && (d(this._fadeFrame), this._fadeFrame = x(this._updateOpacity, this));
      }
    },
    _onOpaqueTile: a,
    _initContainer: function _initContainer() {
      this._container || (this._container = b("div", "leaflet-layer " + (this.options.className || "")), this._updateZIndex(), this.options.opacity < 1 && this._updateOpacity(), this.getPane().appendChild(this._container));
    },
    _updateLevels: function _updateLevels() {
      var e = this._tileZoom,
        i = this.options.maxZoom;
      if (void 0 !== e) {
        for (var o in this._levels) Object.hasOwn(this._levels, o) && (o = Number(o), this._levels[o].el.children.length || o === e ? (this._levels[o].el.style.zIndex = i - Math.abs(e - o), this._onUpdateLevel(o)) : (this._levels[o].el.remove(), this._removeTilesAtZoom(o), this._onRemoveLevel(o), delete this._levels[o]));
        var _t49 = this._levels[e];
        var s = this._map;
        return _t49 || ((_t49 = this._levels[e] = {}).el = b("div", "leaflet-tile-container leaflet-zoom-animated", this._container), _t49.el.style.zIndex = i, _t49.origin = s.project(s.unproject(s.getPixelOrigin()), e).round(), _t49.zoom = e, this._setZoomTransform(_t49, s.getCenter(), s.getZoom()), a(_t49.el.offsetWidth), this._onCreateLevel(_t49)), this._level = _t49;
      }
    },
    _onUpdateLevel: a,
    _onRemoveLevel: a,
    _onCreateLevel: a,
    _pruneTiles: function _pruneTiles() {
      if (this._map) {
        var _t50, _e29;
        var i,
          o = this._map.getZoom();
        if (o > this.options.maxZoom || o < this.options.minZoom) this._removeAllTiles();else {
          for (_t50 in this._tiles) Object.hasOwn(this._tiles, _t50) && ((_e29 = this._tiles[_t50]).retain = _e29.current);
          for (_t50 in this._tiles) Object.hasOwn(this._tiles, _t50) && (_e29 = this._tiles[_t50]).current && !_e29.active && (i = _e29.coords, this._retainParent(i.x, i.y, i.z, i.z - 5) || this._retainChildren(i.x, i.y, i.z, i.z + 2));
          for (_t50 in this._tiles) this._tiles[_t50].retain || this._removeTile(_t50);
        }
      }
    },
    _removeTilesAtZoom: function _removeTilesAtZoom(t) {
      for (var _e30 in this._tiles) this._tiles[_e30].coords.z === t && this._removeTile(_e30);
    },
    _removeAllTiles: function _removeAllTiles() {
      for (var _t51 in this._tiles) Object.hasOwn(this._tiles, _t51) && this._removeTile(_t51);
    },
    _invalidateAll: function _invalidateAll() {
      for (var _t52 in this._levels) Object.hasOwn(this._levels, _t52) && (this._levels[_t52].el.remove(), this._onRemoveLevel(Number(_t52)), delete this._levels[_t52]);
      this._removeAllTiles(), this._tileZoom = void 0;
    },
    _retainParent: function _retainParent(t, e, i, o) {
      var t = Math.floor(t / 2),
        e = Math.floor(e / 2),
        i = i - 1,
        s = new c(+t, +e),
        s = (s.z = i, this._tileCoordsToKey(s)),
        s = this._tiles[s];
      return s && s.active ? s.retain = !0 : (s && s.loaded && (s.retain = !0), o < i && this._retainParent(t, e, i, o));
    },
    _retainChildren: function _retainChildren(t, i, o, s) {
      for (var _e31 = 2 * t; _e31 < 2 * t + 2; _e31++) for (var _t53 = 2 * i; _t53 < 2 * i + 2; _t53++) {
        var n = new c(_e31, _t53),
          n = (n.z = o + 1, this._tileCoordsToKey(n)),
          n = this._tiles[n];
        n && n.active ? n.retain = !0 : (n && n.loaded && (n.retain = !0), o + 1 < s && this._retainChildren(_e31, _t53, o + 1, s));
      }
    },
    _resetView: function _resetView(t) {
      t = t && (t.pinch || t.flyTo);
      this._setView(this._map.getCenter(), this._map.getZoom(), t, t);
    },
    _animateZoom: function _animateZoom(t) {
      this._setView(t.center, t.zoom, !0, t.noUpdate);
    },
    _clampZoom: function _clampZoom(t) {
      var e = this.options;
      return void 0 !== e.minNativeZoom && t < e.minNativeZoom ? e.minNativeZoom : void 0 !== e.maxNativeZoom && e.maxNativeZoom < t ? e.maxNativeZoom : t;
    },
    _setView: function _setView(t, e, i, o) {
      var s = Math.round(e);
      s = void 0 !== this.options.maxZoom && s > this.options.maxZoom || void 0 !== this.options.minZoom && s < this.options.minZoom ? void 0 : this._clampZoom(s);
      var n = this.options.updateWhenZooming && s !== this._tileZoom;
      o && !n || (this._tileZoom = s, this._abortLoading && this._abortLoading(), this._updateLevels(), this._resetGrid(), void 0 !== s && this._update(t), i || this._pruneTiles(), this._noPrune = !!i), this._setZoomTransforms(t, e);
    },
    _setZoomTransforms: function _setZoomTransforms(t, e) {
      for (var _i19 in this._levels) Object.hasOwn(this._levels, _i19) && this._setZoomTransform(this._levels[_i19], t, e);
    },
    _setZoomTransform: function _setZoomTransform(t, e, i) {
      var o = this._map.getZoomScale(i, t.zoom),
        e = t.origin.multiplyBy(o).subtract(this._map._getNewPixelOrigin(e, i)).round();
      Lt(t.el, e, o);
    },
    _resetGrid: function _resetGrid() {
      var t = this._map,
        e = t.options.crs,
        i = this._tileSize = this.getTileSize(),
        o = this._tileZoom,
        s = this._map.getPixelWorldBounds(this._tileZoom);
      s && (this._globalTileRange = this._pxBoundsToTileRange(s)), this._wrapX = e.wrapLng && !this.options.noWrap && [Math.floor(t.project([0, e.wrapLng[0]], o).x / i.x), Math.ceil(t.project([0, e.wrapLng[1]], o).x / i.y)], this._wrapY = e.wrapLat && !this.options.noWrap && [Math.floor(t.project([e.wrapLat[0], 0], o).y / i.x), Math.ceil(t.project([e.wrapLat[1], 0], o).y / i.y)];
    },
    _onMoveEnd: function _onMoveEnd() {
      this._map && !this._map._animatingZoom && this._update();
    },
    _getTiledPixelBounds: function _getTiledPixelBounds(t) {
      var e = this._map,
        i = e._animatingZoom ? Math.max(e._animateToZoom, e.getZoom()) : e.getZoom(),
        i = e.getZoomScale(i, this._tileZoom),
        t = e.project(t, this._tileZoom).floor(),
        e = e.getSize().divideBy(2 * i);
      return new m(t.subtract(e), t.add(e));
    },
    _update: function _update(t) {
      var e = this._map;
      if (e) {
        var i,
          o = this._clampZoom(e.getZoom());
        if (void 0 === t && (t = e.getCenter()), void 0 !== this._tileZoom) {
          var _r2 = this._getTiledPixelBounds(t),
            _h = this._pxBoundsToTileRange(_r2),
            _l = _h.getCenter(),
            _d2 = [],
            _2 = this.options.keepBuffer,
            _u = new m(_h.getBottomLeft().subtract([_2, -_2]), _h.getTopRight().add([_2, -_2]));
          if (!(isFinite(_h.min.x) && isFinite(_h.min.y) && isFinite(_h.max.x) && isFinite(_h.max.y))) throw new Error("Attempted to load an infinite number of tiles");
          for (var _p in this._tiles) !Object.hasOwn(this._tiles, _p) || (i = this._tiles[_p].coords).z === this._tileZoom && _u.contains(new c(i.x, i.y)) || (this._tiles[_p].current = !1);
          if (1 < Math.abs(o - this._tileZoom)) this._setView(t, o);else {
            for (var _e32 = _h.min.y; _e32 <= _h.max.y; _e32++) for (var _t54 = _h.min.x; _t54 <= _h.max.x; _t54++) {
              var s,
                n = new c(_t54, _e32);
              n.z = this._tileZoom, this._isValidTile(n) && ((s = this._tiles[this._tileCoordsToKey(n)]) ? s.current = !0 : _d2.push(n));
            }
            if (_d2.sort(function (t, e) {
              return t.distanceTo(_l) - e.distanceTo(_l);
            }), 0 !== _d2.length) {
              this._loading || (this._loading = !0, this.fire("loading"));
              var a = document.createDocumentFragment();
              for (var _t55 = 0; _t55 < _d2.length; _t55++) this._addTile(_d2[_t55], a);
              this._level.el.appendChild(a);
            }
          }
        }
      }
    },
    _isValidTile: function _isValidTile(t) {
      var e = this._map.options.crs;
      if (!e.infinite) {
        var i = this._globalTileRange;
        if (!e.wrapLng && (t.x < i.min.x || t.x > i.max.x) || !e.wrapLat && (t.y < i.min.y || t.y > i.max.y)) return !1;
      }
      return !this.options.bounds || (e = this._tileCoordsToBounds(t), f(this.options.bounds).overlaps(e));
    },
    _keyToBounds: function _keyToBounds(t) {
      return this._tileCoordsToBounds(this._keyToTileCoords(t));
    },
    _tileCoordsToNwSe: function _tileCoordsToNwSe(t) {
      var e = this._map,
        i = this.getTileSize(),
        o = t.scaleBy(i),
        i = o.add(i);
      return [e.unproject(o, t.z), e.unproject(i, t.z)];
    },
    _tileCoordsToBounds: function _tileCoordsToBounds(t) {
      t = this._tileCoordsToNwSe(t);
      var e = new u(t[0], t[1]);
      return e = this.options.noWrap ? e : this._map.wrapLatLngBounds(e);
    },
    _tileCoordsToKey: function _tileCoordsToKey(t) {
      return "".concat(t.x, ":").concat(t.y, ":") + t.z;
    },
    _keyToTileCoords: function _keyToTileCoords(t) {
      var t = t.split(":"),
        e = new c(+t[0], +t[1]);
      return e.z = +t[2], e;
    },
    _removeTile: function _removeTile(t) {
      var e = this._tiles[t];
      e && (e.el.remove(), delete this._tiles[t], this.fire("tileunload", {
        tile: e.el,
        coords: this._keyToTileCoords(t)
      }));
    },
    _initTile: function _initTile(t) {
      t.classList.add("leaflet-tile");
      var e = this.getTileSize();
      t.style.width = e.x + "px", t.style.height = e.y + "px", t.onselectstart = a, t.onmousemove = a;
    },
    _addTile: function _addTile(t, e) {
      var i = this._getTilePos(t),
        o = this._tileCoordsToKey(t),
        s = this.createTile(this._wrapCoords(t), this._tileReady.bind(this, t));
      this._initTile(s), this.createTile.length < 2 && x(this._tileReady.bind(this, t, null, s)), L(s, i), this._tiles[o] = {
        el: s,
        coords: t,
        current: !0
      }, e.appendChild(s), this.fire("tileloadstart", {
        tile: s,
        coords: t
      });
    },
    _tileReady: function _tileReady(t, e, i) {
      e && this.fire("tileerror", {
        error: e,
        tile: i,
        coords: t
      });
      var o = this._tileCoordsToKey(t);
      (i = this._tiles[o]) && (i.loaded = +new Date(), this._map._fadeAnimated ? (i.el.style.opacity = 0, d(this._fadeFrame), this._fadeFrame = x(this._updateOpacity, this)) : (i.active = !0, this._pruneTiles()), e || (i.el.classList.add("leaflet-tile-loaded"), this.fire("tileload", {
        tile: i.el,
        coords: t
      })), this._noTilesToLoad()) && (this._loading = !1, this.fire("load"), this._map._fadeAnimated ? setTimeout(this._pruneTiles.bind(this), 250) : x(this._pruneTiles, this));
    },
    _getTilePos: function _getTilePos(t) {
      return t.scaleBy(this.getTileSize()).subtract(this._level.origin);
    },
    _wrapCoords: function _wrapCoords(t) {
      var e = new c(this._wrapX ? D(t.x, this._wrapX) : t.x, this._wrapY ? D(t.y, this._wrapY) : t.y);
      return e.z = t.z, e;
    },
    _pxBoundsToTileRange: function _pxBoundsToTileRange(t) {
      var e = this.getTileSize();
      return new m(t.min.unscaleBy(e).floor(), t.max.unscaleBy(e).ceil().subtract([1, 1]));
    },
    _noTilesToLoad: function _noTilesToLoad() {
      for (var _t56 in this._tiles) if (!this._tiles[_t56].loaded) return !1;
      return !0;
    }
  });
  function li(t) {
    return new hi(t);
  }
  var di = hi.extend({
    options: {
      minZoom: 0,
      maxZoom: 18,
      subdomains: "abc",
      errorTileUrl: "",
      zoomOffset: 0,
      tms: !1,
      zoomReverse: !1,
      detectRetina: !1,
      crossOrigin: !1,
      referrerPolicy: !1
    },
    initialize: function initialize(t, e) {
      this._url = t, (e = n(this, e)).detectRetina && v.retina && 0 < e.maxZoom ? (e.tileSize = Math.floor(e.tileSize / 2), e.zoomReverse ? (e.zoomOffset--, e.minZoom = Math.min(e.maxZoom, e.minZoom + 1)) : (e.zoomOffset++, e.maxZoom = Math.max(e.minZoom, e.maxZoom - 1)), e.minZoom = Math.max(0, e.minZoom)) : e.zoomReverse ? e.minZoom = Math.min(e.maxZoom, e.minZoom) : e.maxZoom = Math.max(e.minZoom, e.maxZoom), "string" == typeof e.subdomains && (e.subdomains = e.subdomains.split("")), this.on("tileunload", this._onTileRemove);
    },
    setUrl: function setUrl(t, e) {
      return this._url === t && void 0 === e && (e = !0), this._url = t, e || this.redraw(), this;
    },
    createTile: function createTile(t, e) {
      var i = document.createElement("img");
      return P(i, "load", this._tileOnLoad.bind(this, e, i)), P(i, "error", this._tileOnError.bind(this, e, i)), !this.options.crossOrigin && "" !== this.options.crossOrigin || (i.crossOrigin = !0 === this.options.crossOrigin ? "" : this.options.crossOrigin), "string" == typeof this.options.referrerPolicy && (i.referrerPolicy = this.options.referrerPolicy), i.alt = "", i.src = this.getTileUrl(t), i;
    },
    getTileUrl: function getTileUrl(t) {
      var e = {
        r: v.retina ? "@2x" : "",
        s: this._getSubdomain(t),
        x: t.x,
        y: t.y,
        z: this._getZoomForUrl()
      };
      return this._map && !this._map.options.crs.infinite && (t = this._globalTileRange.max.y - t.y, this.options.tms && (e.y = t), e["-y"] = t), F(this._url, h(e, this.options));
    },
    _tileOnLoad: function _tileOnLoad(t, e) {
      t(null, e);
    },
    _tileOnError: function _tileOnError(t, e, i) {
      var o = this.options.errorTileUrl;
      o && e.getAttribute("src") !== o && (e.src = o), t(i, e);
    },
    _onTileRemove: function _onTileRemove(t) {
      t.tile.onload = null;
    },
    _getZoomForUrl: function _getZoomForUrl() {
      var t = this._tileZoom;
      var e = this.options.maxZoom,
        i = this.options.zoomReverse,
        o = this.options.zoomOffset;
      return (t = i ? e - t : t) + o;
    },
    _getSubdomain: function _getSubdomain(t) {
      t = Math.abs(t.x + t.y) % this.options.subdomains.length;
      return this.options.subdomains[t];
    },
    _abortLoading: function _abortLoading() {
      var t, e;
      for (t in this._tiles) {
        var i;
        this._tiles[t].coords.z !== this._tileZoom && ((e = this._tiles[t].el).onload = a, e.onerror = a, e.complete || (e.src = H, i = this._tiles[t].coords, e.remove(), delete this._tiles[t], this.fire("tileabort", {
          tile: e,
          coords: i
        })));
      }
    },
    _removeTile: function _removeTile(t) {
      var e = this._tiles[t];
      if (e) return e.el.setAttribute("src", H), hi.prototype._removeTile.call(this, t);
    },
    _tileReady: function _tileReady(t, e, i) {
      if (this._map && (!i || i.getAttribute("src") !== H)) return hi.prototype._tileReady.call(this, t, e, i);
    }
  });
  function _i(t, e) {
    return new di(t, e);
  }
  var ui = di.extend({
    defaultWmsParams: {
      service: "WMS",
      request: "GetMap",
      layers: "",
      styles: "",
      format: "image/jpeg",
      transparent: !1,
      version: "1.1.1"
    },
    options: {
      crs: null,
      uppercase: !1
    },
    initialize: function initialize(t, e) {
      this._url = t;
      var i = h({}, this.defaultWmsParams);
      for (var _s6 in e) _s6 in this.options || (i[_s6] = e[_s6]);
      var t = (e = n(this, e)).detectRetina && v.retina ? 2 : 1,
        o = this.getTileSize();
      i.width = o.x * t, i.height = o.y * t, this.wmsParams = i;
    },
    onAdd: function onAdd(t) {
      this._crs = this.options.crs || t.options.crs, this._wmsVersion = parseFloat(this.wmsParams.version);
      var e = 1.3 <= this._wmsVersion ? "crs" : "srs";
      this.wmsParams[e] = this._crs.code, di.prototype.onAdd.call(this, t);
    },
    getTileUrl: function getTileUrl(t) {
      var e = this._tileCoordsToNwSe(t),
        i = this._crs,
        i = _(i.project(e[0]), i.project(e[1])),
        e = i.min,
        i = i.max,
        e = (1.3 <= this._wmsVersion && this._crs === we ? [e.y, e.x, i.y, i.x] : [e.x, e.y, i.x, i.y]).join(","),
        i = di.prototype.getTileUrl.call(this, t);
      return i + N(this.wmsParams, i, this.options.uppercase) + (this.options.uppercase ? "&BBOX=" : "&bbox=") + e;
    },
    setParams: function setParams(t, e) {
      return h(this.wmsParams, t), e || this.redraw(), this;
    }
  });
  di.WMS = ui, _i.wms = function (t, e) {
    return new ui(t, e);
  };
  var A = $e.extend({
      initialize: function initialize(t) {
        n(this, _objectSpread(_objectSpread({}, t), {}, {
          continuous: !1
        })), r(this), this._layers = this._layers || {};
      },
      onAdd: function onAdd(t) {
        $e.prototype.onAdd.call(this, t), this.on("update", this._updatePaths, this);
      },
      onRemove: function onRemove() {
        $e.prototype.onRemove.call(this), this.off("update", this._updatePaths, this);
      },
      _onZoomEnd: function _onZoomEnd() {
        for (var _t57 in this._layers) Object.hasOwn(this._layers, _t57) && this._layers[_t57]._project();
      },
      _updatePaths: function _updatePaths() {
        for (var _t58 in this._layers) Object.hasOwn(this._layers, _t58) && this._layers[_t58]._update();
      },
      _onViewReset: function _onViewReset() {
        for (var _t59 in this._layers) Object.hasOwn(this._layers, _t59) && this._layers[_t59]._reset();
      },
      _onSettled: function _onSettled() {
        this._update();
      },
      _update: a
    }),
    pi = A.extend({
      options: {
        tolerance: 0
      },
      getEvents: function getEvents() {
        var t = A.prototype.getEvents.call(this);
        return t.viewprereset = this._onViewPreReset, t;
      },
      _onViewPreReset: function _onViewPreReset() {
        this._postponeUpdatePaths = !0;
      },
      onAdd: function onAdd(t) {
        A.prototype.onAdd.call(this, t), this._draw();
      },
      _initContainer: function _initContainer() {
        var t = this._container = document.createElement("canvas");
        P(t, "mousemove", this._onMouseMove, this), P(t, "click dblclick mousedown mouseup contextmenu", this._onClick, this), P(t, "mouseout", this._handleMouseOut, this), t._leaflet_disable_events = !0, this._ctx = t.getContext("2d");
      },
      _destroyContainer: function _destroyContainer() {
        d(this._redrawRequest), delete this._ctx, A.prototype._destroyContainer.call(this);
      },
      _resizeContainer: function _resizeContainer() {
        var t = A.prototype._resizeContainer.call(this),
          e = this._ctxScale = window.devicePixelRatio;
        this._container.width = e * t.x, this._container.height = e * t.y;
      },
      _updatePaths: function _updatePaths() {
        if (!this._postponeUpdatePaths) {
          this._redrawBounds = null;
          for (var _t60 in this._layers) Object.hasOwn(this._layers, _t60) && this._layers[_t60]._update();
          this._redraw();
        }
      },
      _update: function _update() {
        var t, e;
        this._map._animatingZoom && this._bounds || (t = this._bounds, e = this._ctxScale, this._ctx.setTransform(e, 0, 0, e, -t.min.x * e, -t.min.y * e), this.fire("update"));
      },
      _reset: function _reset() {
        A.prototype._reset.call(this), this._postponeUpdatePaths && (this._postponeUpdatePaths = !1, this._updatePaths());
      },
      _initPath: function _initPath(t) {
        this._updateDashArray(t);
        t = (this._layers[r(t)] = t)._order = {
          layer: t,
          prev: this._drawLast,
          next: null
        };
        this._drawLast && (this._drawLast.next = t), this._drawLast = t, this._drawFirst = this._drawFirst || this._drawLast;
      },
      _addPath: function _addPath(t) {
        this._requestRedraw(t);
      },
      _removePath: function _removePath(t) {
        var e = t._order,
          i = e.next,
          e = e.prev;
        i ? i.prev = e : this._drawLast = e, e ? e.next = i : this._drawFirst = i, delete t._order, delete this._layers[r(t)], this._requestRedraw(t);
      },
      _updatePath: function _updatePath(t) {
        this._extendRedrawBounds(t), t._project(), t._update(), this._requestRedraw(t);
      },
      _updateStyle: function _updateStyle(t) {
        this._updateDashArray(t), this._requestRedraw(t);
      },
      _updateDashArray: function _updateDashArray(i) {
        if ("string" == typeof i.options.dashArray) {
          var o = i.options.dashArray.split(/[, ]+/),
            s = [];
          var _t61, _e33;
          for (_e33 = 0; _e33 < o.length; _e33++) {
            if (_t61 = Number(o[_e33]), isNaN(_t61)) return;
            s.push(_t61);
          }
          i.options._dashArray = s;
        } else i.options._dashArray = i.options.dashArray;
      },
      _requestRedraw: function _requestRedraw(t) {
        this._map && (this._extendRedrawBounds(t), this._redrawRequest = this._redrawRequest || x(this._redraw, this));
      },
      _extendRedrawBounds: function _extendRedrawBounds(t) {
        var e;
        t._pxBounds && (e = (t.options.weight || 0) + 1, this._redrawBounds = this._redrawBounds || new m(), this._redrawBounds.extend(t._pxBounds.min.subtract([e, e])), this._redrawBounds.extend(t._pxBounds.max.add([e, e])));
      },
      _redraw: function _redraw() {
        this._redrawRequest = null, this._redrawBounds && (this._redrawBounds.min._floor(), this._redrawBounds.max._ceil()), this._clear(), this._draw(), this._redrawBounds = null;
      },
      _clear: function _clear() {
        var t,
          e = this._redrawBounds;
        e ? (t = e.getSize(), this._ctx.clearRect(e.min.x, e.min.y, t.x, t.y)) : (this._ctx.save(), this._ctx.setTransform(1, 0, 0, 1, 0, 0), this._ctx.clearRect(0, 0, this._container.width, this._container.height), this._ctx.restore());
      },
      _draw: function _draw() {
        var e,
          t,
          i = this._redrawBounds;
        this._ctx.save(), i && (t = i.getSize(), this._ctx.beginPath(), this._ctx.rect(i.min.x, i.min.y, t.x, t.y), this._ctx.clip()), this._drawing = !0;
        for (var _t62 = this._drawFirst; _t62; _t62 = _t62.next) e = _t62.layer, (!i || e._pxBounds && e._pxBounds.intersects(i)) && e._updatePath();
        this._drawing = !1, this._ctx.restore();
      },
      _updatePoly: function _updatePoly(s, n) {
        if (this._drawing) {
          var _t63, _e34, _i20, _o7;
          var a = s._parts,
            r = a.length,
            h = this._ctx;
          if (r) {
            for (h.beginPath(), _t63 = 0; _t63 < r; _t63++) {
              for (_e34 = 0, _i20 = a[_t63].length; _e34 < _i20; _e34++) _o7 = a[_t63][_e34], h[_e34 ? "lineTo" : "moveTo"](_o7.x, _o7.y);
              n && h.closePath();
            }
            this._fillStroke(h, s);
          }
        }
      },
      _updateCircle: function _updateCircle(t) {
        var e, i, o, s;
        this._drawing && !t._empty() && (e = t._point, i = this._ctx, o = Math.max(Math.round(t._radius), 1), 1 != (s = (Math.max(Math.round(t._radiusY), 1) || o) / o) && (i.save(), i.scale(1, s)), i.beginPath(), i.arc(e.x, e.y / s, o, 0, 2 * Math.PI, !1), 1 != s && i.restore(), this._fillStroke(i, t));
      },
      _fillStroke: function _fillStroke(t, e) {
        var i = e.options;
        i.fill && (t.globalAlpha = i.fillOpacity, t.fillStyle = i.fillColor || i.color, t.fill(i.fillRule || "evenodd")), i.stroke && 0 !== i.weight && (t.setLineDash && t.setLineDash(e.options && e.options._dashArray || []), t.globalAlpha = i.opacity, t.lineWidth = i.weight, t.strokeStyle = i.color, t.lineCap = i.lineCap, t.lineJoin = i.lineJoin, t.stroke());
      },
      _onClick: function _onClick(e) {
        var i = this._map.mouseEventToLayerPoint(e);
        var o, s;
        for (var _t64 = this._drawFirst; _t64; _t64 = _t64.next) (o = _t64.layer).options.interactive && o._containsPoint(i) && (("click" === e.type || "preclick" === e.type) && this._map._draggableMoved(o) || (s = o));
        this._fireEvent(!!s && [s], e);
      },
      _onMouseMove: function _onMouseMove(t) {
        var e;
        !this._map || this._map.dragging.moving() || this._map._animatingZoom || (e = this._map.mouseEventToLayerPoint(t), this._handleMouseHover(t, e));
      },
      _handleMouseOut: function _handleMouseOut(t) {
        var e = this._hoveredLayer;
        e && (this._container.classList.remove("leaflet-interactive"), this._fireEvent([e], t, "mouseout"), this._hoveredLayer = null, this._mouseHoverThrottled = !1);
      },
      _handleMouseHover: function _handleMouseHover(t, o) {
        var _this6 = this;
        if (!this._mouseHoverThrottled) {
          var _e35, _i21;
          for (var _t65 = this._drawFirst; _t65; _t65 = _t65.next) (_e35 = _t65.layer).options.interactive && _e35._containsPoint(o) && (_i21 = _e35);
          _i21 !== this._hoveredLayer && (this._handleMouseOut(t), _i21) && (this._container.classList.add("leaflet-interactive"), this._fireEvent([_i21], t, "mouseover"), this._hoveredLayer = _i21), this._fireEvent(!!this._hoveredLayer && [this._hoveredLayer], t), this._mouseHoverThrottled = !0, setTimeout(function () {
            _this6._mouseHoverThrottled = !1;
          }, 32);
        }
      },
      _fireEvent: function _fireEvent(t, e, i) {
        this._map._fireDOMEvent(e, i || e.type, t);
      },
      _bringToFront: function _bringToFront(t) {
        var e,
          i,
          o = t._order;
        o && (e = o.next, i = o.prev, e) && ((e.prev = i) ? i.next = e : e && (this._drawFirst = e), o.prev = this._drawLast, (this._drawLast.next = o).next = null, this._drawLast = o, this._requestRedraw(t));
      },
      _bringToBack: function _bringToBack(t) {
        var e,
          i,
          o = t._order;
        o && (e = o.next, i = o.prev) && ((i.next = e) ? e.prev = i : i && (this._drawLast = i), o.prev = null, o.next = this._drawFirst, this._drawFirst.prev = o, this._drawFirst = o, this._requestRedraw(t));
      }
    });
  function ci(t) {
    return new pi(t);
  }
  function mi(t, e) {
    var i = "",
      o,
      s,
      n,
      a,
      r,
      h;
    for (o = 0, n = t.length; o < n; o++) {
      for (r = t[o], s = 0, a = r.length; s < a; s++) h = r[s], i += (s ? "L" : "M") + h.x + " " + h.y;
      i += e ? "z" : "";
    }
    return i || "M0 0";
  }
  var gi = function gi(t) {
      return document.createElementNS("http://www.w3.org/2000/svg", t);
    },
    fi = A.extend({
      _initContainer: function _initContainer() {
        this._container = gi("svg"), this._container.setAttribute("pointer-events", "none"), this._rootGroup = gi("g"), this._container.appendChild(this._rootGroup);
      },
      _destroyContainer: function _destroyContainer() {
        A.prototype._destroyContainer.call(this), delete this._rootGroup, delete this._svgSize;
      },
      _resizeContainer: function _resizeContainer() {
        var t = A.prototype._resizeContainer.call(this);
        this._svgSize && this._svgSize.equals(t) || (this._svgSize = t, this._container.setAttribute("width", t.x), this._container.setAttribute("height", t.y));
      },
      _update: function _update() {
        var t, e;
        this._map._animatingZoom && this._bounds || (e = (t = this._bounds).getSize(), this._container.setAttribute("viewBox", [t.min.x, t.min.y, e.x, e.y].join(" ")), this.fire("update"));
      },
      _initPath: function _initPath(t) {
        var _e$classList3;
        var e = t._path = gi("path");
        t.options.className && (_e$classList3 = e.classList).add.apply(_e$classList3, _toConsumableArray(l(t.options.className))), t.options.interactive && e.classList.add("leaflet-interactive"), this._updateStyle(t), this._layers[r(t)] = t;
      },
      _addPath: function _addPath(t) {
        this._rootGroup || this._initContainer(), this._rootGroup.appendChild(t._path), t.addInteractiveTarget(t._path);
      },
      _removePath: function _removePath(t) {
        t._path.remove(), t.removeInteractiveTarget(t._path), delete this._layers[r(t)];
      },
      _updatePath: function _updatePath(t) {
        t._project(), t._update();
      },
      _updateStyle: function _updateStyle(t) {
        var e = t._path,
          t = t.options;
        e && (t.stroke ? (e.setAttribute("stroke", t.color), e.setAttribute("stroke-opacity", t.opacity), e.setAttribute("stroke-width", t.weight), e.setAttribute("stroke-linecap", t.lineCap), e.setAttribute("stroke-linejoin", t.lineJoin), t.dashArray ? e.setAttribute("stroke-dasharray", t.dashArray) : e.removeAttribute("stroke-dasharray"), t.dashOffset ? e.setAttribute("stroke-dashoffset", t.dashOffset) : e.removeAttribute("stroke-dashoffset")) : e.setAttribute("stroke", "none"), t.fill ? (e.setAttribute("fill", t.fillColor || t.color), e.setAttribute("fill-opacity", t.fillOpacity), e.setAttribute("fill-rule", t.fillRule || "evenodd")) : e.setAttribute("fill", "none"));
      },
      _updatePoly: function _updatePoly(t, e) {
        this._setPath(t, mi(t._parts, e));
      },
      _updateCircle: function _updateCircle(t) {
        var e = t._point,
          i = Math.max(Math.round(t._radius), 1),
          o = "a".concat(i, ",").concat(Math.max(Math.round(t._radiusY), 1) || i, " 0 1,0 "),
          e = t._empty() ? "M0 0" : "M".concat(e.x - i, ",").concat(e.y).concat(o).concat(2 * i, ",0 ").concat(o).concat(2 * -i, ",0 ");
        this._setPath(t, e);
      },
      _setPath: function _setPath(t, e) {
        t._path.setAttribute("d", e);
      },
      _bringToFront: function _bringToFront(t) {
        wt(t._path);
      },
      _bringToBack: function _bringToBack(t) {
        bt(t._path);
      }
    });
  function yi(t) {
    return new fi(t);
  }
  C.include({
    getRenderer: function getRenderer(t) {
      var e = t.options.renderer || this._getPaneRenderer(t.options.pane) || this.options.renderer || this._renderer;
      return e = e || (this._renderer = this._createRenderer()), this.hasLayer(e) || this.addLayer(e), e;
    },
    _getPaneRenderer: function _getPaneRenderer(t) {
      if ("overlayPane" === t || void 0 === t) return !1;
      var e = this._paneRenderers[t];
      return void 0 === e && (e = this._createRenderer({
        pane: t
      }), this._paneRenderers[t] = e), e;
    },
    _createRenderer: function _createRenderer(t) {
      return this.options.preferCanvas && ci(t) || yi(t);
    }
  });
  var vi = je.extend({
    initialize: function initialize(t, e) {
      je.prototype.initialize.call(this, this._boundsToLatLngs(t), e);
    },
    setBounds: function setBounds(t) {
      return this.setLatLngs(this._boundsToLatLngs(t));
    },
    _boundsToLatLngs: function _boundsToLatLngs(t) {
      return [(t = f(t)).getSouthWest(), t.getNorthWest(), t.getNorthEast(), t.getSouthEast()];
    }
  });
  function xi(t, e) {
    return new vi(t, e);
  }
  fi.create = gi, fi.pointsToPath = mi, k.geometryToLayer = Ne, k.coordsToLatLng = Fe, k.coordsToLatLngs = He, k.latLngToCoords = Ue, k.latLngsToCoords = Ve, k.getFeature = Ge, k.asFeature = qe, C.mergeOptions({
    boxZoom: !0
  });
  var ye = y.extend({
      initialize: function initialize(t) {
        this._map = t, this._container = t._container, this._pane = t._panes.overlayPane, this._resetStateTimeout = 0, t.on("unload", this._destroy, this);
      },
      addHooks: function addHooks() {
        P(this._container, "pointerdown", this._onPointerDown, this);
      },
      removeHooks: function removeHooks() {
        z(this._container, "pointerdown", this._onPointerDown, this);
      },
      moved: function moved() {
        return this._moved;
      },
      _destroy: function _destroy() {
        this._pane.remove(), delete this._pane;
      },
      _resetState: function _resetState() {
        this._resetStateTimeout = 0, this._moved = !1;
      },
      _clearDeferredResetState: function _clearDeferredResetState() {
        0 !== this._resetStateTimeout && (clearTimeout(this._resetStateTimeout), this._resetStateTimeout = 0);
      },
      _onPointerDown: function _onPointerDown(t) {
        if (!t.shiftKey || 0 !== t.button) return !1;
        this._clearDeferredResetState(), this._resetState(), Zt(), St(), this._startPoint = this._map.mouseEventToContainerPoint(t), P(document, {
          contextmenu: Gt,
          pointermove: this._onPointerMove,
          pointerup: this._onPointerUp,
          keydown: this._onKeyDown
        }, this);
      },
      _onPointerMove: function _onPointerMove(t) {
        this._moved || (this._moved = !0, this._box = b("div", "leaflet-zoom-box", this._container), this._container.classList.add("leaflet-crosshair"), this._map.fire("boxzoomstart")), this._point = this._map.mouseEventToContainerPoint(t);
        var t = new m(this._point, this._startPoint),
          e = t.getSize();
        L(this._box, t.min), this._box.style.width = e.x + "px", this._box.style.height = e.y + "px";
      },
      _finish: function _finish() {
        this._moved && (this._box.remove(), this._container.classList.remove("leaflet-crosshair")), Ot(), Et(), z(document, {
          contextmenu: Gt,
          pointermove: this._onPointerMove,
          pointerup: this._onPointerUp,
          keydown: this._onKeyDown
        }, this);
      },
      _onPointerUp: function _onPointerUp(t) {
        0 === t.button && (this._finish(), this._moved) && (this._clearDeferredResetState(), this._resetStateTimeout = setTimeout(this._resetState.bind(this), 0), t = new u(this._map.containerPointToLatLng(this._startPoint), this._map.containerPointToLatLng(this._point)), this._map.fitBounds(t).fire("boxzoomend", {
          boxZoomBounds: t
        }));
      },
      _onKeyDown: function _onKeyDown(t) {
        "Escape" === t.code && (this._finish(), this._clearDeferredResetState(), this._resetState());
      }
    }),
    s = (C.addInitHook("addHandler", "boxZoom", ye), C.mergeOptions({
      doubleClickZoom: !0
    }), y.extend({
      addHooks: function addHooks() {
        this._map.on("dblclick", this._onDoubleClick, this);
      },
      removeHooks: function removeHooks() {
        this._map.off("dblclick", this._onDoubleClick, this);
      },
      _onDoubleClick: function _onDoubleClick(t) {
        var e = this._map,
          i = e.getZoom(),
          o = e.options.zoomDelta,
          i = t.originalEvent.shiftKey ? i - o : i + o;
        "center" === e.options.doubleClickZoom ? e.setZoom(i) : e.setZoomAround(t.containerPoint, i);
      }
    })),
    wi = (C.addInitHook("addHandler", "doubleClickZoom", s), C.mergeOptions({
      dragging: !0,
      inertia: !0,
      inertiaDeceleration: 3400,
      inertiaMaxSpeed: 1 / 0,
      easeLinearity: .2,
      worldCopyJump: !1,
      maxBoundsViscosity: 0
    }), y.extend({
      addHooks: function addHooks() {
        var t;
        this._draggable || (t = this._map, this._draggable = new ae(t._mapPane, t._container), this._draggable.on({
          dragstart: this._onDragStart,
          drag: this._onDrag,
          dragend: this._onDragEnd
        }, this), this._draggable.on("predrag", this._onPreDragLimit, this), t.options.worldCopyJump && (this._draggable.on("predrag", this._onPreDragWrap, this), t.on("zoomend", this._onZoomEnd, this), t.whenReady(this._onZoomEnd, this))), this._map._container.classList.add("leaflet-grab", "leaflet-touch-drag"), this._draggable.enable(), this._positions = [], this._times = [];
      },
      removeHooks: function removeHooks() {
        this._map._container.classList.remove("leaflet-grab", "leaflet-touch-drag"), this._draggable.disable();
      },
      moved: function moved() {
        return this._draggable && this._draggable._moved;
      },
      moving: function moving() {
        return this._draggable && this._draggable._moving;
      },
      _onDragStart: function _onDragStart() {
        var t,
          e = this._map;
        e._stop(), this._map.options.maxBounds && this._map.options.maxBoundsViscosity ? (t = f(this._map.options.maxBounds), this._offsetLimit = _(this._map.latLngToContainerPoint(t.getNorthWest()).multiplyBy(-1), this._map.latLngToContainerPoint(t.getSouthEast()).multiplyBy(-1).add(this._map.getSize())), this._viscosity = Math.min(1, Math.max(0, this._map.options.maxBoundsViscosity))) : this._offsetLimit = null, e.fire("movestart").fire("dragstart"), e.options.inertia && (this._positions = [], this._times = []);
      },
      _onDrag: function _onDrag(t) {
        var e, i;
        this._map.options.inertia && (e = this._lastTime = +new Date(), i = this._lastPos = this._draggable._absPos || this._draggable._newPos, this._positions.push(i), this._times.push(e), this._prunePositions(e)), this._map.fire("move", t).fire("drag", t);
      },
      _prunePositions: function _prunePositions(t) {
        for (; 1 < this._positions.length && 50 < t - this._times[0];) this._positions.shift(), this._times.shift();
      },
      _onZoomEnd: function _onZoomEnd() {
        var t = this._map.getSize().divideBy(2),
          e = this._map.latLngToLayerPoint([0, 0]);
        this._initialWorldOffset = e.subtract(t).x, this._worldWidth = this._map.getPixelWorldBounds().getSize().x;
      },
      _viscousLimit: function _viscousLimit(t, e) {
        return t - (t - e) * this._viscosity;
      },
      _onPreDragLimit: function _onPreDragLimit() {
        var t, e;
        this._viscosity && this._offsetLimit && (t = this._draggable._newPos.subtract(this._draggable._startPos), e = this._offsetLimit, t.x < e.min.x && (t.x = this._viscousLimit(t.x, e.min.x)), t.y < e.min.y && (t.y = this._viscousLimit(t.y, e.min.y)), t.x > e.max.x && (t.x = this._viscousLimit(t.x, e.max.x)), t.y > e.max.y && (t.y = this._viscousLimit(t.y, e.max.y)), this._draggable._newPos = this._draggable._startPos.add(t));
      },
      _onPreDragWrap: function _onPreDragWrap() {
        var t = this._worldWidth,
          e = Math.round(t / 2),
          i = this._initialWorldOffset,
          o = this._draggable._newPos.x,
          s = (o - e + i) % t + e - i,
          o = (o + e + i) % t - e - i,
          t = Math.abs(s + i) < Math.abs(o + i) ? s : o;
        this._draggable._absPos = this._draggable._newPos.clone(), this._draggable._newPos.x = t;
      },
      _onDragEnd: function _onDragEnd(t) {
        var e = this._map,
          i = e.options,
          o = !i.inertia || t.noInertia || this._times.length < 2;
        if (e.fire("dragend", t), o) e.fire("moveend");else {
          this._prunePositions(+new Date());
          var _s7 = this._lastPos.subtract(this._positions[0]),
            _n4 = (this._lastTime - this._times[0]) / 1e3,
            _a5 = i.easeLinearity,
            _r3 = _s7.multiplyBy(_a5 / _n4),
            _h2 = _r3.distanceTo([0, 0]),
            _l2 = Math.min(i.inertiaMaxSpeed, _h2),
            _d3 = _r3.multiplyBy(_l2 / _h2),
            _3 = _l2 / (i.inertiaDeceleration * _a5);
          var _t66 = _d3.multiplyBy(-_3 / 2).round();
          _t66.x || _t66.y ? (_t66 = e._limitOffset(_t66, e.options.maxBounds), x(function () {
            e.panBy(_t66, {
              duration: _3,
              easeLinearity: _a5,
              noMoveStart: !0,
              animate: !0
            });
          })) : e.fire("moveend");
        }
      }
    })),
    bi = (C.addInitHook("addHandler", "dragging", wi), C.mergeOptions({
      keyboard: !0,
      keyboardPanDelta: 80
    }), y.extend({
      keyCodes: {
        left: ["ArrowLeft"],
        right: ["ArrowRight"],
        down: ["ArrowDown"],
        up: ["ArrowUp"],
        zoomIn: ["Equal", "NumpadAdd", "BracketRight"],
        zoomOut: ["Minus", "NumpadSubtract", "Digit6", "Slash"]
      },
      initialize: function initialize(t) {
        this._map = t, this._setPanDelta(t.options.keyboardPanDelta), this._setZoomDelta(t.options.zoomDelta);
      },
      addHooks: function addHooks() {
        var t = this._map._container;
        t.tabIndex <= 0 && (t.tabIndex = "0"), P(t, {
          focus: this._onFocus,
          blur: this._onBlur,
          pointerdown: this._onPointerDown
        }, this), this._map.on({
          focus: this._addHooks,
          blur: this._removeHooks
        }, this);
      },
      removeHooks: function removeHooks() {
        this._removeHooks(), z(this._map._container, {
          focus: this._onFocus,
          blur: this._onBlur,
          pointerdown: this._onPointerDown
        }, this), this._map.off({
          focus: this._addHooks,
          blur: this._removeHooks
        }, this);
      },
      _onPointerDown: function _onPointerDown() {
        var t, e, i;
        this._focused || (i = document.body, t = document.documentElement, e = i.scrollTop || t.scrollTop, i = i.scrollLeft || t.scrollLeft, this._map._container.focus(), window.scrollTo(i, e));
      },
      _onFocus: function _onFocus() {
        this._focused = !0, this._map.fire("focus");
      },
      _onBlur: function _onBlur() {
        this._focused = !1, this._map.fire("blur");
      },
      _setPanDelta: function _setPanDelta(t) {
        var e = this._panKeys = {},
          i = this.keyCodes;
        var o, s;
        for (o = 0, s = i.left.length; o < s; o++) e[i.left[o]] = [-1 * t, 0];
        for (o = 0, s = i.right.length; o < s; o++) e[i.right[o]] = [t, 0];
        for (o = 0, s = i.down.length; o < s; o++) e[i.down[o]] = [0, t];
        for (o = 0, s = i.up.length; o < s; o++) e[i.up[o]] = [0, -1 * t];
      },
      _setZoomDelta: function _setZoomDelta(t) {
        var e = this._zoomKeys = {},
          i = this.keyCodes;
        var o, s;
        for (o = 0, s = i.zoomIn.length; o < s; o++) e[i.zoomIn[o]] = t;
        for (o = 0, s = i.zoomOut.length; o < s; o++) e[i.zoomOut[o]] = -t;
      },
      _addHooks: function _addHooks() {
        P(document, "keydown", this._onKeyDown, this);
      },
      _removeHooks: function _removeHooks() {
        z(document, "keydown", this._onKeyDown, this);
      },
      _onKeyDown: function _onKeyDown(e) {
        if (!(e.altKey || e.ctrlKey || e.metaKey)) {
          var i,
            o = e.code,
            s = this._map;
          var _t67;
          if (o in this._panKeys) s._panAnim && s._panAnim._inProgress || (_t67 = this._panKeys[o], e.shiftKey && (_t67 = g(_t67).multiplyBy(3)), s.options.maxBounds && (_t67 = s._limitOffset(g(_t67), s.options.maxBounds)), s.options.worldCopyJump ? (i = s.wrapLatLng(s.unproject(s.project(s.getCenter()).add(_t67))), s.panTo(i)) : s.panBy(_t67));else if (o in this._zoomKeys) s.setZoom(s.getZoom() + (e.shiftKey ? 3 : 1) * this._zoomKeys[o]);else {
            if ("Escape" !== o || !s._popup || !s._popup.options.closeOnEscapeKey) return;
            s.closePopup();
          }
          Gt(e);
        }
      }
    })),
    Li = (C.addInitHook("addHandler", "keyboard", bi), C.mergeOptions({
      scrollWheelZoom: !0,
      wheelDebounceTime: 40,
      wheelPxPerZoomLevel: 60
    }), y.extend({
      addHooks: function addHooks() {
        P(this._map._container, "wheel", this._onWheelScroll, this), this._delta = 0;
      },
      removeHooks: function removeHooks() {
        z(this._map._container, "wheel", this._onWheelScroll, this);
      },
      _onWheelScroll: function _onWheelScroll(t) {
        var e = Yt(t),
          i = this._map.options.wheelDebounceTime,
          e = (this._delta += e, this._lastMousePos = this._map.mouseEventToContainerPoint(t), this._startTime || (this._startTime = +new Date()), Math.max(i - (+new Date() - this._startTime), 0));
        clearTimeout(this._timer), this._timer = setTimeout(this._performZoom.bind(this), e), Gt(t);
      },
      _performZoom: function _performZoom() {
        var t = this._map,
          e = t.getZoom(),
          i = this._map.options.zoomSnap || 0,
          o = (t._stop(), this._delta / (4 * this._map.options.wheelPxPerZoomLevel)),
          o = 4 * Math.log(2 / (1 + Math.exp(-Math.abs(o)))) / Math.LN2,
          i = i ? Math.ceil(o / i) * i : o,
          o = t._limitZoom(e + (0 < this._delta ? i : -i)) - e;
        this._delta = 0, this._startTime = null, o && ("center" === t.options.scrollWheelZoom ? t.setZoom(e + o) : t.setZoomAround(this._lastMousePos, e + o));
      }
    }));
  C.addInitHook("addHandler", "scrollWheelZoom", Li);
  C.mergeOptions({
    tapHold: v.touchNative && v.safari && v.mobile,
    tapTolerance: 15
  });
  var Pi = y.extend({
      addHooks: function addHooks() {
        P(this._map._container, "touchstart", this._onDown, this);
      },
      removeHooks: function removeHooks() {
        z(this._map._container, "touchstart", this._onDown, this);
      },
      _onDown: function _onDown(t) {
        var _this7 = this;
        if (clearTimeout(this._holdTimeout), 1 === t.touches.length) {
          var _e36 = t.touches[0];
          this._startPos = this._newPos = new c(_e36.clientX, _e36.clientY), this._holdTimeout = setTimeout(function () {
            _this7._cancel(), _this7._isTapValid() && (P(document, "touchend", M), P(document, "touchend touchcancel", _this7._cancelClickPrevent), _this7._simulateEvent("contextmenu", _e36));
          }, 600), P(document, "touchend touchcancel contextmenu", this._cancel, this), P(document, "touchmove", this._onMove, this);
        }
      },
      _cancelClickPrevent: function t() {
        z(document, "touchend", M), z(document, "touchend touchcancel", t);
      },
      _cancel: function _cancel() {
        clearTimeout(this._holdTimeout), z(document, "touchend touchcancel contextmenu", this._cancel, this), z(document, "touchmove", this._onMove, this);
      },
      _onMove: function _onMove(t) {
        t = t.touches[0];
        this._newPos = new c(t.clientX, t.clientY);
      },
      _isTapValid: function _isTapValid() {
        return this._newPos.distanceTo(this._startPos) <= this._map.options.tapTolerance;
      },
      _simulateEvent: function _simulateEvent(t, e) {
        t = new MouseEvent(t, {
          bubbles: !0,
          cancelable: !0,
          view: window,
          screenX: e.screenX,
          screenY: e.screenY,
          clientX: e.clientX,
          clientY: e.clientY
        });
        t._simulated = !0, e.target.dispatchEvent(t);
      }
    }),
    Ti = (C.addInitHook("addHandler", "tapHold", Pi), C.mergeOptions({
      touchZoom: v.touch,
      bounceAtZoomLimits: !0
    }), y.extend({
      addHooks: function addHooks() {
        this._map._container.classList.add("leaflet-touch-zoom"), P(this._map._container, "touchstart", this._onTouchStart, this);
      },
      removeHooks: function removeHooks() {
        this._map._container.classList.remove("leaflet-touch-zoom"), z(this._map._container, "touchstart", this._onTouchStart, this);
      },
      _onTouchStart: function _onTouchStart(t) {
        var e,
          i,
          o = this._map;
        !t.touches || 2 !== t.touches.length || o._animatingZoom || this._zooming || (e = o.mouseEventToContainerPoint(t.touches[0]), i = o.mouseEventToContainerPoint(t.touches[1]), this._centerPoint = o.getSize()._divideBy(2), this._startLatLng = o.containerPointToLatLng(this._centerPoint), "center" !== o.options.touchZoom && (this._pinchStartLatLng = o.containerPointToLatLng(e.add(i)._divideBy(2))), this._startDist = e.distanceTo(i), this._startZoom = o.getZoom(), this._moved = !1, this._zooming = !0, o._stop(), P(document, "touchmove", this._onTouchMove, this), P(document, "touchend touchcancel", this._onTouchEnd, this), M(t));
      },
      _onTouchMove: function _onTouchMove(t) {
        if (t.touches && 2 === t.touches.length && this._zooming) {
          var e = this._map,
            i = e.mouseEventToContainerPoint(t.touches[0]),
            o = e.mouseEventToContainerPoint(t.touches[1]),
            s = i.distanceTo(o) / this._startDist;
          if (this._zoom = e.getScaleZoom(s, this._startZoom), !e.options.bounceAtZoomLimits && (this._zoom < e.getMinZoom() && s < 1 || this._zoom > e.getMaxZoom() && 1 < s) && (this._zoom = e._limitZoom(this._zoom)), "center" === e.options.touchZoom) {
            if (this._center = this._startLatLng, 1 == s) return;
          } else {
            i = i._add(o)._divideBy(2)._subtract(this._centerPoint);
            if (1 == s && 0 === i.x && 0 === i.y) return;
            this._center = e.unproject(e.project(this._pinchStartLatLng, this._zoom).subtract(i), this._zoom);
          }
          this._moved || (e._moveStart(!0, !1), this._moved = !0), d(this._animRequest);
          o = e._move.bind(e, this._center, this._zoom, {
            pinch: !0,
            round: !1
          }, void 0);
          this._animRequest = x(o, this, !0), M(t);
        }
      },
      _onTouchEnd: function _onTouchEnd() {
        this._moved && this._zooming ? (this._zooming = !1, d(this._animRequest), z(document, "touchmove", this._onTouchMove, this), z(document, "touchend touchcancel", this._onTouchEnd, this), this._map.options.zoomAnimation ? this._map._animateZoom(this._center, this._map._limitZoom(this._zoom), !0, this._map.options.zoomSnap) : this._map._resetView(this._center, this._map._limitZoom(this._zoom))) : this._zooming = !1;
      }
    })),
    ye = (C.addInitHook("addHandler", "touchZoom", Ti), C.BoxZoom = ye, C.DoubleClickZoom = s, C.Drag = wi, C.Keyboard = bi, C.ScrollWheelZoom = Li, C.TapHold = Pi, C.TouchZoom = Ti, {
      __proto__: null,
      BlanketOverlay: $e,
      Bounds: m,
      Browser: v,
      CRS: o,
      Canvas: pi,
      Circle: Be,
      CircleMarker: Ee,
      Class: Y,
      Control: Z,
      DivIcon: ai,
      DivOverlay: B,
      DomEvent: nt,
      DomUtil: st,
      Draggable: ae,
      Evented: e,
      FeatureGroup: E,
      GeoJSON: k,
      GridLayer: hi,
      Handler: y,
      Icon: Te,
      ImageOverlay: Ye,
      LatLng: p,
      LatLngBounds: u,
      Layer: S,
      LayerGroup: be,
      LineUtil: rt,
      Map: C,
      Marker: Ze,
      Path: Se,
      Point: c,
      PolyUtil: at,
      Polygon: je,
      Polyline: Ie,
      Popup: ii,
      PosAnimation: Jt,
      Projection: xe,
      Rectangle: vi,
      Renderer: A,
      SVG: fi,
      SVGOverlay: ti,
      TileLayer: di,
      Tooltip: si,
      Transformation: et,
      Util: $,
      VideoOverlay: Je,
      bounds: _,
      canvas: ci,
      circle: Ae,
      circleMarker: ke,
      control: te,
      divIcon: ri,
      extend: h,
      featureGroup: Pe,
      geoJSON: Ke,
      geoJson: ot,
      gridLayer: li,
      icon: ze,
      imageOverlay: Xe,
      latLng: w,
      latLngBounds: f,
      layerGroup: Le,
      map: Qt,
      marker: Oe,
      point: g,
      polygon: De,
      polyline: Re,
      popup: oi,
      rectangle: xi,
      setOptions: n,
      stamp: r,
      svg: yi,
      svgOverlay: ei,
      tileLayer: _i,
      tooltip: ni,
      transformation: it,
      version: I,
      videoOverlay: Qe
    });
  (function () {
    if ("undefined" != typeof globalThis) return globalThis;
    if ("undefined" != typeof self) return self;
    if ("undefined" != typeof window) return window;
    if ("undefined" == typeof global) throw new Error("Unable to locate global object.");
    return global;
  })().L = ye, t.BlanketOverlay = $e, t.Bounds = m, t.Browser = v, t.CRS = o, t.Canvas = pi, t.Circle = Be, t.CircleMarker = Ee, t.Class = Y, t.Control = Z, t.DivIcon = ai, t.DivOverlay = B, t.DomEvent = nt, t.DomUtil = st, t.Draggable = ae, t.Evented = e, t.FeatureGroup = E, t.GeoJSON = k, t.GridLayer = hi, t.Handler = y, t.Icon = Te, t.ImageOverlay = Ye, t.LatLng = p, t.LatLngBounds = u, t.Layer = S, t.LayerGroup = be, t.LineUtil = rt, t.Map = C, t.Marker = Ze, t.Path = Se, t.Point = c, t.PolyUtil = at, t.Polygon = je, t.Polyline = Ie, t.Popup = ii, t.PosAnimation = Jt, t.Projection = xe, t.Rectangle = vi, t.Renderer = A, t.SVG = fi, t.SVGOverlay = ti, t.TileLayer = di, t.Tooltip = si, t.Transformation = et, t.Util = $, t.VideoOverlay = Je, t.bounds = _, t.canvas = ci, t.circle = Ae, t.circleMarker = ke, t.control = te, t.default = ye, t.divIcon = ri, t.extend = h, t.featureGroup = Pe, t.geoJSON = Ke, t.geoJson = ot, t.gridLayer = li, t.icon = ze, t.imageOverlay = Xe, t.latLng = w, t.latLngBounds = f, t.layerGroup = Le, t.map = Qt, t.marker = Oe, t.point = g, t.polygon = De, t.polyline = Re, t.popup = oi, t.rectangle = xi, t.setOptions = n, t.stamp = r, t.svg = yi, t.svgOverlay = ei, t.tileLayer = _i, t.tooltip = ni, t.transformation = it, t.version = I, t.videoOverlay = Qe;
});

}).call(this)}).call(this,typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {})

},{}],13:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initMap = void 0;
var initMap = function initMap() {
  if (document.querySelector('.map')) {
    var map = L.map('map').setView([55.811953, 37.636374], 14);
    L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);
    var icon = new L.Icon({
      iconUrl: 'img/svg/marker-map.svg',
      iconSize: [48, 48]
    });
    L.marker([55.811953, 37.636374], {
      icon: icon
    }).addTo(map);
  }
};
exports.initMap = initMap;

},{}],14:[function(require,module,exports){
"use strict";

var header = document.querySelector('.header');
var menuButton = document.querySelector('.menu__toggle');
var body = document.querySelector('body');
var linkHeader = document.querySelectorAll('.menu a');
function trapFocus(element) {
  var focusableEls = document.querySelectorAll('header a[href]:not([disabled])');
  var firstFocusableEl = focusableEls[0];
  var lastFocusableEl = focusableEls[focusableEls.length - 1];
  var KEYCODE_TAB = 9;
  element.addEventListener('keydown', function (e) {
    var isTabPressed = e.key === 'Tab' || e.keyCode === KEYCODE_TAB;
    if (!isTabPressed) {
      return;
    }
    if (document.activeElement === lastFocusableEl) {
      firstFocusableEl.focus();
      e.preventDefault();
    }
  });
}
var outsideHeaderClick = function outsideHeaderClick(evt) {
  if (evt.target === header && evt.target.closest !== 'header__wrapper') {
    header.classList.remove('open-menu');
    body.style.position = 'inherit';
  }
};
header.classList.remove('no-js');
header.classList.remove('open-menu');
if (document.querySelector('.menu') && menuButton) {
  menuButton.addEventListener('click', function () {
    header.classList.toggle('open-menu');
    if (header.classList.contains('open-menu')) {
      body.style.position = 'fixed';
      trapFocus(header);
      header.addEventListener('click', outsideHeaderClick);
      header.addEventListener('keydown', function (e) {
        e.preventDefault();
        if (e.key === 'Escape' || e.keyCode === 27) {
          header.classList.remove('open-menu');
          body.style.position = 'inherit';
        }
      });
      linkHeader.forEach(function (el) {
        el.addEventListener('click', function () {
          header.classList.remove('open-menu');
          body.style.position = 'inherit';
        });
      });
    } else {
      body.style.position = 'inherit';
    }
  });
}

},{}],15:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initPhotogallerySwiper = void 0;
var initPhotogallerySwiper = function initPhotogallerySwiper() {
  new Swiper('.photogallery__swiper', {
    navigation: {
      nextEl: '.photogallery__swiper .slider-button--next',
      prevEl: '.photogallery__swiper .slider-button--prev'
    },
    // отключение скролла
    simulateTouch: false,
    breakpoints: {
      320: {
        slidesPerView: 2,
        spaceBetween: 5
      },
      768: {
        slidesPerView: 2.75,
        spaceBetween: 5
      },
      1200: {
        slidesPerView: 5,
        spaceBetween: 5
      }
    }
  });
};
exports.initPhotogallerySwiper = initPhotogallerySwiper;

},{}],16:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initReviewsSwiper = void 0;
var initReviewsSwiper = function initReviewsSwiper() {
  new Swiper('.reviews__swiper', {
    navigation: {
      nextEl: '.reviews__swiper .slider-button--next',
      prevEl: '.reviews__swiper .slider-button--prev'
    },
    // отключение скролла
    simulateTouch: false,
    breakpoints: {
      320: {
        // Количество слайдов на странице
        slidesPerView: 1
      },
      768: {
        slidesPerView: 1.2,
        spaceBetween: 30
      },
      1200: {
        slidesPerView: 1.5,
        spaceBetween: 30
      }
    }
  });
};
exports.initReviewsSwiper = initReviewsSwiper;

},{}],17:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initToursSwiper = void 0;
var initToursSwiper = function initToursSwiper() {
  new Swiper('.tours__swiper', {
    navigation: {
      nextEl: '.tours__swiper .slider-button--next',
      prevEl: '.tours__swiper .slider-button--prev'
    },
    // Задает класс активным слайдам
    watchSlidesProgress: true,
    // Задаем класс активным слайдам
    slideVisibleClass: 'slider__slide--visible',
    // отключение скролла
    simulateTouch: false,
    breakpoints: {
      320: {
        // Количество слайдов на странице
        slidesPerView: 1
      },
      768: {
        slidesPerView: 2,
        // Отступ между слайдами
        spaceBetween: 18
      },
      1200: {
        slidesPerView: 3,
        spaceBetween: 30
      }
    }
  });
};
exports.initToursSwiper = initToursSwiper;

},{}],18:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initTrainingSwiper = void 0;
var initTrainingSwiper = function initTrainingSwiper() {
  new Swiper('.training__swiper', {
    navigation: {
      nextEl: '.training__swiper .slider-button--next',
      prevEl: '.training__swiper .slider-button--prev'
    },
    // Задает класс активным слайдам
    watchSlidesProgress: true,
    // Задаем класс активным слайдам
    slideVisibleClass: 'slider__slide--visible',
    // отключение скролла
    simulateTouch: false,
    breakpoints: {
      320: {
        // Количество слайдов на странице
        slidesPerView: 1
      },
      768: {
        slidesPerView: 3,
        // Отступ между слайдами
        spaceBetween: 30
      },
      1200: {
        slidesPerView: 4,
        spaceBetween: 30
      }
    }
  });
};
exports.initTrainingSwiper = initTrainingSwiper;

},{}],19:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.initVideo = void 0;
var initVideo = function initVideo() {
  var video = document.querySelector('video');
  var buttonPlay = document.querySelector('.hero__video-button');
  if (video && buttonPlay) {
    buttonPlay.addEventListener('click', function () {
      if (video.classList.contains('is-active')) {
        video.classList.remove('is-active');
      } else {
        video.play();
        video.setAttribute('controls', '');
        video.classList.add('is-active');
        buttonPlay.classList.add('visually-hidden');
      }
    });
  }
};
exports.initVideo = initVideo;

},{}],20:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.iosChecker = void 0;
var iosChecker = function iosChecker() {
  return ['iPad Simulator', 'iPhone Simulator', 'iPod Simulator', 'iPad', 'iPhone', 'iPod'].includes(navigator.platform)
  // iPad on iOS 13 detection
  || navigator.userAgent.includes('Mac') && 'ontouchend' in document;
};
exports.iosChecker = iosChecker;

},{}],21:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.iosVhFix = void 0;
var _iosChecker = require("./ios-checker");
var iosVhFix = function iosVhFix() {
  if (!(!!window.MSInputMethodContext && !!document.documentMode)) {
    if ((0, _iosChecker.iosChecker)()) {
      var vh = window.innerHeight * 0.01;
      document.documentElement.style.setProperty('--vh', "".concat(vh, "px"));
      window.addEventListener('resize', function () {
        vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', "".concat(vh, "px"));
      });
    }
  }
};
exports.iosVhFix = iosVhFix;

},{"./ios-checker":20}]},{},[1])


//# sourceMappingURL=main.min.js.map
